# Retro 2026-08-07 — la semana en que verify-source enseñó su borde

**TL;DR:** Semana con MUCHA señal, casi toda de una sola familia: **afirmar más de lo que la evidencia sostiene**. Ocho instancias en dos sesiones (3 el 05-08 de "comprobación parcial", 5 el 06-08 de "instrumento incapaz"), con `verify-source` cargada y activa las ocho veces. El diagnóstico no es que la regla falló: es que la regla funciona por sus ejemplos concretos, y las instancias nuevas caen fuera de ellos. La mitad "cobertura" ya se enmendó el mismo 05-08 (punto 4); la mitad "capacidad del instrumento" quedó propuesta en el friction-log y **sin construir** — es la propuesta 1. La 2 es el gate que Andrés pidió anotar (nombrar la superficie consultada en el contrato de reporte). La 3 es un watch (session-close no sabe qué proyecto → qué vault). En lo bueno: v0.13.0 (patterns + rules-have-loaders) atacó una enfermedad de nivel superior con medición contundente, capture incremental funciona (las entradas del 05–06 son las más ricas del log), y el buzón está a cero pendientes — el loop decidió en cadencia por primera vez. Un despiste de housekeeping: **v0.13.0 subió versión sin entrada en CHANGELOG**.

## Necesito que decidas

1. **Propuesta 1 — extender `verify-source` con el eje de capacidad del instrumento.** Evidencia ×5 en una sesión. El texto ya está redactado en el friction-log (08-06): tres ejemplos nuevos ("una pasada no es una medida", "verde solo dice lo que el check mira", "si lo medido y lo observado se contradicen, duda del instrumento primero") + la pregunta forzada ("antes de afirmar un número o diagnóstico, di qué NO habría podido detectar tu instrumento"). Inline, sin gate posible. Recomendación: **aprobar** — es la señal más fuerte de la semana y el fix ya está diseñado.
2. **Propuesta 2 — cláusula de "superficie nombrada" en el contrato de reporte.** Tú mismo la encargaste el 05-08: los tres fallos los cazaste tú preguntando; las afirmaciones en prosa no las ve ningún grep. El mecanismo: al afirmar un hecho verificado, el reporte nombra QUÉ superficie se consultó, de forma que omitirlo sea visible en el propio texto. Riesgo conocido: puede engordar los reportes — la cláusula debe pedirlo solo para afirmaciones load-bearing, no para cada frase. Recomendación: **aprobar acotada**.
3. **Propuesta 3 — mapa proyecto→vault en session-close/open.** 1ª aparición (08-06, daily AE): el ritual asume el esquema clásico y Landbot vive en `tech-knowledge` con otro esquema. Recomendación: **watch, no construir** (sesgo a no crear; materializar en 2ª aparición).
4. **Housekeeping, no propuesta:** falta la entrada v0.13.0 en `meta/CHANGELOG.md` (la versión subió, los commits existen, el registro no). Dos líneas desde el Mac. Irónico y relevante: la enfermedad de v0.13.0 era exactamente "el último tramo, la entrega".

## Síntomas (capa 1)

Ventana 2026-08-01 → 2026-08-07. Del friction-log y las sesiones de Cowork:

- **Afirmaciones no sostenidas por la evidencia: 8** (3 el 05-08 tipo cobertura-parcial; 5 el 06-08 tipo instrumento-incapaz). Las ocho con `verify-source` cargada. Todas cazadas por Andrés o por la realidad, ninguna por la regla.
- **Rework por ceguera de patrón: 1 grande** (06-08: tres horas moviendo documentos de diseño sin cuestionar si el documento era el mecanismo; interrumpido por Andrés con "matriushkas").
- **Gates que no cubren lo que dicen cubrir: ~5 bugs propios en una sesión** (06-08: check de tokens ciego a JSX, `grep -v` sin escapar el punto, gate de jerga mirando la carpeta equivocada, y el gate nuevo repitiendo la forma dos veces).
- **miss-vault: 1** (session-close sin mapa proyecto→vault, daily AE).
- **Re-explicaciones / for dummies: 0. Overrides de routing: 0. Arranques en frío: 0.** Las sesiones de Cowork de la semana (smoke de filtros, PR del wizard, digestión de mocks AE) salieron limpias y con el contrato aplicado.

## Enfermedades (capa 2)

**1. `verify-source` ve la cobertura pero no la capacidad (dominante, 8 instancias en 2 ejes).** La regla dispara con sus ejemplos: "un fichero no es el repo, una tabla no es el esquema" cazan subconjuntos no mirados. Ninguno de sus ejemplos describe "el instrumento no puede contestar la pregunta que le haces" (un EXPLAIN suelto no resuelve 300 ms; un navegador degradado no es producción; un check verde solo dice lo que mira). El 06-08 lo demostró cinco veces seguidas: cada vez que el defecto era de CÓDIGO, "patrón antes que reparación" sí disparó; cuando el defecto estaba en la EVIDENCIA, nada disparó. No es diagnóstico fallido a reabrir: es una regla correcta con el dominio recortado. → Propuestas 1 y 2 (la 1 corrige el juicio antes de afirmar; la 2 hace visible en el texto cuándo no se ejerció).

**2. Los tres fallos en prosa no tienen gate posible — solo visibilidad (encargada por Andrés).** El caso "borrar algo con id" ya se gateó (regla en el CLAUDE.md de MintStash). Los otros dos son afirmaciones; el único mecanismo es que el contrato exija nombrar la superficie, haciendo la omisión detectable a ojo. → Propuesta 2.

**3. El ritual de cierre asume un solo esquema de vault (1ª aparición).** Landbot capta a `tech-knowledge` (Dec-/Got-/Sys-/Ref-), MintStash a `mintstash-knowledge`, el sistema al vault clásico; session-close/open no declara el mapa y hubo que buscarlo a mano. → Propuesta 3, como watch.

Mención sin propuesta (vive en mintstash, no en el plugin): **"un gate es código sin tests"** — ningún check de `review.sh` tiene fixture que viole la regla a propósito. El fix pertenece al repo de mintstash; el plugin ya practica la contramedida (rules-have-loaders nació con prueba negativa). Si reaparece en gates DEL SISTEMA, sube a propuesta.

## Auto-auditoría (capa 3)

| Fix | Métrica objetivo | Veredicto |
|---|---|---|
| verify-source (v0.11.0) | 0 diagnósticos afirmados sin verificar | **La aguja se movió EN CONTRA — pero por borde, no por núcleo.** 0 recurrencias de "explicar de memoria" (su enfermedad original); 8 instancias de dos formas vecinas que sus ejemplos no describen. La enmienda de cobertura (05-08) ya está dentro; la de capacidad es la propuesta 1. No se reabre el diagnóstico: se extiende el dominio. |
| Frontera de escrituras en mounts (v0.12.0) | 0 fricción de mount | **Sostiene.** 0 entradas nuevas; la retro 08-01 y esta escriben con herramientas de archivo, sin git desde el sandbox. |
| patterns + rules-have-loaders (v0.13.0) | rework por ceguera de patrón; reglas sin vía de carga | **Demasiado fresco para medir** (construido 06-08). Su medición de nacimiento fue contundente (reglas en CLAUDE.md desplegado: 0 incumplimientos; reglas solo en docs/: masivamente incumplidas). Auditar la próxima retro. **Defecto de entrega detectado: sin entrada en CHANGELOG.** |
| Capture incremental (v0.9.0) | 0 sesiones con fricción sin loggear | **Win claro.** Las entradas del 05–06 se escribieron en el momento, con detalle de causa raíz — el log es hoy la mejor fuente del sistema. |
| Loop retro→decisión (v0.8.0) | decidido en cadencia | **Demostrado por primera vez.** Buzón a 0 pendientes; las filas del 08-01 se decidieron y construyeron el mismo día; los commits "falta commit" del plugin están en el repo (bd6fcf0…f90c8e6). Los builds lado-mintstash (anomaly-debug, sección CLAUDE.md) no puedo verificarlos desde aquí — hueco menor. |
| Hook vault-freshness (v0.7.0) | 0 miss-vault por staleness | Métrica quieta, **mecanismo sigue sin confirmar en Cowork** (ninguna sesión reportó ver el bloque inyectado). Caveat vivo, 3ª semana. El miss-vault del 06-08 es de routing de esquema, no de staleness — no cuenta contra este hook. |
| Comms / registro / design gate | correcciones de registro; rework de arquitectura | **Sostienen** (0 señal en contra en la ventana). |

## skill-candidates

Ninguna cruzó umbral. **Enforcement general**: sigue aparcado — los 8 fallos de la semana ocurrieron con la regla cargada y leída; no es dilución de prosa, es forma de la regla. **Ops de volumen**: sin nueva ocurrencia (sigue en 1). **capture-audit**: sin señal.

## Barrido del vault

`meta/` sigue durable. Dos apuntes: (1) el friction-log ya tiene arcos cerrados compactables — las entradas localhost 06-27/07-08 (invalidadas el 07-25) y el arco trial-backend pueden comprimirse a una línea cada uno cuando toque; no urge. (2) La entrada CHANGELOG de v0.13.0 falta (housekeeping arriba). Nada que borrar.

## Confianza y huecos

**Confianza alta en el diagnóstico dominante**: las 8 instancias están documentadas con detalle inusual (capture incremental funcionando) y el propio friction-log ya trae el fix redactado — esta retro sobre todo lo formaliza en el buzón. **Media en la auto-auditoría de v0.13.0** (un día de vida, sin ventana). Huecos: (a) las sesiones de Claude Code de la semana no son legibles desde aquí — el friction-log las cubre, pero solo lo que se loggeó; (b) los builds lado-mintstash del 08-01 sin verificar; (c) el hook SessionStart en Cowork sigue sin confirmación directa, tercera semana — si nadie lo observa la próxima, propongo un test explícito de un minuto en vez de seguir arrastrando el caveat. Writes de esta corrida: solo este archivo y las filas del buzón, según v0.8.0.
