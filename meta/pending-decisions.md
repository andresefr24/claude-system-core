# Pendientes de decisión

El buzón entre la retro y Andrés. Cada retro deposita aquí sus propuestas; ninguna propuesta
se considera viva hasta tener fila aquí, y ninguna sale del buzón sin decisión registrada.
Es el ÚNICO archivo que una corrida automática de retro puede escribir.

Estados: **pendiente** · **aprobada → vX.Y.Z** · **rechazada (razón en una línea)**.
Una rechazada con razón vale tanto como una aprobada: evita re-proponerla.

| Fecha retro | Propuesta | Enfermedad que ataca | Estado |
|---|---|---|---|
| 2026-06-26 | Guard de freshness del vault | el vault canónico no es lo que se consume (miss-vault ×4) | Aprobada → v0.7.0 (hook `vault-freshness`) — decidida 2026-07-25 |
| 2026-06-26 | Regla de comms dentro de Docs | audiencia+registro+canal no eran inputs de primera clase | Aprobada → v0.7.0 (`rules/comms.md`) — decidida 2026-07-25 |
| 2026-06-26 | Mantener aparcado el hook de enforcement general (no-acción) | disciplina de auto-auditoría | Confirmada 2026-07-25 — sigue aparcado (ver `skill-candidates.md`) |
| 2026-07-07 | Cerrar el loop retro→decisión (este archivo + wiring en retro/session-open) | las propuestas de retro morían sin decisión registrada | Aprobada → v0.8.0 — decidida 2026-07-25 |
| 2026-07-07 | Registro llano promovido a regla global | la cláusula de registro (v0.6.1) no cubría explicaciones ad-hoc / digestiones | Aprobada → v0.8.0 (`rules/registro.md`) — decidida 2026-07-25 |
| 2026-07-07 | "UI local → deploy a preview" en la regla de verificación | — | Rechazada — premisa falsa: Claude-in-Chrome sí entra a localhost (Andrés, 2026-07-25); corrección anotada en friction-log |
| 2026-07-25 | Autorizar a la retro un write a `pending-decisions.md` | propuestas de retro morían en transcripts efímeros | Ya resuelta por v0.8.0 antes de esta retro — sin acción |
| 2026-07-25 | Codificar límites de verificación de UI en navegador (viewport real declarado; verificar limitaciones contra el presente) | la regla de verificación asumía capacidades no escritas del tooling (viewport cap ×2) | Aprobada → v0.9.0 (`rules/routing.md`) — decidida 2026-07-25. Parte localhost excluida (rechazada 07-07) |
| 2026-07-25 | Capture incremental de fricción + friction-log único de sistema | fricción acoplada al cierre ordenado se perdía en pivots (idealista, cohort); log duplicado en tech-knowledge | Aprobada → v0.9.0 (`rules/session-close.md`) — decidida 2026-07-25 |
| 2026-07-17 | Cerrar "prod ≠ local sin aviso" en mintstash (remote inalcanzable ×3 + debugging fantasma 16-jul) | la ceremonia de release/Actions asume push por CLI que este setup no tiene; `main` local se adelanta a `origin` sin señal | Aprobada — decidida 2026-07-25. Resolución raíz: Andrés configura el remote con acceso completo (acción suya, fuera del plugin). Sin regla-parche de deploy-state; reabrir si el drift recurre con el remote ya autenticado |
| 2026-07-17 | Design gate: en decisiones que fijan estructura, exponer 2-3 opciones con tradeoffs antes de codificar | ante problemas que moldean arquitectura se salta a la primera solución que funciona (oracle-price: migración one-way rehecha como flag) | Aprobada → v0.10.0 (`rules/routing.md`) — decidida 2026-07-25 |
| 2026-07-25 | Auditar que los fixes v0.7.0–v0.10.0 movieron su aguja (línea base por fix en `retro-2026-07-25.md`) | disciplina de auto-auditoría capa-3: los fixes shipearon el mismo día, sin poder medir | Cerrada 2026-08-01 (Andrés): auditoría ejecutada, ver `retro-2026-08-01.md`. Sin regresiones; caveats vivos (hook SessionStart sin confirmar en Cowork; cadencia del loop sin probar) pasan a watch |
| 2026-07-25 | Promote/demote explícito de los 3 skill candidates de MintStash "at threshold" desde hace ≥2 meses: prompt-writing para CC (abril, 25+ evidencias), visual audit con capturas (abril), MCP trust-debugging (mayo, 8+). Detalle en `mintstash-knowledge/skill-candidates.md` §Imminent | la sección "Imminent" exige promote o demote y lleva meses sin decisión — el pipeline de skills se atasca en el último paso (detectado en la auditoría del vault 2026-07-25; encargado por Andrés a la retro) | Decidida 2026-08-01 (Andrés, con vault ya montado): **#1 prompt-writing → PROMOTE** pero revisar venue (usa CC directo ahora → probablemente convención CC-side / slash-command, no skill desktop). **#2 visual audit → DEMOTE** (a working-preferences; reabrir solo en fase Dashboard audit). **#3 MCP trust-debugging → PROMOTE pero RE-ENMARCADO** → ver fila `verify-source` abajo. **Builds hechos 2026-08-01** (repo de mintstash montado): #1 = sección "Delegating work" en `mintstash/CLAUDE.md` (venue elegido por Andrés); #3-helper = skill `mintstash/.claude/skills/anomaly-debug/`; #2 demote registrado en `mintstash-knowledge/skill-candidates.md`. Falta commit (Mac/CC) |
| 2026-08-01 | Regla base "verificar contra la fuente antes de afirmar" en `claude-system-core/rules/` (always-on, junto a report-contract/registro/comms), con la skill de trust-debugging como procedimiento-helper que la regla apunta | la enfermedad "explicar de memoria en vez de verificar" (16, 17-jul; 2/3 en friction-log) es un hueco de comportamiento BASE, no un workflow ausente; curarla con un skill *triggered* trata el síntoma —el skill no dispara justo cuando el modelo no reconoce que debe verificar—. La cura es regla siempre-activa (insight de Andrés 2026-08-01). Nota: Andrés elige atacarla ya, aunque estaba en 2/3, por su valor estructural | Aprobada y CONSTRUIDA 2026-08-01 (Andrés) → **v0.11.0**: `rules/verify-source.md` (genérica, portable) + wiring en `CLAUDE.md.template`. Desacoplada tras nota de Andrés: la regla describe el helper en abstracto, el trust-debugging de mintstash es una instancia que vive en su repo, no una dependencia del plugin. Helper construido en `mintstash/.claude/skills/anomaly-debug/`. Falta commit (Mac/CC) |
| 2026-08-01 | Extender el feed del friction-log a proyectos no-mintstash, o aceptar el punto ciego de forma explícita | capture incremental (v0.9.0) solo alcanza los proyectos cableados al sistema; los nacidos fuera (security-report-app: export de v0, sin vault ni `CLAUDE.md` apuntando al sistema) no tienen ruta para escribir al log único de v0.9.0 → su fricción de proceso se evapora sin dejar rastro | Aprobada como WATCH — decidida 2026-08-01 (Andrés). No parche; materializar solo en 2ª aparición del hueco |
| 2026-08-01 | Regla "escrituras/git en repos montados → Claude Code / Mac; el sandbox de Cowork solo para lecturas y para lo que no toca `.claude/`" | las escrituras sobre repos reales desde el sandbox van a contracorriente del mount: crea-pero-no-borra (`rm` vetado sin permiso) y el Write tool protege `.claude/`. 3ª manifestación (locks de git 07-25, `rm` de archivos 07-25, y ahora `.claude/` + huérfano al construir `anomaly-debug` 08-01) → cruza la regla de tres. No es imposible (bash + permiso de borrado), pero es semi-seguro y frágil | Aprobada y CONSTRUIDA 2026-08-01 (Andrés) → **v0.12.0**: subsección "Escrituras y git en repos montados desde el sandbox de Cowork" en `rules/routing.md`. Falta commit (Mac/CC) |
| 2026-08-07 | Extender `rules/verify-source.md` con el eje de CAPACIDAD del instrumento: tres ejemplos nuevos ("una pasada no es una medida", "verde solo dice lo que el check mira", "si lo medido y lo observado se contradicen, duda del instrumento primero") + pregunta forzada antes de afirmar un número o diagnóstico ("¿qué NO habría podido detectar mi instrumento?"). Texto ya redactado en friction-log 08-06. Inline; sin gate posible | la regla funciona por sus ejemplos concretos y todos son de COBERTURA (subconjunto no mirado); ninguno describe "el instrumento no puede contestar la pregunta". 5 instancias el 06-08 (firma SQL en falso verde ×2, navegador degradado ≈ caída fantasma, EXPLAIN suelto ≈ regresión inventada, llamada en frío ≈ presupuesto al 84%) con la regla cargada y sin disparar. Recomendación: aprobar | Aprobada → v0.14.0 (punto 5 de `verify-source`) — decidida 2026-08-07 |
| 2026-08-07 | Cláusula de "superficie nombrada" en `rules/report-contract.md`: al afirmar un hecho verificado load-bearing, el reporte nombra QUÉ superficie se consultó (fichero, tabla, query, estado vivo), de forma que omitirla sea visible en el propio texto. Acotada a afirmaciones load-bearing para no engordar reportes | los fallos de verificación en PROSA no tienen gate posible (ningún grep ve una afirmación): los 3 del 05-08 los cazó Andrés preguntando. Encargo explícito de Andrés en friction-log 08-05 ("falta un gate... el único mecanismo plausible es que el contrato exija nombrar la superficie"). Complementa la fila anterior: aquella corrige el juicio, esta hace visible cuándo no se ejerció. Recomendación: aprobar acotada | Aprobada → v0.14.0 (sección "Superficie nombrada" en `report-contract`, acotada a load-bearing) — decidida 2026-08-07 |
| 2026-08-07 | Mapa proyecto→vault en session-close/session-open (declarado o detectado por cwd/rama): Landbot → `tech-knowledge` (esquema Dec-/Got-/Sys-/Ref-), MintStash → `mintstash-knowledge`, sistema → vault clásico | el ritual de cierre asume un solo esquema de vault (`active-context.md`/`decisions.md`/`gotchas.md`) y en el daily AE 08-06 hubo que buscar a mano dónde capturar. 1ª aparición. Recomendación: WATCH, no construir — materializar en 2ª aparición (sesgo a no crear) | Aprobada como WATCH — decidida 2026-08-07. **2ª APARICIÓN REGISTRADA 2026-08-10 → la condición se ha cumplido, procede materializar.** Re-listada por la retro 2026-08-14 como Propuesta 3, con diseño de materialización (rituales por CATEGORÍAS + mapeo declarado en `CONVENTIONS` de cada vault + grep del vault antes de proponer un delta); ver `retro-2026-08-14.md` §P3. Mordió por los dos extremos en la misma sesión de MintStash: al ABRIR, el retrieve pide «los cuatro» y este vault no tiene `decisions.md` ni `gotchas.md` —las decisiones son ficheros `decision-*.md` fechados y los gotchas viven repartidos entre `working-preferences.md` y `tech-debt.md`—, así que hubo que deducir el mapeo a mano y se avisó en el primer mensaje, sin respuesta; al CERRAR, hubo que volver a decidir a mano el destino de cada delta. Nota de alcance que añade esta 2ª aparición y la 1ª no tenía: **el mapeo no es solo proyecto→carpeta, es proyecto→ESQUEMA** (qué fichero hace de `decisions.md` y de `gotchas.md` en cada vault), que es la parte que se deduce cada vez. **Aprobada → v0.15.0** (`session-open.md` + `session-close.md`: categorías + mapeo en `CONVENTIONS` + grep antes de proponer) — decidida 2026-08-14 |
| 2026-08-07 | Cláusula de densidad de ámbitos en `rules/registro.md` (una idea por frase; enumeraciones → bullets; no abrir ámbito nuevo en subordinada) | `registro.md` regulaba el NIVEL de lenguaje pero no la DENSIDAD de ámbitos por frase; doble "for dummies" en la conversación de la retro 08-07 con la regla activa, causa nombrada por Andrés. 1ª aparición, materializada en pausa explícita de revisión | Aprobada → v0.14.0 — decidida 2026-08-07 |
| 2026-08-07 | Sugerir el cierre por producción (commits/deliverables), no por longitud de sesión — cláusula en `session-close.md` | las sugerencias de corte se apoyaban solo en la longitud de la conversación, indicador no fiable; la producción real es verificable. Petición directa de Andrés, no de retro | Aprobada → v0.14.0 — decidida 2026-08-07 |
| 2026-08-07 | Scrub de datos sensibles en capture/ingest (accionable del informe /insights 08-05) | leak real de nombres de clientes en una entrada de KB pese a instrucción explícita; el gate de preview no lo caza porque resume | Aprobada → v0.14.0 — decidida 2026-08-07 |
| 2026-08-07 | "Setup antes de mirar" en verificación de UI (accionable del informe /insights 08-05) | fricción recurrente de browser: server viejo, cwd equivocado, viewport sin confirmar (375px auditando desktop) | Aprobada → v0.14.0 — decidida 2026-08-07 |
| 2026-08-14 | Escalera de enforcement: regla violada ≥2 veces estando CARGADA → se reescribe como sustituto concreto o gate, nunca como más prosa (cláusula en `rules/patterns.md` o `retro.md`) + encolar 3 builds maduros lado-CC: (a) `node -v` vs `.nvmrc` al arrancar tarea de rama código, (b) bloqueo mecánico de `npx vitest` con sustituto `npm test -- <fichero>` (hook PreToolUse), (c) disciplina git-escritura (`git add` antes de mutar/restaurar ficheros nuevos; sin `2>/dev/null` en comandos que escriben; `git diff --cached --name-only` tras add). Revalidar los builds contra el árbol al abordarlos (lección TODO-stale 08-11) | el fallo ya no es el loader, es el DISPARADOR: toda regla que falló esta semana estaba cargada, leída y a veces citada el mismo día (`npx vitest` ×2, Node ×3, mejora afirmada con la regla citada). Discriminante demostrado en ambas direcciones: "«Nunca X» falló tres veces; «para este caso, teclea Y» funcionó a la primera" + gate de Node rojo en su primera hora sobre condición real. Detalle en `retro-2026-08-14.md` §P1 | Aprobada → v0.15.0 (escalera en `patterns.md`; los 3 builds encolados lado-CC, ver CHANGELOG) — decidida 2026-08-14 |
| 2026-08-14 | Reabrir el fix de densidad (v0.14.0) y sustituirlo por un TOPE ESTRUCTURAL en `report-contract`/`registro`: el reporte por defecto ES la versión corta (veredicto en una línea + "necesito que decidas" numerado + huecos, nada más); el recorrido solo bajo demanda; escribir primero la versión de cinco líneas | fix v0.14.0 FALLÓ su métrica (objetivo 0 "for dummies"; hubo 5 en 3 días consecutivos con la cláusula cargada). Diagnóstico reabierto: la densidad crece con el trabajo verificado acumulado — el reporte degenera en registro de lo hecho; una preferencia de estilo no puede contra ese gradiente, un límite estructural sí. Es además la señal de dilución que el candidato aparcado "enforcement general" esperaba — la cura elegida es el tope, no el hook re-afirmador. Detalle en `retro-2026-08-14.md` §P2 | Aprobada → v0.15.0 (tope estructural en `report-contract.md` + puntero en `registro.md`) — decidida 2026-08-14 |
| 2026-08-14 | Cambio de criterio en `hooks/rules-have-loaders.sh` (del brief del harness, §5): clasificar cada regla en *enforzable-por-hook* vs *solo-criterio*, y FALLAR cuando una enforzable esté cargando solo por skill. Con prueba negativa, como el original | el check acepta "skill" como vía de carga, pero una skill dispara porque el modelo decide invocarla — justo la decisión que el harness quiere quitar. El meta-check sale verde midiendo la enfermedad; con el cambio, el propio harness dice qué queda por bajar | Aprobada 2026-08-14 — **build encolado** (sesión de sistema en Mac/CC: exige clasificar las 11 reglas y probar el rojo; revalidar contra el árbol al abordar) |
| 2026-08-14 | Core silencioso por defecto (del brief del harness, §7): `vault-freshness.sh` sale callado con exit 0 donde no encuentra vault, en vez de emitir "vault vacío" en cada sesión de proyectos sin él | core dispara escenarios que no aplican fuera de proyectos con vault (fricción Landbot 08-06: el aviso es útil en MintStash y ruido en cada sesión de AE); principio: core silencioso por defecto, ruidoso por opt-in | Aprobada 2026-08-14 — **build encolado** (misma sesión de sistema que el criterio de `rules-have-loaders`; con prueba de las dos ramas: calla sin vault, habla con él) |
| 2026-08-21 | **Sello de versión al arrancar**: el hook de arranque imprime la versión del plugin CARGADO y su ruta de procedencia, y la compara contra la versión del repo cuando éste es alcanzable; si difieren, lo dice en la primera línea de la sesión. Corolario de modelo: «instalado» deja de ser un booleano y pasa a ser una pregunta POR RUNTIME (Claude Code / Cowork) | el sistema no sabe qué versión de sí mismo corre, y hay más de un sitio donde correrla. **Evidencia dura de esta corrida:** el plugin cargado es **v0.5.0** contra **v0.15.0** del repo — faltan `verify-source`, `registro`, `comms`, `patterns`, `inception`, las versiones nuevas de `report-contract` y los rituales, y el directorio `hooks/` ENTERO (superficies: `Read` de `.../plugin_0118KkTYNnaWLPhdd8viUtcG/.claude-plugin/plugin.json` → `"version": "0.5.0"`; glob del mismo árbol → 6 reglas, sin `hooks/`; `ls plugins/core/rules/` → 11 reglas + 3 hooks). 2ª instancia independiente: sesión del 08-18 abierta con `session-open.md` cuatro días superseded, detectada 8 turnos después por `diff` a mano (friction 273, que nombra la forma: «un hook que verifica la frescura de todo menos la suya»). **Reabre v0.14.2**, que curó el camino de CC y no contempló un segundo runtime — 2ª violación ⇒ por la escalera de `patterns.md` deja de ser prosa y pasa a gate. **Cierra además el caveat de 4 semanas** «SessionStart sin confirmar en Cowork»: no es que no se confirmara, es que la versión que Cowork carga no tiene hooks. Recomendación: aprobar, y primero — sin esto las otras dos no se pueden medir | **pendiente** (re-listada 2026-08-28: **2ª medición semanal, mismo valor** — Cowork sigue en v0.5.0 contra v0.15.0 del repo, 6 reglas y ningún `hooks/`. Tercera semana seguida con la capa 3 de la retro comida por esto. **Re-listada 2026-09-04: 3ª medición semanal, mismo valor** — `.remote-plugins/plugin_0118KkTYNnaWLPhdd8viUtcG/.claude-plugin/plugin.json` → `"version": "0.5.0"`; su `rules/` → 6 ficheros (`ingest`, `report-contract`, `retro`, `routing`, `session-close`, `session-open`); su `hooks/` → no existe. Detalle nuevo de esta corrida: la retro siguió el `retro.md` DEL REPO solo porque se leyó por ruta explícita; una corrida que se dejara llevar por la skill cargada habría seguido el de v0.5.0 — sin loop de decisión, sin auditor externo, sin «la entrega es parte del cambio». 14 días sin decisión). **Re-listada 2026-09-11: 4ª medición semanal, mismo valor, y con ALCANCE AMPLIADO — 21 días sin decisión.** Superficies de esta corrida: `…/claude-hostloop-plugins/ea2bfa78f10631c9/plugin_0118KkTYNnaWLPhdd8viUtcG/.claude-plugin/plugin.json` y `…/rpm/plugin_0118KkTYNnaWLPhdd8viUtcG/.claude-plugin/plugin.json`, las dos `"version": "0.5.0"`, las dos con 6 reglas y sin `hooks/`. **Lo nuevo es un modo de fallo que el sello, tal como está redactado, NO detectaría:** desde v0.15.0 han aterrizado en `rules/` tres ediciones **sin subir versión y sin entrada de CHANGELOG** (`056cd29` 08-18, `d8e3a7a` y `b437436` 09-08; CHANGELOG sin tocarse desde el 08-15). El sello compara «cargado vs repo»; aquí el repo mismo va 24 días por delante de su última versión anunciada, así que las dos puntas coincidirían y el sello callaría. **Esto reabre v0.14.2 por segunda vez y por una vía nueva:** aquel fix curó «Andrés no instaló la versión» y no previene «nadie cortó versión». Agravante circular: `d8e3a7a` es justamente la cura del hallazgo del 09-08 («el sistema se contaba con dos piezas que sus reglas no cargaban») y se escribió sin cargarla. **Enmienda propuesta:** el sello imprime las tres puntas —versión CARGADA, versión declarada en `plugin.json` del repo, y si `git log` muestra commits a `rules/` posteriores al último bump— y habla si cualquiera de las dos comparaciones difiere. Corroboración independiente esta ventana: E3 del `retro-2026-09-11.md`, 4 instancias de «el control existe y su vía de carga no está conectada» (gate en pre-push sobre fichero de cada sesión, 29 commits en rojo; `core.hooksPath` sin configurar, 3 semanas mudo; decisión en vigor en cliente y no en el endpoint desplegado). **Por el criterio de la propia capa 3, re-listar es el mecanismo que ya falló tres veces: o se aprueba o se da de baja por escrito.** Detalle en `retro-2026-09-11.md` §E1) |
| 2026-08-21 | **Dos modos declarados en `rules/report-contract.md`**: modo ENTREGA (el trabajo está hecho) → tres secciones, «qué ha sucedido · qué necesitas de mí · cuáles son los siguientes pasos»; modo DECISIÓN (hace falta su criterio) → las cuatro actuales, con el mecanismo ANTES del menú. Regla de precedencia explícita entre el contrato y `registro.md` | el contrato tiene UN modo y se aplica a dos géneros. 7 peticiones de bajar el registro del 14 al 18-08 (friction 218, 222×2, 243×2, 245×2, 261, 263×2). Las cinco curas anteriores fueron todas del eje de la MEDIDA (más breve, ≤3 huecos, tope estructural de v0.15.0); ninguna preguntó si el formato era el correcto para ese turno. **Contraste controlado dentro de una sola sesión (263, 18-08): con 4 secciones, 2 quejas en 6 turnos; con las 3 que dictó Andrés, 0 quejas en 10 turnos y 7 entregas.** Mecanismo de por qué el actual se alarga por construcción (245): conflicto entre dos reglas siempre activas donde nadie declara cuál gana, y gana la que es checklist. Mitad que falta (261): con problema nuevo, el orden del contrato invierte el orden de la comprensión. **La evidencia es independiente de la versión**, así que la propuesta no depende de si v0.15.0 llegó a instalarse. Recomendación: aprobar | **pendiente** (re-listada 2026-08-28. Señal nueva, débil y a favor: **0 «for dummies» explícitos** en la ventana 22–28 contra 7 en la anterior; no atribuible al tope de v0.15.0 porque no está cargado, pero sí compatible con que el eje del FORMATO —no el de la medida— sea el correcto. Se mantiene 1 síntoma del mismo género: dos turnos gastados pidiendo alejar el foco, línea 316. **Re-listada 2026-09-04, 14 días sin decisión. La señal favorable se revierte:** vuelve el doble «for dummies» en el mismo hilo (09-02), y aparece un eje NUEVO que esta fila no lleva — el reporte se ordena por MI incertidumbre y no por el estado de las cosas, así que el escenario que NO fallaba pareció el fallo justo antes de una decisión de merge (09-03). Sugerencia al decidir: el modo ENTREGA fija las secciones, pero falta fijar el ORDEN dentro de ellas — «primero qué está roto y qué no; mi hueco de verificación va después, no delante»). **Re-listada 2026-09-11, 21 días. Sin señal nueva del eje del FORMATO en esta ventana** — cero «for dummies» explícitos en las 20 entradas del 09-05 al 09-11. Se mantiene tal cual: la evidencia del 14–18-08 sigue en pie y la ausencia en una ventana no la invalida, pero no gana prioridad frente a la 39 |
| 2026-08-21 | **Precondición de viabilidad antes de ofrecer un menú** (en `report-contract.md`, acotada a los menús que piden decisión a Andrés): antes de escribir un menú, la pregunta obligada es «¿qué dato falta para que solo quede una opción?» y se va a buscar; una rama no verificada se ofrece marcada como tal o no se ofrece; si una rama escribe/publica algo, la pregunta pide el nombre de la rama y no admite «ok» | ofrecer opciones se usa como sustituto de terminar el diagnóstico, y cada menú gasta una decisión de Andrés. 4 instancias en 3 días: A/B sobre una diferencia que el código decía inexistente (241, 17-08); menú de 3 donde la elegida no existía, muerta en 4 consultas y dos rondas de decisión (257, 18-08); gate de 3 checks aprobado del que la medición tumbó uno y reescribió otro — «una aprobación se gastó sobre información que yo aún no tenía» (277, 18-08); menú A/B/C cuando se pedía una secuencia, con «es extraño tener que corregirte con esto» (291, 19-08). Causa nombrada por el log: «un menú de opciones equivalentes es señal de diagnóstico incompleto, no de neutralidad útil». **Acotada a propósito** para no romper el design gate de v0.10.0, que sigue siendo bueno donde aplica: la enfermedad es su sobre-aplicación. Recomendación: aprobar acotada | **pendiente** (re-listada 2026-08-28, 7 días sin decisión. **Re-listada 2026-09-04, 14 días. Sin señal nueva en esta ventana** — ningún menú prematuro registrado en las 26 entradas del 08-30 al 09-04. No se retira: la evidencia del 17–19-08 sigue en pie y su ausencia en una sola ventana no la invalida, pero baja de prioridad frente a las tres de arriba. **Re-listada 2026-09-11, 21 días. Segunda ventana consecutiva sin instancias nuevas** — ningún menú prematuro en las 20 entradas del 09-05 al 09-11. Es la candidata más clara a **darse de baja** si hay que aligerar el buzón: dos semanas sin señal y su evidencia toda de agosto) |
| 2026-08-28 | **Contrapresión entre la retro y la cola de builds** — DECIDIR ESTA PRIMERA, gobierna a las otras dos. Dos cortes, el segundo complementa al primero y no lo sustituye: **(1) la sesión de build se agenda como se agenda la retro** (scheduled task semanal lado-CC en el Mac cuyo único trabajo es vaciar filas aprobadas-sin-construir, revalidándolas contra el árbol antes de tocarlas, según ya manda v0.15.0); **(2) cláusula de contrapresión en `rules/retro.md`**: mientras el buzón tenga filas aprobadas y sin construir de hace >2 semanas, la retro no abre propuestas nuevas — su entrega es la cola ordenada y una sola pregunta, «¿se vacía o se dan de baja?». **Corolario aparte y barato:** la retro commitea lo que escribe (hoy `retro-2026-08-21.md` está untracked y `pending-decisions.md` modificado sin commitear — el buzón anti-olvido vive en el árbol de trabajo de una sola máquina) | el sistema produce proceso más rápido de lo que lo instala, y **la asimetría es que el productor está automatizado y el consumidor no**: la retro corre sola cada semana, la sesión de build la tiene que acordarse alguien. El tope de 3 propuestas limita la PRODUCCIÓN, no la COLA, así que la cola crece aunque cada retro se porte bien. **Medido hoy:** 5 builds aprobados el 08-14, **0 aterrizados en 14 días** (`git log --since=2026-08-14` sobre el repo = 17 commits, 16 de ellos entradas de friction-log); 3 propuestas del 08-21, **0 decididas en 7 días**; Cowork sigue en **v0.5.0 contra v0.15.0**, segunda medición semanal con el mismo valor. **Reabre el criterio de la retro anterior**, que vio los mismos números y decidió no subirlo («re-listar ya es el mecanismo previsto») — re-listar produjo cero movimiento, y por el criterio de la propia capa 3 un mecanismo que no movió su aguja se reabre. Detalle en `retro-2026-08-28.md` §Enfermedad C. Recomendación: aprobar (1); (2) solo si (1) no basta | **pendiente** — **re-listada 2026-09-04 con la 2ª semana de datos, y la fila se valida a sí misma.** Medido hoy sobre el árbol: `git log --since=2026-08-14` = **26 commits, 25 de ellos entradas de friction-log y 1 solo tocando `plugins/core`** (`056cd29`, y no venía del buzón) ⇒ **25 observaciones por cada cambio de proceso aterrizado**. De los 5 builds del 08-14, los 3 verificables desde aquí comprobados uno a uno y **ninguno construido en 21 días**: `vault-freshness.sh` sigue emitiendo texto cuando no encuentra vault; `rules-have-loaders.sh` sin clasificación enforzable/solo-criterio; ningún rastro de gate `node -v`/`.nvmrc` en el plugin. Los 2 lado-CC no se pueden medir desde el sandbox. Además 3 filas del 08-01 siguen diciendo «falta commit (Mac/CC)» ⇒ **34 días**. Y **0 decisiones** en toda la ventana. **La condición de disparo que la propia fila define —filas aprobadas sin construir de hace >2 semanas— se cumple hoy**, así que la retro del 09-04 se ha portado como si (2) estuviera aprobada: una sola propuesta nueva. **El corolario barato YA ATERRIZÓ** (único cambio de proceso cerrado en la ventana): `retro-2026-08-21.md`, `retro-2026-08-28.md` y `pending-decisions.md` están todos *tracked* y el árbol está limpio. **Re-listada 2026-09-11: 3ª semana consecutiva con la condición de disparo cumplida, 14 días sin decisión.** Medido hoy: `git log --since=2026-08-14` = **43 commits, 41 entradas de friction-log y 4 tocando `plugins/core`** (~10 observaciones por cambio de proceso). Los 5 builds del 08-14 siguen sin construir (**28 días**); las 3 filas del 08-01 siguen diciendo «falta commit» (**41 días**); **0 decisiones** en la ventana. **Señal nueva que la fila no preveía y que empeora su diagnóstico:** de los 4 commits a `plugins/core`, tres editaron `rules/` **sin subir versión ni escribir CHANGELOG**. O sea que la cola no solo no se vacía por el canal previsto — ha empezado a **desbordarse por fuera de él**, produciendo reglas que no son ni «aprobadas sin construir» ni «shipeadas», sino un tercer estado sin nombre. El corolario barato sigue aguantando (árbol limpio, todo *tracked*). **La pregunta de la fila, sin rodeos: ¿se vacía o se da de baja?** |
| 2026-08-28 | **Calibración antes de la lectura** — sustituto concreto en `rules/verify-source.md`, no prosa nueva: antes de usar como prueba una AUSENCIA, un NÚMERO medido o un verde/rojo, correr el mismo instrumento contra un caso de respuesta conocida. Tres formas canónicas, cada una con su tecla: **(a) ausencia** → busca primero algo que SÍ está, y solo entonces lee el vacío como evidencia; **(b) número que acusa a tu propio cambio** → mídelo contra el sujeto sin tocar, en el mismo instante; **(c) test nuevo** → rojo con el defecto puesto **y verde con el arreglo puesto**, no solo lo primero. Más una línea en `report-contract.md` que extiende «superficie nombrada» (v0.14.0): una afirmación load-bearing que venga de un instrumento nombra **contra qué se calibró**, no solo qué superficie consultó | `verify-source` es un control POSTERIOR («si lo medido y lo observado se contradicen, duda del instrumento primero», punto 5 de v0.14.0) y **en la mayoría de estos fallos no hay contradicción que notar: el número sale limpio**. 8 instancias en 7 días (líneas 302, 310, 314, 321, 323, 325, 327, 329 del friction-log): tres tests verdes con el defecto puesto; consola devolviendo errores de una sesión Vite muerta; `git status` mintiendo por `core.worktree` ajeno; guarda propia rechazando dos veces una migración correcta por medir `SELECT f()` contra un `SELECT * FROM f()` (60.284 vs 16.135 sobre la función SIN TOCAR); hook de identidad acusando a los commits correctos porque su referencia faltaba; una medida del vault leída como mecanismo y reenviada dentro de un chip a otra sesión; 3-de-3 AUSENTE grepeando el artefacto equivocado; un test más ancho que su sujeto acusando al código bueno. En 314 la regla se cita literalmente en la propia entrada y no la evita. **La cura ya está descubierta tres veces con tres nombres distintos por el propio log** —«control positivo» (327, que además avisa: *«lleva tres instancias y sigue siendo prosa»* — grep de secretos 11-08, vista de permisos 18-08, esta), «correrla contra el sujeto sin tocar» (321), «verlo verde con el arreglo» (329)— y funcionó las tres veces que se aplicó. Por la escalera de `patterns.md` (v0.15.0), 8 violaciones estando la regla presente ⇒ sube de vía. Detalle en `retro-2026-08-28.md` §Enfermedad A. Recomendación: aprobar | **pendiente** — **re-listada 2026-09-04 CON ENMIENDA. 10 instancias nuevas en 6 días ⇒ 18 en 14 días**, con `verify-source` cargada inline en el `CLAUDE.md` del repo donde ocurrieron (dos entradas la citan textualmente y no la evitan). Las tres formas canónicas de la fila siguen bien y **no cubren tres ejes que estas 10 exponen**, todos aguas ARRIBA de la calibración, en la ELECCIÓN de la sonda: **(d) la elección se justifica contra la pregunta, no por disponibilidad ni por nombre** — antes de leer el número, «¿qué otra cosa podría producir este mismo resultado?»: `find` sin `-L` leído como «no está clonado» y 431 MB de clon inútil (09-03); grep del bundle por el ASUNTO DE UN COMMIT, cadena que no está ni ha estado en `src/`, afirmado en dos reportes (09-03); criterio de éxito rellenado con el único hash a mano, o sea circular (09-03); histograma de `updated_at` ciego a 5 de los 6 escritores, que acusó al inocente (09-02); pregunta sobre un TERCERO contestada consultando NUESTRO inventario de conectores (09-04); `.limit(10000)` leído en el código y dado por respetado cuando PostgREST corta en 1.000, con dos hipótesis externas montadas antes de dudar de la propia consulta (09-04). **(e) quién toma la medida es parte del instrumento** — el número sale perfecto y describe un camino que nadie recorre: los 3 instrumentos del arco del 500 comparten la ceguera a RLS (sonda `list_read_cost` como `service_role`, MCP de Supabase que se la salta, 10 funciones `SECURITY DEFINER`), 20.008 bloques y 458 ms medidos contra **158.141 y 8,16 s** reales; 5 días de diagnóstico sobre el camino que no fallaba y una spec de 800 líneas describiendo una pantalla que ningún dueño ve (09-04). **(f) la simetría** — una sonda que CONFIRMA recibe la misma segunda pasada que una que contradice: la regla se aplicó en cuanto la sonda incomodó y no cuando dio la razón (grep con el nombre de función equivocado, cero que coincidía con la ficha, constante viva y en pantalla — 09-04); y verificar que un cambio es SEGURO no es verificar que sea SUFICIENTE (migración 193 aplicada a producción por un aviso de linter cierto pero que explicaba 24.226 de 158.141 bloques — 09-04). Si se prefiere no ampliar el alcance: aprobar la fila tal cual y abrir (d)(e)(f) como fila aparte — pero no dejarla otra semana sin decidir. **Re-listada 2026-09-11 CON DISCRIMINANTE, 14 días. 16 instancias nuevas en 5 días** (friction-log 411, 415, 419, 425, 437, 441, 447, 449, 451, 453, 455, 466, 468, 470, 472, 491), con `verify-source` cargada inline en el `CLAUDE.md` del repo donde ocurrieron y dos entradas citándola textualmente sin evitarla. **Las tres formas canónicas (a)(b)(c) y las tres enmiendas (d)(e)(f) rodean estas 16 sin nombrarlas; el log sí las nombra, y cuatro veces con las mismas palabras:** «seguir la cadena hasta quien CONSUME: no quien lo declara, no quien lo calcula, quien lo pinta» (447), «el consumidor antes que el productor» (415), «enumerar las PANTALLAS que esa ruta sirve, no la ruta» (455), «al dar un recuento, decir sobre qué población se contó» (437). **Propuesta de enmienda, una sola línea que sustituye a seis:** antes de afirmar un comportamiento, identificar QUIÉN CONSUME el dato en la pantalla o el camino exactos de la pregunta, y verificar ahí — el declarante, el que calcula y el preset no son el sujeto. Casos que cubre y (d)(e)(f) no: preset `hold:{direction:'up'}` verificado y `isRankablePurpose` filtrándolo (441); tres RPC + adaptador + cliente verificados y `/try` sin manejadores de ratón (415); «16 ramas» que eran 142 (437); `/try` leído como una ruta siendo dos pantallas (455). **Coste ya pagado: tres afirmaciones encadenadas y la tercera publicada en el changelog de la v0.58.1** (447). Detalle en `retro-2026-09-11.md` §E2 |
| 2026-08-28 | **Contrato de plan** — sección corta en `rules/report-contract.md` (o `rules/plan-contract.md` si se prefiere separado) con tres preguntas mecánicas, cada una con su prueba de una línea ya redactada en el friction-log: **(1)** ¿hay algo en este plan que, si sale mal, invalida el resto? → es Fase 0, y va **antes** de la numeración, no dentro; **(2)** ¿el punto 2 deja de tener sentido si no se hace el 1? → si no, son fichas y **van sin numerar**; **(3)** ¿he abierto los ficheros que este paso nombra? → si no, el alcance se declara **estimado**, nunca prometido | el plan es el único entregable del sistema **sin contrato**: los reportes tienen uno, los tickets tienen skill, las capturas tienen esquema, los commits tienen gates — y el plan, que es el artefacto que más decisiones de Andrés gasta aguas abajo, no pasa por ninguna puerta. La `Phase 0 — reconnaissance with an explicit stop point` existe en el `CLAUDE.md` de mintstash **solo para delegar a un subagente**; cuando el que planifica es el modelo, no aplica. 3 instancias en 4 días, tres caras del mismo defecto: «solo hooks, sin migración, sin riesgo» sobre un fichero no abierto que necesitaba SQL (306, 23-08); dos problemas distintos numerados como pasos 1 y 2, con un bug vivo de producción disfrazado de mejora opcional — «¿esto es una opción o un paso 2?» (312, 23-08); spec de trece pasos con propuesta de empezar por el 3 en la misma respuesta — «¿cómo es que haces un plan, enumerado, y no quieres empezar por el paso 1?» (319, 26-08). **El log marca la reincidencia él solo** (319): *«la pregunta se hizo, nadie la cargó, y tres días después el mismo fallo con la misma forma»*. Detalle en `retro-2026-08-28.md` §Enfermedad B. Recomendación: aprobar | **pendiente** (re-listada 2026-09-04, 7 días. **Sin instancias nuevas de plan mal formado en esta ventana**, pero señal indirecta a favor: la spec de 800 líneas del arco del 500 describía una pantalla que ningún dueño ve — un plan cuyo alcance se prometió sobre ficheros que sí se abrieron pero medidos por el instrumento equivocado. Es la prueba (3) de la fila con un sujeto nuevo: «he abierto los ficheros» no basta si el número que los describe viene de un camino distinto al del usuario. Se mantiene tal cual; sin enmienda. **Re-listada 2026-09-11, 14 días. Una instancia nueva de la prueba (3)**, «¿he abierto los ficheros que este paso nombra?», con sujeto de despliegue: el 09-11 se desplegó código que pedía una columna antes de que su migración estuviera aplicada, y se avisó del riesgo equivocado — se dijo que rompía el `upsert` y rompió el `select`, o sea que la app dejó de LEER con Andrés delante (friction 483). La forma general que añade: **al cruzar dos caminos de publicación, la pregunta del plan es qué lado rompe PRIMERO si el otro no está.** Candidata a cuarta pregunta mecánica de la fila) |
| 2026-09-04 | **Precondición de arranque: proyecto declarado y árbol exclusivo** — sustituto concreto en `rules/session-open.md`, dos preguntas de respuesta mecánica ANTES del primer paso del ritual, no prosa nueva. **(a) ¿Qué proyecto?** Si el directorio de arranque contiene más de un `active-context.md` en su árbol, el ritual **no adivina**: lista los candidatos, declara cuál ha rozado y por qué, y deja que Andrés lo fije con su primera petición. **(b) ¿Estoy solo en este árbol?** En todo repo al que la sesión vaya a ESCRIBIR —y el vault cuenta, porque el ritual de cierre escribe ahí—: `git status` y `git log origin/main..main --name-only` al **abrir**, y declarar en el primer mensaje si hay trabajo ajeno. **(c) Corolario de una línea:** la decisión de aislamiento enumera **todos** los repos que la tarea toca, no solo el que la tarea nombra | **E3: el arranque da por sabido su propio sujeto.** `route-task` enruta por tipo de output y la apertura lee el vault DEL PROYECTO; entre las dos falta elegir proyecto, dato que ninguna pide y las dos necesitan. Con un proyecto por directorio no se ve nunca; con un directorio paraguas se ve siempre. **5 instancias en 3 días, y cobertura CERO en el buzón** (la fila del 08-07 resuelve proyecto→ESQUEMA de vault, o sea qué fichero hace de `decisions.md`; no resuelve qué proyecto es): sesión abierta en `~/Documents` con tres vaults y ninguna tarea declarada, resuelta a mano buscando `active-context.md` y preguntando (09-04); **dos agentes escribiendo en el mismo `mintstash-knowledge`** porque el worktree separa los checkouts de código y el vault es un repo aparte y uno solo — `roadmap.md` y una spec aparecieron modificados sin commitear y no eran míos (09-04); **sesión paralela descubierta al hacer `git push`** (`ahead 2`), commit ajeno subido con los míos, revisado por suerte de haber mirado `ahead` y no por proceso — **y repetido media hora después** (09-04); entorno de pruebas montado en un temporal de sesión cuando el consumidor del entregable era una corrida posterior, con el brief apuntando a una ruta que muere con la sesión (09-03); `git push` de `main` desde un worktree que reinicializó el `.git/config` COMPARTIDO y dejó los dos checkouts inservibles, con la protección documentada puesta pero escrita solo para el caso de TypeScript (08-30, dos entradas). Coste acumulado real: minutos. **El modo de fallo que el log nombra es un `git add -A` que se lleve trabajo a medias de otra sesión**, y esa no se recupera mirando `ahead`. Las dos preguntas son mecánicas y baratas: es exactamente lo que funcionó las tres veces, elevado a paso del ritual en vez de a suerte. Detalle en `retro-2026-09-04.md` §P2. Recomendación: aprobar | **pendiente** (re-listada 2026-09-11, 7 días. **Una instancia nueva del eje (c)**, «la decisión de aislamiento enumera TODOS los repos que la tarea toca»: el 09-11 `EnterWorktree` falló con «not in a git repository» porque el cwd de sesión es `~/Documents`, que no es ningún repo — el vault y el código cuelgan de ahí, no son ahí (friction 476). Refuerza (a) desde el lado de la herramienta: **una herramienta que opera sobre «el repo» resuelve por cwd, y en esta máquina el cwd de sesión casi nunca es un repo.** Sin enmienda; la fila ya lo cubre) |
| 2026-09-11 | **La regla fresca no se carga** — sustituto concreto en `rules/session-close.md` y `rules/patterns.md`, dos gestos mecánicos, no prosa nueva. **(a) Regla escrita = regla releída:** cuando en la misma sesión se escribe un normativo Y el artefacto que lo obedece, el artefacto se relee contra el normativo **como paso aparte y explícito**, no se da por cumplido porque lo escribió el mismo que la regla. **(b) Un normativo recién editado entra en la ruta de lectura del turno siguiente**, no del proyecto siguiente: al cerrar la edición de un fichero de reglas, sus cláusulas nuevas se citan literalmente en el mensaje que las estrena. **(c) Corolario de release, que es la misma regla un tramo arriba:** una edición a `plugins/core/rules/` no se commitea sin subir `plugin.json` y escribir CHANGELOG — si no hay versión, no hay nada que instalar | **E1, escala del turno: la frescura se confunde con vigencia, y son opuestas** — una regla recién escrita es justamente la que menos probabilidad tiene de estar en la ruta de lectura. **4 instancias en 4 días y cobertura CERO en el buzón** (la fila 39 cubre «escrito ≠ instalado» a escala de release; la 43 cubre el instrumento; ninguna cubre el turno): el 09-08 el sistema se contaba con dos piezas —tope de 3, clear al cerrar— que sus propias reglas no decían, y **el detector fue explicárselo a un tercero**, que no es un control programable (427); el 09-09, tres párrafos después de anotar «el concierge promete atención, no roadmap», salió un borrador con «dímelo y lo construyo yo», corregido por Andrés al enviar (457); el 09-10 **dos veces la misma noche**, con dos reglas distintas que el propio agente había redactado el día antes, las dos levantadas por Andrés (464); el 09-11, una regla del repo existente en la otra dirección no se aplicó al cruce análogo y la app dejó de leer en producción con Andrés delante (483). El log nombra la causa mejor que yo: *«la doctrina vive en un fichero que no reabro al redactar»* y *«yo era el hook y no me disparé»*. **La cura ya se descubrió y funcionó una vez** (464: lista de comprobación de seis líneas al principio del punto 6, para leer ANTES de cada borrador) — o sea que (a) y (b) no son teoría. **(c) tiene evidencia dura propia:** tres ediciones a `rules/` desde v0.15.0 sin bump ni CHANGELOG (`056cd29`, `d8e3a7a`, `b437436`), y el agravante circular de que `d8e3a7a` es la cura del hallazgo 427 escrita sin cargarla. Por la escalera de `patterns.md`, 4 violaciones ⇒ sustituto concreto, no prosa. Detalle en `retro-2026-09-11.md` §E1. Recomendación: aprobar — es el peldaño de abajo de la misma escalera que la fila 39, y el más barato de los tres | **pendiente** |
