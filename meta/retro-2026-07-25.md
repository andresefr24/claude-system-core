# Retro 2026-07-25 — tranquila (puesta al día + línea base de auditoría)

**TL;DR:** La corrida automática de esta retro arrancó de un snapshot pre-ráfaga y proponía cerrar el loop retro→decisión, capture incremental y design gate — todo eso Andrés ya lo shipeó hoy (v0.7.0→v0.10.0). El backlog de 4 retros quedó vaciado: `pending-decisions.md` no tiene filas abiertas. No hay enfermedades nuevas accionables. El trabajo de esta retro cambió de *proponer* a *dejar la línea base para auditar* la ráfaga la semana que viene.

## Lo decidido hoy (contexto)

Todo con estado registrado en `pending-decisions.md`, decidido 2026-07-25:
- v0.7.0 — hook `vault-freshness` (enforcement determinista de observabilidad del vault) + regla de comms.
- v0.8.0 — loop retro→decisión (`pending-decisions.md` + wiring) + registro llano global.
- v0.9.0 — verificación de UI en navegador (viewport real declarado) + capture incremental de fricción + friction-log único.
- v0.10.0 — design gate en `routing.md`.
- Remote mintstash: aprobado, raíz fuera del plugin (Andrés autentica el remote).
- "UI local → deploy a preview": rechazado, premisa falsa (Claude-in-Chrome sí entra a localhost).

## Auto-auditoría — DIFERIDA, con línea base montada

Los 7+ fixes shipearon el mismo día (2026-07-25), así que la capa-3 ("¿movió la aguja?") no se puede medir aún — es demasiado fresco. Esto define, por fix, la métrica objetivo y cómo medirla en la retro **2026-08-01**:

| Fix (versión) | Enfermedad | Métrica objetivo | Cómo se mide |
|---|---|---|---|
| Hook vault-freshness (v0.7.0) | vault canónico ≠ el consumido (`miss-vault ×4`) | 0 arranques con vault desfasado / identidad equivocada | ¿nueva entrada `miss-vault` en friction-log? + ¿el hook corrió? (Cowork no garantiza SessionStart) |
| Regla de comms (v0.7.0) | audiencia/registro/canal no eran inputs de primera clase | 0 correcciones de registro/audiencia en mensajes a humanos | entradas `reexplicacion` sobre comms |
| Loop retro→decisión (v0.8.0) | propuestas morían en limbo | pending-decisions sin filas "pendiente" >1 semana **y decidido en cadencia, no en ráfaga** | inspección de pending-decisions + ¿avisó session-open de pendientes viejos? |
| Registro global (v0.8.0) | "for dummies" fuera de deliverables bajo contrato | 0 "for dummies" en digestiones/explicaciones ad-hoc | entradas `reexplicacion` |
| Verificación de UI (v0.9.0) | viewport asumido (cap ×2) | 0 workarounds propuestos sin declarar viewport real alcanzado | sesiones con browser/UI |
| Capture incremental (v0.9.0) | fricción acoplada al cierre ordenado se perdía | 0 sesiones con fricción sin loggear (el log crece sin que nadie lo pida) | ¿el próximo smoke/review/pivot dejó su línea sola? |
| Design gate (v0.10.0) | salto a la primera solución en decisiones que fijan arquitectura | 0 rework por saltar el diseño; se expusieron 2-3 opciones antes de codificar | sesiones de arquitectura (tabla/vista/flag/contrato nuevos) |
| Remote mintstash (decidido) | prod ≠ local / push manual | 0 recurrencias de drift una vez autenticado el remote | entradas `tooling`/`drift` de mintstash |

## Watch (no accionable hoy — no cruza umbral)

- **Ráfaga vs. cadencia.** El fix del loop (v0.8.0) se validó a sí mismo: su propio buzón es lo que permitió la puesta al día. Pero las decisiones cayeron en *ráfaga* (todo un día tras semanas de limbo), no semanalmente. v0.8.0 añadió el aviso en session-open para pendientes >1 semana; si las decisiones siguen acumulándose hasta un catch-up manual, ese aviso no está aterrizando. Vigilar en 2026-08-01.
- **git desde el sandbox de Cowork** (friction-log 2026-07-25, 1ª vez): deja lockfiles huérfanos en `.git` + riesgo de commit con identidad equivocada. Auto-flagueado "regla solo si recurre". No cruza regla de tres.

## skill-candidates

Ninguna cruzó umbral. Hook de enforcement GENERAL sigue aparcado (sin señal de dilución en sesión fresca; el contrato aguantó con 0 fallos). Ops-de-volumen sigue en 1 ocurrencia. Regla de comms ya materializada (v0.7.0).

## Barrido del vault (ejecutado)

- `handoff-2026-06-24.md` **borrado**: se auto-marcaba "bórralo cuando sus hilos estén cerrados"; el sistema completo ya está construido, hilos cerrados.
- Par 06-24 del friction-log (síntoma + su resolución same-day) **compactado** a una entrada, preservando la lección (hipótesis de enforcement de prosa descartada por test limpio).
- Resto del vault: durable y load-bearing.

## Confianza y huecos

Alta — verificado contra `git log` (HEAD v0.10.0), `pending-decisions.md` y friction-log actuales. Hueco real: la capa-3 queda genuinamente diferida a 2026-08-01 (fixes de hoy). Nota de proceso: la corrida automática arrancó de un snapshot pre-ráfaga y su borrador salió obsoleto; Andrés intervino en vivo para replantear. Escrito con herramientas de archivo, **sin git desde el sandbox** (honra la fricción loggeada hoy) — el commit queda para el Mac.
