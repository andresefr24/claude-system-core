# Friction log

Una línea por evento de fricción del PROCESO (no de los proyectos). Lo escribe Claude al
cerrar sesión, como parte del capture. La retro semanal lo lee para buscar enfermedades,
no solo síntomas.

Formato: `FECHA | tipo | qué pasó | rama/contexto`
Tipos: retry · frio (arrancó sin contexto) · reexplicacion · override (routing) · miss-vault

<!-- entradas debajo -->

2026-06-24 | contrato/reexplicacion + RESOLUCIÓN (compactado en retro 2026-07-25) | en sesión larga (cohort MintStash, browser+supabase) los deliverables sustantivos no aplicaron el contrato pese a instrucciones de proyecto. Hipótesis "enforcement de prosa insuficiente" → test limpio en sesión fresca la DESCARTÓ (contrato/retrieve/capture dispararon completos). Causa real probable: dilución por contexto largo/tool-heavy o sesión heredada. Acción tomada: NO construir mecanismo de enforcement general (sigue aparcado en skill-candidates); vigilar si la dilución reaparece en sesiones frescas que crezcan largas.

2026-06-24 | miss-vault | en el test, el snapshot de project-knowledge de Claude.ai ("TCG Vault") estaba desfasado (no contenía people.md); hubo que conectar el vault real a mano. Acercándose al trigger rule-of-three de automatizar el sync.

2026-06-24 | reexplicacion | RECURRENTE entre proyectos y conversaciones: Andrés pide "dumb downs" / "for dummies" de forma constante. | enfermedad: el contrato de reporte fijaba la estructura pero no el registro, así que el default salía demasiado denso. | RESUELTO en v0.6.1: cláusula de registro (llano por defecto, deep dives a petición).

2026-07-25 | miss-vault | RETRO semana 19–26: enfermedad dominante = el vault canónico (disco) no es lo que se consume — snapshot project-knowledge desfasado sin people.md, Gmail apuntando a landbot.io en vez de tcgvault, trabajo de memoria. miss-vault ×4, cruzó la regla de tres del trigger diferido de CONVENTIONS. | RESUELTO en v0.7.0: hook determinista SessionStart (vault-freshness) que inyecta verdad de tierra + guards de conector/snapshot. Métrica objetivo: miss-vault debe caer a ~0 en sesiones frescas. Auditar en la próxima retro.

2026-07-25 | reexplicacion | RETRO: registro de comms (venezolano al equipo / llano hacia fuera) corregido a mano en 3 sesiones (Security app, Cohort, Influencer). | RESUELTO en v0.7.0: rules/comms.md hace audiencia+registro+canal inputs obligatorios; enforcement = persistir correcciones en working-preferences.md.

2026-07-25 | auto-auditoría | contrato de reporte (v0.1.0) y registro llano (v0.6.1) NO regresaron: 0 fallos de contrato en la semana. Hook de enforcement GENERAL sigue sin señal fresca → se mantiene aparcado. Caveat abierto: los hooks SessionStart son de Claude Code; su disparo en Cowork no está garantizado — validar en la próxima sesión fresca real.

2026-06-27 | tooling/retry | smoke test de UI nueva en localhost bloqueado por la política de Claude-in-Chrome (veta localhost Y IPs LAN privadas); varios turnos/intentos (navigate, read, screenshot, IP de LAN 192.168.x) antes de resolver vía deploy a dominio real (Vercel/prod). | enfermedad candidata: no hay forma soportada de smoke-testear UI local con Claude-in-Chrome desde Claude Code (feature request cerrado "not planned"); el flujo de "verificar en el navegador" debería asumir desde el inicio "para UI local, desplegar a preview, no intentar localhost". confound: en Cowork funcionaba por computer-use, de ahí la expectativa de Andrés. | rama: mintstash feat/column-sort-filter.

2026-06-27 | retry | un script nuevo de Scryfall (backfill-artist) salió SIN el backoff de 429 de TD-77 y se topó con 429 masivos → hubo que añadir retry+Retry-After en una segunda pasada. | enfermedad: cada nuevo fetch a Scryfall reinventa u olvida el manejo de rate-limit; falta un helper/checklist compartido ("todo fetch a Scryfall nace con backoff"). capturado como deuda en mintstash [[tech-debt#TD-88]].

2026-07-08 | tooling | RECURRENCIA del item 2026-06-27: Claude-in-Chrome volvió a vetar localhost (y esta vez también instagram/discord para leer DMs); el smoke de UI local no se pudo automatizar y se resolvió con screenshots que pega Andrés + verificación de datos por Supabase MCP. | enfermedad confirmada (2ª aparición): el flujo "verificar en navegador" no asume aún "UI local = deploy a preview o pide screenshot, nunca intentes localhost". Falta codificar esa bifurcación en la skill de smoke/run. | rama: mintstash feat/change-window.

2026-07-08 | retry | un subagente de build (B2.1 windowed-change) murió a mitad por "API Error: Overloaded" (transitorio) dejando el árbol a medio editar (HoldingRow pedía una prop que nadie pasaba, build en rojo); se recuperó relanzando un agente fresco con spec precisa. | enfermedad candidata: los subagentes de código no dejan checkpoint atómico — un fallo transitorio deja el working tree inconsistente; convendría que tareas de código en subagente commiteen a una rama temporal o reporten "incompleto" de forma explícita para no confundir crash con fin de tarea.

2026-07-08 | retry | un comando Bash corrió build+tests DESDE ~/Documents (el cwd del shell se había reseteado) → "npm enoent" + 1051 tests en falso rojo; se resolvió re-ejecutando con `cd` explícito al repo. | enfermedad: el shell resetea cwd entre llamadas y los comandos compuestos que no hacen `cd` corren desde el dir equivocado sin avisar; regla: todo comando de repo empieza con `cd <repo> &&`.

2026-07-08 | tooling | RECURRENTE: el remote git (andresefr24/mintstash) es inalcanzable desde la CLI (repo de otra cuenta) → tag/push de release y commits van por GitKraken a mano. Ya visto en el arco de release; sigue sin resolverse. | enfermedad: la ceremonia de release por CLI (skill `release`) no puede completar push/tag en este setup; o se autentica la cuenta dueña en la terminal o la skill asume "push manual por GitKraken".

2026-07-16 | diseño/rework | la feature "listas por-oracle + precio agnóstico" (mintstash) terminó en un diseño limpio —granularidad = flag de lista (`list_view` lo interpreta, cero mutación) + job semanal dueño de precio/representantes/cache, conversión reversible— que era la intención de Andrés DESDE EL PRINCIPIO. Pero se llegó tras varios turnos de rework: primero construí la conversión mutando 3992 filas (`printing_id`→null) + backfill en el navegador (~1h, una-sola-vía); choqué con esos costes (lentitud, no-reversible, errores concurrentes); y solo aterricé el diseño-flag cuando Andrés preguntó "¿no bastaría un flag en la lista?" y "¿no podría el job semanal guardar también los precios/representantes?". | enfermedad: ante un problema que MOLDEA arquitectura (modelo de datos / read path nuevos), salté a implementar la primera solución que funcionaba en vez de exponer primero el espacio de diseño / el modelo más simple y pedir la intuición de Andrés; el coste fue N turnos de construir-y-rehacer sobre una superficie compartida (list_view). | fix propuesto: para decisiones que fijan estructura (tabla/vista/flag nuevos), presentar 2-3 opciones con tradeoffs (o "el modelo más simple posible") ANTES de codificar e invitar explícitamente la intención arquitectónica del usuario al inicio — no tras el primer prototipo. Candidato a cláusula en route-task / a un paso de "design gate" en tareas de arquitectura. | rama: mintstash oracle-price → granularity-flag.

2026-07-10 | retry×2 | correr `seed-artist-list.ts` (concierge 2ª tanda de artist-lists de Mei) se fumó dos veces por el setup de credenciales a mano: (1) las env vars se pasaron sin `export` en línea aparte → invisibles al proceso hijo `npx tsx`; (2) typo `g`/`q` en el ref de SUPABASE_URL → `fetch failed` genérico que enmascara "host no resuelve" (Scryfall sí funcionaba porque su URL está hardcodeada). | enfermedad: los scripts de seed reciben credenciales por terminal multi-línea, frágil y repetido en cada concierge; falta un `.env.scripts` gitignoreado (o que el script lea SERVICE_ROLE de archivo) para eliminar la clase entera. Vecino de [[tech-debt#TD-88]] (todo script de seed nace con boilerplate de infra frágil). | rama: mintstash / concierge Mei.

2026-07-16 | debug/drift | perseguí ~5 queries SQL un "upsert silencioso que no escribe" (oracle_representative + printing_cache quedaron vacíos tras correr el GH Action, aunque oracle_price sí escribió) — llegué a revisar grants y RLS. La causa real: el refactor que AÑADÍA esas escrituras estaba mergeado a `main` LOCAL pero **sin pushear**, así que el Action corrió el script VIEJO de `origin` (solo escribía precio+history). | enfermedad: recurrencia del 2026-07-08 (remoto de mintstash inalcanzable desde CLI → push manual). `main` local se adelanta a `origin` de forma habitual; como el GH Action corre el `main` de GITHUB y las migraciones se aplican DESDE el archivo local, "lo que corre en prod" ≠ "lo que veo en local" sin ningún aviso → debugging engañoso (parecía bug de código/permisos, era deploy-state). | fix/regla: al depurar "prod no cuadra con mi código", comprobar PRIMERO `git log origin/main..main` (¿el commit relevante está pusheado?) antes de asumir bug de código/datos. Candidato: recordatorio de "commits sin pushear que afectan a CI/prod" antes de disparar Actions. | rama: mintstash oracle-price/granularity.

2026-07-16 | recurrente/tooling | la skill `release` (v0.34.1) volvió a fallar el push: remote andresefr24/mintstash → "Repository not found"; quedó commit `c424ecd` + tag `v0.34.1` locales sin empujar. Misma enfermedad ya documentada (2026-07-08, 2026-07-16 oracle-price). | sin cambio: o se autentica la cuenta dueña en la terminal o la skill `release` asume "push manual" en su contrato en vez de fallar la ceremonia. | rama: mintstash release v0.34.1.

2026-07-16 | recurrente | `review.sh` frenó otra vez un `prepare-release` por comentarios `//` de línea completa en archivos de feature (ListSettingsModal, oraclePriceResolution); se limpiaron a mano a mitad de la ceremonia. Ya señalado como gotcha en el arco de TD-95. | enfermedad: el check no se corre antes de tocar release; `prepare-release` ya lo incluye como paso, pero el comentario entró en el feature work previo sin que nada lo rechazara → conviene un pre-commit hook que rechace comentarios `//` en `src/`, no descubrirlo dentro de la ceremonia. | rama: mintstash release v0.34.1.

2026-07-25 | tooling | git desde el sandbox de Cowork sobre el repo montado deja lockfiles huérfanos (.git/HEAD.lock, index.lock, objects/maintenance.lock) que el sandbox no puede unlink ("Operation not permitted"); el commit v0.10.0 entró bien pero los locks quedan y pueden bloquear git desde el Mac. Workaround: `rm .git/*.lock .git/objects/maintenance.lock` desde el Mac. Además el commit salió firmado con la identidad equivocada (landbot.io en vez del GitHub andresefr24) por falta de config local → RESUELTO en el momento: `[user]` fijado en `.git/config` del repo; pendiente amend de `a921cd2` desde el Mac. | enfermedad candidata (1ª vez): el mount de Cowork permite crear pero no borrar ciertos archivos en .git → operaciones git de escritura desde el sandbox son semi-seguras; si recurre, regla "git de escritura en repos montados → Claude Code / Mac, no sandbox". | rama: claude-system-core v0.10.0.

2026-07-25 | tooling (2ª manifestación del item anterior) | durante el barrido de la retro, `rm` de archivos normales en el mount (handoff en el repo + borrador en outputs) falló con "Operation not permitted" → resuelto con `mcp__cowork__allow_cowork_file_delete` por carpeta y reintento. Generaliza la enfermedad: el mount de Cowork es crear-pero-no-borrar desde el sandbox, no solo para `.git`. Acerca a regla-de-tres. | rama: claude-system-core retro 2026-07-25.

2026-07-16 | reexplicacion×2 | en el análisis cEDH de la colección de Daniel recomendé cartas BANEADAS en Commander (Mana Crypt, Jeweled Lotus, Dockside) y cifré precios DE MEMORIA (Grim Monolith ~10x mal) teniendo la data real de precios en la propia BD; Andrés corrigió dos veces. Además fallé una búsqueda de comandantes por confiar en `type_line` (null en printings viejos → cartas omitidas en silencio). | enfermedad: para dominios con fuente de verdad accesible (reglas de formato vigentes, precios en `price_snapshots`), tiré de memoria en vez de verificar; y filtré por un campo con nulls sin comprobar cobertura. | fix: verificar hechos de dominio y precios contra la fuente antes de afirmar; al filtrar por un campo, comprobar nulls/cobertura primero. | rama: mintstash / análisis colección Daniel.

2026-07-17 | debug/reexplicacion | RECURRENCIA del 2026-07-16: ante una lista por-impresión que salía "Carta desconocida" al girar a modo oracle, di una explicación plausible pero SIN verificar ("mi copia SQL no sembró `oracle_representative`") — que no aguantaba, porque los representantes son globales por `oracle_id` y ya existían. Solo cuando Andrés empujó con intuición de dominio ("si otras listas convierten bien, los representantes ya están; huele a referencias no-únicas / duplicados esperando un job") consulté la BD y apareció la causa real y MÁS GRAVE: `card_lists.items` (jsonb) es una copia paralela lossy de `list_items` (no lleva `oracleId`), y `saveList` reconstruye `list_items` desde ese jsonb en cada guardado → nulifica `oracle_id` en todas las filas → rompe la conversión a oracle y el job semanal no puede repararlo. | enfermedad: ante un resultado anómalo alcancé una narrativa plausible en vez de consultar los datos que tenía a mano; una diagnosis segura-pero-falsa es peor que "déjame comprobar" y consumió la corrección del usuario para desbloquearse. Misma enfermedad que 2026-07-16 (explicar de memoria/modelo en vez de verificar contra la fuente accesible) → 2ª aparición, sube a candidata de retro. | fix: toda explicación de un síntoma es una HIPÓTESIS a confirmar con una query contra la fuente (BD/logs) ANTES de afirmarla al usuario; especialmente cuando la explicación "cierra" el caso cómodamente. | producto: capturado como bug real en mintstash [[tech-debt#TD-110]] (jsonb `items` lossy vs `list_items` + `saveList` destructivo). | rama: mintstash perf/td-103 (smoke playground).

2026-07-25 | corrección | INVALIDADA la enfermedad candidata "UI local → deploy a preview, nunca localhost" (entradas 2026-06-27 y 2026-07-08): Andrés confirma que Claude-in-Chrome SÍ entra a localhost y lo ha usado varias veces recientemente (¿cambio de política o diferencia de superficie?). Las entradas quedan como histórico; la propuesta 3 de la retro 07-jul queda rechazada en pending-decisions. Lección para retros: verificar limitaciones de tooling contra el estado presente de la herramienta antes de proponer reglas sobre ellas.

2026-07-25 | tooling | primer push que tocaba `.github/workflows/` rechazado: el token de gh tenía scope `repo` pero no `workflow` ("refusing to allow an OAuth App to create or update workflow"). Resuelto en el momento por Andrés con `gh auth refresh -s workflow`; durable, no debería recurrir. | rama: mintstash feat/track-playrate-universe.

2026-07-25 | tooling | el MCP de Supabase corre en modo read-only ("cannot execute INSERT in a read-only transaction") → un saneo de 10 filas no pudo aplicarse por SQL directo. El workaround resultó mejor diseño (el saneo se convirtió en paso permanente del workflow semanal), pero la regla operativa queda: escrituras a prod → scripts/workflow con service key o SQL editor de Andrés; el MCP es solo lectura. | rama: mintstash feat/track-playrate-universe.

2026-07-25 | reexplicacion | presenté la cobertura de precios medida contra `card_playrate` de forma que sonó a "cartas de MintStash sin tracking" → Andrés tuvo que parar a preguntar por el invariante ("todas las cartas que tenemos deberían tener tracking"). El universo de referencia (tabla de mercado vs contenido de la app) no estaba nombrado en el reporte. Fix: al reportar métricas de cobertura, declarar explícitamente contra qué universo se mide. | rama: mintstash análisis brief estrategia.

2026-07-26 | tooling | agentes background colgados x2 en 24h: validation-metrics (watchdog 600s, con notificación) y UI /try (wedged ~9h EN SILENCIO, sin notificación de fallo — lo destapó Andrés preguntando "lleva 10h, ¿no es mucho?"). Detección fiable: git log del checkout (commits recientes = vivo; silencio largo = colgado). Fix: TaskStop + resume con el punto exacto donde iba. Para la retro: ¿partir encargos largos en fases con checkpoint? | rama: mintstash feat/validation-metrics + feat/trial-backend.

2026-07-26 | proceso | trabajo en worktree separado invisible para Andrés ("¿en qué rama está? no la veo en mi IDE"): la rama estaba en origin pero no en la carpeta que su IDE mira. Regla adoptada y guardada en memoria del agente: por defecto trabajar en su checkout; worktree solo con ≥2 agentes escribiendo en paralelo, avisando de su existencia, pusheando a menudo y consolidando de vuelta al cerrar la fase paralela. | rama: mintstash feat/trial-backend.

2026-07-26 | proceso | drift de migración manual: se aplicó un borrador viejo de la 076 (campo windowSource singular) en vez del fichero final (windowSource7d/30d/90d). Silencioso: la UI degradaba limpio y solo se vio sondeando la forma de la función en prod. Propuesta (pendiente de OK para el vault): tras cada "migración aplicada", el agente verifica la forma viva contra el fichero con un SELECT antes de dar el paso por bueno. | rama: mintstash feat/trial-backend.

2026-07-26 | tooling | clicks sintéticos del Browser pane no disparaban el submit del form React en desktop (en móvil sí); workaround: click programático via javascript_tool. Relacionado: form_input lanzado en paralelo con resize_window deja el valor en el DOM sin que React lo registre — secuenciar siempre input→click sobre refs frescos del layout final. | rama: mintstash smoke /try.

2026-07-26 | tooling | deploy de edge functions bloqueado al inicio: CLI sin supabase login y MCP en read-only (deploy vetado). Resuelto durable: Andrés hizo `supabase login` → el agente despliega functions por CLI; migraciones siguen manuales a propósito (línea de seguridad TD-40). | rama: mintstash feat/trial-backend.

2026-08-01 | tooling (3ª manifestación → cruza regla de tres) | construyendo la skill `anomaly-debug` en el repo montado de mintstash desde el sandbox de Cowork: el Write tool VETA `.claude/` (ubicación protegida) y bash crea-pero-no-borra (`rm` → "Operation not permitted"), así que un probe file quedó huérfano hasta pedir `allow_cowork_file_delete`. Workaround: bash heredoc para crear + permiso de borrado para limpiar; el `CLAUDE.md` de raíz sí lo editó el Edit tool (protección es específica de `.claude/`). | enfermedad (ya vista 07-25 ×2: locks de git + `rm` de archivos normales): las ESCRITURAS sobre repos reales desde el sandbox de Cowork van a contracorriente del mount (crear-pero-no-borrar; `.claude/` protegido). No es imposible —se puede con bash + permiso de borrado— pero es semi-seguro y frágil. Regla candidata: escrituras/git en repos montados → Claude Code / Mac, y el sandbox de Cowork solo para lecturas y para lo que no toca `.claude/`. | rama: claude-system-core v0.11.0 (build de artefactos de mintstash).

2026-08-05 | verify-source/regla-existente-no-disparó | **TRES fallos del mismo tipo en una sesión, con la regla ya escrita y siempre activa.** (1) Describí el propósito de `scripts/founder-contacted.json` **desde su nombre** —dije que era un CRM duplicado de people.md— y Andrés aprobó borrarlo con esa descripción; al abrirlo resultó ser la lista de exclusión de la métrica de retención, así que borrarlo habría cambiado en silencio lo que mide. (2) Borré una lista de la BD con guarda y verificación de datos, pero **no grepeé el repo por su id**: seguía escrito a mano en `price-invariants.ts` como lista fijada del chequeo diario, que dejó de cubrirla sin fallar ni avisar. (3) Afirmé que "no existe capa de tokens, solo 3 variables" tras grepear **un solo fichero** (`global.css`); existe `tokens.css` con 60 tokens y adopción del 88% en color. | **enfermedad candidata: comprobación PARCIAL tratada como completa.** Los casos 2 y 3 no son "no verifiqué" —sí verifiqué, contra *una* superficie— sino que no me pregunté si esa superficie era toda la fuente. `verify-source` tiene algo cercano en su disciplina punto 2 ("al filtrar por un campo, comprueba nulls/cobertura primero"), pero acotado a columnas con nulos; es el mismo fallo, generalizado a "qué superficie miré". El caso 1 es la forma pura: afirmar desde un nombre de fichero. | **enmienda propuesta a `rules/verify-source.md`, cuarto punto de la disciplina:** *"Una comprobación parcial no es una comprobación. Al verificar, nombra QUÉ superficie miraste y si podría haber otra: un fichero no es el repo, una tabla no es el esquema, la BD no es el código. Antes de borrar o renombrar algo con id, la superficie obligatoria incluye un grep del repo por ese id."* | **APLICADA la enmienda el mismo día** (punto 4 de la disciplina de `verify-source`). | **Y queda abierto lo que la enmienda NO resuelve, que Andrés pidió anotar: falta un gate.** Los tres los cazó él preguntando. El caso del borrado **sí es gateable** —un chequeo de que ningún id que se borra siga escrito en el repo— y de hecho ya se registró como regla de proyecto en el `CLAUDE.md` de MintStash. Los otros dos **no**: son afirmaciones en prosa, y ningún grep las ve. Para esos, el único mecanismo plausible es que el contrato de reporte exija **nombrar la superficie consultada** al afirmar un hecho, de forma que omitirla sea visible en el propio texto en vez de invisible. Candidato para la retro; no se construye nada hoy. | contexto: sesión MintStash del 05-08, iniciativa del panel de control.

2026-08-06 | meta / enfermedad de nivel superior | Andrés: "si no te hablo de enfermedad vs síntoma, la gran mayoría del tiempo lo pasas por alto". Cierto y comprobado en la misma sesión: tres horas reorganizando dónde viven las reglas de diseño de MintStash —un doc borrado, otro disuelto, uno nuevo escrito— sin cuestionar ni una vez si el documento era el mecanismo. Andrés tuvo que interrumpir con "esto tiene pinta de matriushkas". Incluso la pregunta que le hice ofrecía tres opciones que eran todas "dónde ponemos el documento". | enfermedad: la capacidad de encontrar el patrón existe pero nada la invoca; el mecanismo de carga era Andrés diciéndolo en voz alta. Idéntica en forma a lo que estábamos diagnosticando en el producto, lo cual es la señal de que es la enfermedad y no otro síntoma. | fix: `rules/patterns.md` con disparador operativo ("antes de reparar, busca la segunda instancia: una es un incidente, dos es un mecanismo") + inline en el template y en el CLAUDE.md de MintStash, que son las vías que sí cargan. | rama: claude-system-core v0.13.0.

2026-08-06 | enforcement | LA MEJOR PRUEBA de la entrada anterior está en este mismo log: la enfermedad de verificar-contra-la-fuente se detectó el 16-jul, recurrió el 17-jul, escaló correctamente a regla (`rules/verify-source.md`) y se metió en `templates/CLAUDE.md.template`... y NUNCA aterrizó en el `CLAUDE.md` de MintStash, que es el repo donde ocurrieron las dos manifestaciones. Llevaba tres semanas siendo prosa justo donde hacía falta. Igual `registro.md`; y `comms.md` no tenía vía de carga ninguna. | enfermedad: el template no es lo que se carga — lo que se carga es el CLAUDE.md desplegado de cada repo, y nada reconciliaba los dos. El sistema de retro funcionaba (detectó, escaló, escribió); lo que faltaba era el último tramo, la entrega. | fix: `hooks/rules-have-loaders.sh`, en SessionStart y callado si todo va bien, comprueba (a) que cada fichero de `rules/` tenga skill, hook o inline, y (b) que el CLAUDE.md desplegado lleve los bullets que el template declara. Con prueba negativa. | medición del día: en MintStash, TODAS las reglas del bloque siempre-activo de CLAUDE.md están a 0 incumplimientos, con gate o sin él (incluida "no switch para dispatch", que no tiene ninguno); todas las que vivían solo en `docs/` están masivamente incumplidas (382 valores crudos, 114 de 129 ficheros importando Mantine). El discriminante no es tener gate: es estar en el fichero que se carga solo. | rama: claude-system-core v0.13.0.

2026-08-06 | tooling / recurrencia de forma | tres bugs propios en la misma sesión, todos "una comprobación que no cubre lo que dice cubrir": el check de tokens de Claude Design solo entendía sintaxis CSS y era ciego a 71 radios en JSX; `review.sh` excluye con `grep -v ".test."` y el punto sin escapar se traga `test-render.tsx` entero; su gate de jerga financiera escanea `src/ui/` cuando el copy vive en `src/shared/i18n/`, mira 3 de los 6 términos del MANIFESTO y excluye toda línea con `=>`. Y luego mi propio gate nuevo cometió la misma forma dos veces (subcadena, y paréntesis leídos como grupo de captura). | enfermedad: un gate en verde no es evidencia de cumplimiento, y ninguno de nuestros gates tiene fixtures. Un gate es código sin tests. | fix parcial: prueba negativa en el gate nuevo. Pendiente: fixtures para los 7 checks de `review.sh` — cada uno con un fichero que viola la regla a propósito y que el check tiene que cazar. | rama: mintstash iniciativa panel de control, I0.

2026-08-06 | miss-vault | el ritual de session-close (`claude-system-core`) asume un vault de proyecto con `active-context.md`/`decisions.md`/`gotchas.md`, pero el trabajo de Landbot (Agent Experience) vive en el vault Obsidian `tech-knowledge` con OTRO esquema (Dec-/Got-/Sys-/Ref- + SCHEMA.md, sin active-context). Hubo que buscar dónde capturar antes de proponer deltas. | enfermedad candidata: el enrutado de session-close/session-open no distingue qué proyecto → qué vault; para Landbot el destino es `tech-knowledge`, para MintStash es `mintstash-knowledge`, y para los del sistema es el vault de proyecto clásico. 1ª aparición explícita; si recurre, session-close debería declarar el mapa proyecto→vault (o detectarlo por cwd/rama). | rama: digestión daily AE 2026-08-06 + captura de deltas.

2026-08-07 | reexplicacion | Andrés pidió "for dummies" DOS veces sobre el resumen de la retro (una tras la puesta al día, otra explícita), con `registro.md` activa. Su diagnóstico, literal: "tu descripción pasada abre muchos ámbitos en cada frase, bullet points ayudarían a hacer menos densa la conversación". | enfermedad candidata: `registro.md` regula el NIVEL de lenguaje (jerga, llano) pero no la DENSIDAD DE ÁMBITOS — cuántos hilos abre cada frase y si lo enumerable se presenta como lista en vez de prosa trenzada. Misma meta-forma que verify-source esta semana: regla correcta con el dominio recortado; el fallo nuevo no se parece a sus ejemplos. 1ª aparición post-v0.8.0 con causa nombrada por Andrés. RESUELTO el mismo día en v0.14.0: cláusula "Densidad de ámbitos" en `registro.md` (una idea por frase; enumeraciones → bullets; no abrir un ámbito nuevo en subordinada), aprobada por Andrés en la pausa de revisión de la retro. Métrica objetivo: 0 "for dummies" sobre explicaciones ya en llano; auditar próxima retro. | rama: claude-system-core retro 2026-08-07.

2026-08-07 | entrega/CRÍTICO-2 (4ª manifestación, escalón nuevo: instalado ≠ CARGADO) | tras el update a v0.14.x, el plugin ENTERO falló al cargar en CC: el cargador nuevo rechaza que el manifiesto declare `hooks/hooks.json` (se auto-carga desde ahora) y el rechazo tumba todo — contrato, rituales, routing, ambos hooks. Sin aviso funcional: `claude plugin list` enseñaba la versión correcta; solo un "Hook load failed" que nadie mira. La clave venía desde v0.7.0; lo que cambió fue el cargador. Hotfix en CC (commit c8e50e3). | enfermedad: la cadena de entrega tiene CUATRO tramos y cada uno falló alguna vez esta semana — regla escrita→CLAUDE.md desplegado (v0.13.0), repo→marketplace (×2), marketplace→instalado (v0.10.0 tres semanas), instalado→CARGADO (hoy). Y el tramo final tiene un límite estructural: nada DENTRO del plugin puede avisar de que el plugin no cargó — el vigilante estaba dentro del incendio. | fix (v0.14.2): canario de carga en `CLAUDE.md.template` (vive fuera del plugin, comprueba que las skills del sistema aparecen y avisa antes de continuar) + paso 5 de verificación de carga en README §Cómo se actualiza. | rama: claude-system-core v0.14.2.

2026-08-07 | entrega/CRÍTICO (3ª tramo de la misma enfermedad → cruza regla de tres) | Andrés reveló que ha estado corriendo el plugin en v0.10.0 mientras el repo iba por v0.14.0: las versiones v0.11–v0.13 (verify-source incluida) NUNCA se cargaron en sus sesiones. Además el commit "Lanza v0.14.0" dejó `marketplace.json` en 0.12.0 (2ª desincronización; la 1ª fue v0.8.0). | enfermedad: la MISMA que v0.13.0 nombró (lo que se carga no es lo que se escribe), un tramo más arriba: rules-have-loaders reconcilia template↔CLAUDE.md desplegado, pero nada reconciliaba repo↔plugin instalado. CONSECUENCIA EPISTÉMICA GRAVE: la retro 08-07 afirmó "las 8 instancias con la regla cargada" — probablemente falso (plugin en 0.10.0 + CLAUDe.md de mintstash sin el bullet hasta el 06-08); el veredicto de auto-auditoría de verify-source queda corregido en addendum del reporte. E irónico: la afirmación falsa de la retro es ella misma un fallo de superficie no comprobada (asumí "regla escrita = regla cargada" sin verificar la versión instalada). | fix aplicado (petición directa de Andrés): paso a paso canónico de update en README §Cómo se actualiza + disciplina de entrega en rules/retro.md (sync marketplace.json + paso a paso en cada bump; fix no instalado = no shipeado). Candidato adicional ofrecido: extender rules-have-loaders para fallar si plugin.json ≠ marketplace.json y avisar si versión corriendo ≠ versión del repo. | rama: claude-system-core v0.14.x.

2026-08-07 | comunicación | petición directa de Andrés (sugerir cortes por producción, no por longitud) se CONSTRUYÓ pero se confirmó en una sola línea enterrada al inicio de un reporte largo sobre otro tema → Andrés creyó que se le había ignorado ("pasaste olímpicamente de mí"). | enfermedad: misma familia que la densidad de ámbitos (resuelta el mismo día en v0.14.0): la información existía pero no era visible. Forma específica: las peticiones personales de Andrés merecen confirmación propia y visible (qué se hizo, dónde quedó), no una línea de transición. 1ª aparición con la cláusula de densidad ya activa; si recurre, la cláusula necesita el ejemplo "una confirmación no es una transición". | rama: claude-system-core retro 2026-08-07.

2026-08-06 | epistémico/rework | **CINCO instancias en una sola sesión (mintstash, arco del alcance de filtros) de una misma enfermedad, y las dos reglas que deberían haberla cazado no dispararon ninguna vez.** Las instancias: (1) mi comprobación de firmas SQL dio verde ante una coma que FALTABA — contaba comas sobrantes; la migración llegó rota a Andrés. (2) Sustituida por `pglast`, dio verde ante un alias de salida dentro de un `CASE` del `ORDER BY`: parsea pero no resuelve; la función reventaba en CADA llamada y volvió a llegar rota. (3) El navegador integrado dijo "producción renderiza vacío" durante ~20 minutos; estaba degradado (URLs obsoletas, red de otra sesión, cuelgue de 300 s) y producción estaba perfecta — estuve a un paso de anunciar una caída y revertir un deploy bueno. (4) Un `EXPLAIN` suelto dio 589 ms vs 286 → lo llamé regresión y escribí una migración para arreglarla; el MISMO plan se midió después en 281 y en 3.774 ms con la BD ociosa: el instrumento no resuelve 300 ms. (5) Una llamada en frío dio 2,5 s → afirmé "la página está al 84% de su presupuesto"; la mediana en caliente es 0,58 s, o sea el 19%. | **por qué no dispararon.** *Patrón antes que reparación* SÍ funcionó, pero solo con defectos de CÓDIGO: la coma→pglast, seis bloques de filtro duplicados→un predicado, dos mocks que discrepaban→un `scopeMatches`, el cron con la firma vieja→la regla del espejo. Cada vez que el defecto estaba en el código fui a por el mecanismo. **Nunca disparó con un defecto en mi EVIDENCIA**, porque dice "un defecto" y eso se lee como defecto de código. Y *verificar contra la fuente* tiene la forma correcta —"una comprobación parcial no es una comprobación"— pero sus tres ejemplos (fichero≠repo, tabla≠esquema, grep≠lectura) son todos de COBERTURA: miraste un subconjunto. Ninguno es de CAPACIDAD: el instrumento no puede contestar la pregunta que le haces. Las cinco de hoy son de capacidad, así que la regla las ve pasar. | **qué debe cambiar.** No otro principio: un disparador que actúe ANTES de afirmar, no después de que me pillen. La regla actual funciona por sus ejemplos concretos, así que la extensión necesita los suyos: *"una pasada no es una medida"* (todo lo que varía), *"verde solo dice lo que el check mira"*, y *"si lo medido y lo observado se contradicen, duda del instrumento primero"* — en la instancia (3) la API devolvía 200 con datos mientras el navegador enseñaba vacío, y aun así seguí creyéndole al navegador. El núcleo accionable es una pregunta forzada: **antes de afirmar un número o un diagnóstico, decir qué NO habría podido detectar mi instrumento.** Las cinco caen con eso. | fix propuesto: cláusula en `rules/` de claude-system-core que extienda *verificar contra la fuente* con el eje de capacidad (hoy solo cubre cobertura); sin gate posible, así que va inline o no existe. | rama: mintstash feat/scope-axes → main, v0.40.1.

2026-08-07 | verify-source/capacidad (1ª recurrencia CON la cláusula nueva ya cargada) | **Me inventé un fallo de producción que no existía, y lo reporté con una cifra.** Andrés preguntó si la comparación de propiedad usa oráculo o impresión. Escribí a mano un cruce de `holdings` con `list_items` uniendo siempre por impresión, lo contrasté con el mismo cruce por oráculo, y presenté la diferencia —«154 de 269, el 57% dicen te falta y la tienes»— como el comportamiento de la app. **Medí mi propia reimplementación.** Las cuatro funciones vivas ya aplicaban exactamente la regla que yo iba a proponer como arreglo: `CASE WHEN lv.is_card_level THEN oo.qty ELSE o.qty END`. | **y hubo una pista que ignoré**: leí la migración 053, que efectivamente une solo por impresión, sin comprobar que la vista que define **ya no existe** (la sustituyó `get_list_summaries`). O sea dos superficies equivocadas encadenadas: un fichero de migración leído como estado vivo, y una reimplementación medida como si fuera el sistema. | **lo relevante para la regla**: la v0.14.0 acababa de añadir el eje de capacidad a `verify-source` («una pasada no es una medida», «si lo medido y lo observado se contradicen, duda del instrumento primero») y estaba cargada y verificada por el canario en esa misma sesión. No disparó. Los tres ejemplos de la cláusula son sobre **medir mal algo real**; ninguno cubre **medir un sustituto que yo mismo he construido**. Es una forma distinta: el instrumento funciona perfectamente, lo que no es el sistema es el sujeto. | **candidato para la retro, sin construir hoy:** un cuarto ejemplo en la cláusula de capacidad, del tipo *«si has escrito la consulta que mides, estás midiendo tu hipótesis, no el sistema: llama a la función viva»*. Y una nota más general que quizá sea la misma enfermedad de siempre en otra ropa: `pg_proc`/`pg_get_functiondef` es la fuente; `supabase/migrations/*.sql` es el historial. | contexto: mintstash, spec de I1, tras la reacción de CD.

2026-08-08 | verify-source/capacidad · el espejo comprobado a medias | Al portar las celdas de una fila a definiciones de columna —un refactor cuyo requisito explícito es ser invisible— escribí un test que comparaba **lo que pinta cada celda** y lo di por espejo. Dos diferencias se colaron y las cazó **una captura de pantalla que Andrés mandó**, no los tests: (1) la columna de acabado va SIN cabecera y mis definiciones le pusieron una, porque el test miraba celdas y no cabeceras; (2) la fila resolvía edición y rareza con `item.printing?.X ?? item.X`, dando prioridad a la impresión, y mi definición leía solo el campo plano — el fixture tenía el plano puesto y `printing: null`, así que pasaba en verde por la mitad que no fallaba. | **enfermedad, y es una vuelta de tuerca sobre la cláusula de capacidad de v0.14.0:** un test de equivalencia comprueba la equivalencia QUE SU FIXTURE PERMITE, y el fixture lo escribe quien ya tiene la hipótesis en la cabeza. No es cobertura insuficiente ni instrumento incapaz: es que el sujeto de comparación lo eligió el mismo que quería que coincidiera. Muy cercano a la entrada del 07-08 (medir una reimplementación propia), pero aquí SÍ se medía el original — solo que un trozo de él. | **candidato para la retro:** al afirmar «esto es equivalente a lo anterior», nombrar qué dimensiones cubre la comparación y cuáles no (contenido sí, cabecera no; caso con dato sí, caso con el dato en el otro sitio no). Y una nota práctica que hoy valió más que cualquier test: **una captura de pantalla del ANTES, pedida al humano, es el único oráculo que no comparte mis sesgos.** Costó dos mensajes y cazó dos regresiones. | contexto: mintstash I1, migración de ListItemRow a definiciones de columna.

2026-08-08 | entrega/gate ausente (misma familia que el plugin que no cargaba) | `package.json` de mintstash declaraba `"lint": "eslint ."` desde hacía meses **sin fichero de configuración**: el comando reventaba con el error de migración de ESLint 9 y nadie lo notó, porque nadie lo corría. `react-hooks/exhaustive-deps` llevaba todo ese tiempo instalada en `node_modules` sin mirar una línea. Costó un fallo en producción de dos días: un `useMemo` que leía tres filtros sin declararlos, así que marcar un color en la colección propia no filtraba nada. | **enfermedad: un comando declarado en `package.json` que nadie ejecuta no falla, no avisa, y da la impresión de que hay una comprobación donde no la hay.** Es exactamente la forma del plugin que no cargaba (07-08) y de las reglas que vivían solo en `docs/` (06-08): la diferencia entre existir y cargarse. Tercera manifestación de la misma familia en tres días. | fix aplicado: `eslint.config.js` con esa regla en `warn`, corriendo en el pre-push con `--max-warnings` clavado al recuento del día. Techo, no bendición. Verificado reintroduciendo el fallo exacto. | **lo que queda abierto, y es la pregunta buena:** ¿qué OTROS scripts declarados no ha ejecutado nadie nunca, en este repo y en los demás? Correrlos uno a uno cuesta minutos. Nadie lo ha hecho. Candidato a comprobación genérica del sistema, no solo de mintstash. | contexto: mintstash v0.41.2.

2026-08-09 | verify-source/capacidad · la regla se cargó, se leyó en voz alta, y aun así no disparó | El documento de la iniciativa de mintstash dice, en negrita y etiquetado como **normativo**: «Con muestreo repetido de punta a punta al nivel de privilegio del usuario, y mirando la mediana. **No con un `EXPLAIN ANALYZE` suelto**». Lo leí y lo CITÉ en esta misma sesión al explicarle a Andrés el estado de I7. Horas después entregué la migración 114 afirmando que ahorraba 1,4 s de carga de página, cifra sacada de exactamente un `EXPLAIN ANALYZE` del bloque viejo (470 ms) contra uno del nuevo (3,95 ms), multiplicado por tres llamadas. Medido después de punta a punta con 12 muestras: las medianas se movieron centésimas. La migración era correcta por otra razón (contestaba la pregunta equivocada, y unificaba tres copias), pero el ahorro que anuncié no existe. | **enfermedad, y es un peldaño por encima de la familia conocida.** Lo documentado hasta hoy es «una regla existe si se carga» (plugin que no carga, `docs/` sin loader, script declarado que nadie corre). Esto es distinto: la regla estaba cargada, la había leído ese mismo día y la había repetido a Andrés. **No falló el loader, falló el disparador.** Y la hipótesis de por qué es incómoda: la regla se activa cuando dudo de un número, y no dudé porque el número decía que mi trabajo había valido la pena. Es razonamiento motivado, no olvido. | **candidato para la retro, sin construir hoy:** el disparador de «una pasada no es una medida» está colgado del acto de MEDIR, y ahí sí funciona (esa misma sesión rechacé dos veces un dato por muestrearlo mal). Habría que colgarlo también del acto de **afirmar una mejora**: antes de escribir «esto ahorra X», la pregunta forzada es «¿lo he medido por el mismo camino por el que lo va a sufrir el usuario?». Nótese que el fix NO es más escepticismo genérico: es un segundo punto de enganche para la misma regla. | contexto: mintstash, migración 114, I7.

2026-08-09 | verify-source/capacidad · un recuento no es una clasificación (3 instancias en una sesión) | (1) Escribí una guarda que decidía qué días de valor de lista se apuntan y cuáles no. La comprobé sobre un día concreto: «30 listas existían, 24 se apuntan, 5 se descartan». El recuento parecía sano y la di por buena. **Nunca pregunté cuáles cinco.** Eran las listas de set completo y artista completo, que traen cartas sin precio para siempre: quedaban sin histórico ENTERO, no ese día. Salió en la salida del relleno (48 listas de 57) y costó una migración correctiva el mismo día. (2) Al planear el eje de precio medí qué funciones «tenían el precio a mano» buscando el nombre de la columna en el cuerpo de la función; la respuesta correcta era mirar de qué VISTA lee cada una, y las seis lo tenían. (3) La misma sesión, una comprobación de visibilidad escrita a mano en vez de usar el RLS que ya la implementaba, que dejó ciego al backend ante las listas privadas. | **enfermedad: cuando un control PARTE una población, mirar el tamaño de los trozos no es mirar el control.** Es primo del eje de capacidad del 06-08, pero con forma propia: el instrumento funciona, la muestra es completa, y aun así la comprobación es ciega, porque la pregunta que contesta («¿cuántos quedan fuera?») no es la que se le hace («¿son los correctos?»). Las tres instancias son del mismo día y ninguna la cazó una comprobación previa: la (1) la cazó la salida del relleno, la (3) la cazó el invariante diario a la mañana siguiente. | fix aplicado en el proyecto: regla inline en el `CLAUDE.md` de mintstash — *un control no está hecho hasta correrlo contra la población entera y NOMBRAR los que excluye*, con las dos instancias dentro y con el puntero a `review-selftest.sh`, que es el mecanismo que ya hace esto bien para los gates de texto y que nadie había extendido a las guardas de SQL. | contexto: mintstash, migraciones 110 a 113. La entrada es para que la retro vea la recurrencia, no para pedir mecanismo nuevo.

2026-08-09 | comunicación/reexplicacion | Le ofrecí a Andrés una decisión de diseño («el eje de precio, ¿suelo o tramos?») con opciones bien construidas, previews y recomendación, **sin nombrar en qué pantalla vive el filtro**. Descartó la pregunta y contestó: «de qué vista estamos hablando si no es el radar?». La pregunta era incontestable tal como estaba formulada, y encima al aclararla se vio que mi propio argumento principal («reutilicemos el modelo del radar») era más flojo de lo que lo había vendido, porque el radar es un gráfico de dispersión y la tabla se puede ordenar por precio. | **enfermedad: misma familia que la entrada de comunicación del 07-08** (la información existía pero no era usable). Forma específica: al pedir una decisión, el SUJETO —qué superficie, qué usuario, qué pantalla— es parte de la pregunta, no del contexto que se asume compartido. Yo llevaba media hora dentro del código de esas cuatro páginas; Andrés venía de otra cosa. | **nota útil, no regla:** el descarte de la pregunta fue más barato que una respuesta a la pregunta mal hecha. Que Andrés pueda descartar una `AskUserQuestion` sin coste está funcionando como amortiguador de mis preguntas mal planteadas. | contexto: mintstash, I2, eje de precio.

2026-08-09 | herramienta/miss | El directorio de trabajo del shell se desplazó solo a `~/Documents` a mitad de sesión, y un `find` de ficheros de smoke me devolvió resultados de `ceo-tools` (otro proyecto) mientras yo creía estar en mintstash. Lo cacé porque `ls docs/` falló, no porque revisara la salida. Un `ls` que hubiera acertado por casualidad me habría dado evidencia del repo equivocado. | **enfermedad: es la forma menor de «medir el sujeto equivocado» del 07-08**, pero por herramienta y no por razonamiento: allí construí yo el sustituto, aquí me lo dio el entorno. Coste real hoy: dos comandos. Coste posible: leer el estado de otro proyecto como si fuera éste. | **candidato barato, si recurre:** anclar los comandos al repo con ruta absoluta o `cd` explícito en cada llamada en vez de confiar en que el cwd persista, que es lo que la propia herramienta promete y hoy no cumplió. | contexto: mintstash, sesión larga del 09-08.

2026-08-10 | entrega/higiene | Al commitear la captura de la sesión al vault de mintstash hice `git add -A`, y el commit se llevó **siete ficheros de sesiones anteriores** que llevaban tiempo en el árbol sin entrar (el brief de Cowork del 09-08, el doc del recomendador, la decisión de la gráfica, `product-overview`, `working-preferences`, `skill-candidates`, la iniciativa), todos bajo un mensaje que solo describía lo mío. Lo cacé leyendo la salida del commit, no antes de hacerlo. Se partió en dos después, a petición de Andrés. | **enfermedad: `git add -A` afirma «todo lo pendiente es mío», y en el vault esa premisa es falsa por defecto**, porque ahí escriben también Cowork y las sesiones anteriores. No es un descuido puntual del que aprender a tener más cuidado: es que el sujeto del comando no coincide con el sujeto de la intención, y coincide por casualidad en los repos de código donde el hábito se formó. | fix barato y mecánico: en el vault, `git status --short` antes de cualquier `add`, y si aparece algo que no toqué en esta sesión, `add` por rutas explícitas. | contexto: mintstash-knowledge, cierre de la sesión del eje de precio.

2026-08-10 | herramienta/miss | Los clics por `ref` del navegador interno dejaron de aterrizar tras un re-render de la página. El tool reportaba `left_click at (291, 319)` con las coordenadas correctas, `elementFromPoint` confirmaba que el elemento estaba justo ahí, y un listener en fase de captura no recibía nada. Perdí un buen rato convencido de haber roto el cambio de pestaña con mi propio código: llegué a reiniciar el servidor de desarrollo y a leer los logs de Vite buscando una causa que no existía. Se arregla tomando una captura de pantalla, que recachea las dimensiones con las que el tool mapea refs a píxeles. | **enfermedad: hermana de la del cwd desplazado (09-08), pero peor por cómo se presenta. La herramienta falla en silencio Y CON APARIENCIA DE ÉXITO**: devuelve «left_click at (x, y)», que se lee como confirmación de que el clic ocurrió. Un fallo que se reporta como éxito cuesta mucho más que uno que da error, porque desvía la sospecha hacia el propio trabajo. | candidato, barato: ante un clic por `ref` que no produce efecto observable, la primera hipótesis es el mapeo y no el código — una captura antes de dudar de lo que uno acaba de escribir. | contexto: mintstash, verificación del eje de precio y de la siembra del radar.

2026-08-10 | verify-source/capacidad · una comprobación que no podía salir roja | Para verificar que un trigger recién creado DISPARA, propuse contar las filas donde la columna que mantiene y la función que la calcula discrepan. Lo ofrecí **dos veces** como el cierre pendiente del arco. Al correrlo dio cero — y solo entonces miré la fecha de la última escritura de la tabla: 09-08 a las 18:55, **anterior a la creación del trigger**. Ninguna fila se ha escrito desde que existe, así que ese cero habría salido cero con el trigger roto, ausente o perfecto. | **enfermedad, y es un peldaño ANTES de «un recuento no es una clasificación» (09-08).** Allí la comprobación miraba la población entera y contestaba la pregunta equivocada; aquí contestaba la pregunta correcta sobre una población **vacía**. La pregunta que faltaba no es «¿qué mide esto?» sino «**¿puede este check salir rojo hoy?**». Lo incómodo es que el propio repo ya tiene el mecanismo que lo hace bien: `review-selftest.sh` corre cada gate contra fixtures que lo violan a propósito, exactamente para demostrar que sabe ponerse rojo. Lo cité y lo ejecuté en esta misma sesión, y no se me ocurrió aplicármelo a la comprobación que estaba proponiendo. | **candidato para la retro:** antes de ofrecer una comprobación como cierre de algo, exigirse nombrar el caso concreto en que saldría roja. Si ese caso no existe hoy, no es un cierre, es un placebo — y peor que no comprobar, porque se apunta como verificado. | contexto: mintstash, migración 118, trigger de `printing_cache`.

2026-08-10 | instrumento/descarte | Para decidir si dos componentes eran gemelos medí sus líneas comunes: `CollectionTabsLayout` contra `ListTabsLayout` dio **39 de 251**, y con ese número los reporté como distintos y los dejé fuera del alcance. Eran el mismo componente escrito dos veces —mismas pestañas, mismo `CardDashboard`, mismo panel de cartas, solo cambiaban los identificadores— y al fusionarlos pasaron de 597 líneas en cuatro ficheros a 197 en dos. Lo cazó Andrés con una pregunta de tres palabras: «¿la idea no era unificar las vistas?». | **enfermedad: usé para DESCARTAR un instrumento que solo puede CONFIRMAR.** Un medidor de texto que sale alto prueba parecido; que salga bajo no prueba nada, porque la duplicación cara es la misma decisión tomada dos veces y esa aparece con los identificadores cambiados en cada línea. Es la familia del trigger del 09-08 (una comprobación que no podía salir roja) con el signo invertido: allí el check no podía fallar, aquí no podía acertar en la dirección que yo le estaba pidiendo. Lo agravante: **la cabecera de `CardTableSection` y el spec de I1 dicen literalmente «la duplicación es estructural, no textual; quien entre buscando código repetido no lo encontrará y concluirá que no hay duplicación», y había leído los dos esa misma sesión.** Leer la advertencia no me impidió ser exactamente el caso que describe. | fix barato: ante dos componentes que sospechas gemelos, **leerlos en paralelo ANTES de medirlos**; la métrica entra después y solo para confirmar. Escrito ya en `working-preferences` de mintstash. Candidato de sistema: cuando un documento del proyecto describe un modo de fallo, el retrieve debería devolvérmelo como advertencia activa y no como prosa que ya «leí». | contexto: mintstash, I1, fusión de los dos layouts de pantalla de cartas.

2026-08-10 | alcance/encuadre | Andrés pidió «los envoltorios de página», citando su propia frase del 10-08: *«las 4 deberían ser 1»*. En Phase 0 enumeré las cinco duplicaciones que iba a atacar y dije explícitamente qué dejaba fuera; lo aprobó. Entregué eso y lo reporté como «los envoltorios están hechos». Su siguiente mensaje fue «¿por qué aún existe una compartida y otra que no? ¿la idea no era unificar las vistas?». | **enfermedad: enumerar el alcance NO es lo mismo que declarar la distancia respecto a lo pedido.** Mi Phase 0 era honesto sobre su contenido y mudo sobre su ambición: listaba lo que haría sin decir «esto no llega a que las cuatro sean una, y esto es lo que faltaría». Un aprobado sobre una enumeración se lee como aprobado sobre el objetivo, y el hueco solo aparece en la entrega, que es el momento más caro para descubrirlo. | candidato: cuando la petición trae una frase de objetivo («las 4 deberían ser 1»), el Phase 0 cierra citándola y contestando en una línea **cuánto de ella cubre el alcance propuesto**. No es más trabajo: es una frase, y va donde ya se pide confirmación. | contexto: mintstash, I1, envoltorios de página.

2026-08-10 | requisito/sin instrumento | El spec de I1 pone como pre-requisito «medir el primer pintado de las cuatro páginas, con muestreo repetido y mediana» antes del primer commit, y como criterio de terminado que no haya empeorado. Lo busqué: **no existe ningún instrumento en el repo** —ni script, ni registro de mediciones previas, nada— y el arco lleva quince trozos cerrados con ese requisito escrito. Lo declaré como hueco en los dos reportes en vez de fingir que lo cumplía. | **enfermedad: un requisito sin herramienta se cumple diciendo que sí.** No falló nadie en particular: falló que la regla se escribió en prosa y nunca se le construyó el cargador, que es exactamente la regla de sistema «una regla existe si se carga» aplicada a un criterio de aceptación. Quince trozos lo han saltado y el documento sigue pidiéndolo, así que hoy el spec afirma una garantía que nunca ha tenido. | fix: o se construye el medidor (un script que abra las cuatro rutas y saque la mediana de N pasadas) o se retira el requisito del spec y se dice qué lo sustituye. Lo que no puede seguir es pedido y no medido, porque eso es peor que no pedirlo: hace creer que está cubierto. | contexto: mintstash, spec-i1, pre-requisitos.

2026-08-10 | frio/contrato del retrieve · el ritual pide dos ficheros que el vault no tiene | El ritual de apertura manda rozar «los cuatro»: `active-context.md`, normativos, **`decisions.md`** y **`gotchas.md`**. El vault de mintstash no tiene ninguno de esos dos, y no por descuido: las decisiones son ficheros fechados (`decision-*.md`) y los gotchas están **promovidos** dentro de `working-preferences` y `tech-debt`, que es el statu quo que `CONVENTIONS.md` ratifica explícitamente. Lo resolví mapeando a mano y declarándolo como hueco en el reporte de apertura, así que el coste de hoy fue una línea. | **enfermedad: el ritual nombra ficheros donde debería nombrar CATEGORÍAS.** «Decisiones recientes» y «gotchas del área» son las preguntas; `decisions.md` y `gotchas.md` son una implementación posible de las respuestas, y este vault eligió otra a propósito. Un rito que nombra la implementación obliga a cada proyecto a tener el layout del rito, o a que el agente improvise el mapeo en cada apertura — y el que improvisa no deja rastro de si acertó. | **y no es la primera vez que este desajuste asoma:** `CONVENTIONS.md` arrastra desde el 25-07 una decisión pendiente de Andrés sobre adoptar la taxonomía `tech/` (Systems/Decisions/Gotchas/Runbooks) o ratificar el statu quo y liberar la carpeta, con los dos triggers ya cumplidos. Es la misma tensión vista desde el otro lado: el vault sabe que no encaja con la taxonomía y lleva dos semanas sin decidir. | **candidato para la retro, sin construir hoy:** reescribir los puntos 3 y 4 del rito como preguntas con una lista de sitios donde pueden vivir la respuesta («decisiones recientes: `decisions.md`, o ficheros `decision-*`, o el bloque de decisiones del estado vigente»), y que el vault declare su mapeo una vez en su `CONVENTIONS`. Resolvería el desajuste sin esperar a la decisión de `tech/`. | contexto: mintstash, apertura de la sesión del orden de los workflows.

2026-08-10 | capture/delta duplicado · propuse guardar algo que el vault ya decía | Al cerrar, ofrecí cuatro deltas y el cuarto era «el MCP de Supabase es de solo lectura, que no estaba escrito en ningún sitio». Andrés lo aprobó. Al ir a escribirlo, `working-preferences` §Tools ya lo decía desde el 18-05, con el flag y todo: `read-only mode (--read-only flag)`. Lo cacé porque leí la sección antes de editarla, no antes de proponerla. Reconvertido en el acto a lo que sí faltaba —la consecuencia al DISEÑAR una verificación, que descarta forzar un estado desde la sesión del agente— y declarado como corrección en el reporte. | **enfermedad: el capture propone contra la memoria de la sesión, no contra el vault.** El retrieve de apertura leyó las secciones núcleo de `working-preferences` (workflow, smoke, arquitectura, calidad) y §Tools no estaba entre ellas, así que a la hora de proponer, «no lo he visto» se convirtió en «no está». Es la familia de *verify-source* aplicada al propio vault: afirmé la ausencia de un dato con la fuente a un grep de distancia. Y el coste no es teórico — un delta duplicado envenena el retrieval igual que un `validated: false` perpetuo, porque grep devuelve las dos copias con el mismo peso. | **fix barato, mecánico, y encaja donde ya hay un paso:** antes de proponer un delta, grepear el vault por su idea (dos o tres términos, no el fichero entero). Si hay acierto, el delta no es «añadir» sino «afilar lo que ya está», que además suele ser mejor entrada. Nótese que la aprobación de Andrés no puede filtrar esto: él aprueba una descripción de una línea, no puede saber si ya existe. **El gate humano de entrada al vault no protege contra duplicados; solo el grep lo hace.** | contexto: mintstash, cierre de la sesión del orden de los workflows.

2026-08-10 | herramienta/red de seguridad ausente · planifiqué un backup, falló en silencio, y mutéé el fichero igual | Para demostrar que un gate nuevo sabe ponerse rojo sobre el fichero REAL (no solo sobre fixtures), tenía que invertir a mano el orden de los pasos en `.github/workflows/playrate-weekly.yml`. Encadené `cp fichero /scratchpad/backup && node -e "muta el fichero"`. El `cp` falló —el cwd del shell seguía en un subdirectorio de una llamada anterior, así que la ruta relativa no resolvía— y el `&&` hizo su trabajo: el `node` no corrió. **Y en la llamada siguiente relancé solo la mutación.** O sea que modifiqué un fichero real del repo creyendo tener una copia que no existía. Salió bien porque el fichero estaba commiteado y porque reconstruí el orden con la transformación inversa, pero eso es suerte de circunstancias: si el fichero hubiera tenido cambios sin commitear, o si la inversa no hubiera sido exacta, no había vuelta. | **enfermedad, y es la de siempre en ropa nueva: `A && B` no es una red de seguridad, es una secuencia.** El `&&` garantiza que B no corre si A falla, y eso funcionó. Lo que no puede garantizar es que yo no relance B por separado en el turno siguiente, que es exactamente lo que hice, porque para entonces «tengo backup» ya era una premisa de mi cabeza y no un estado del disco. Es *«una regla existe si se carga»* aplicada a los backups: **un backup que falló es un backup que no existe, y nada me lo dijo al volver a mutar.** El fallo no fue no verificar A; fue que la verificación de A vivía en el mismo comando que B y no sobrevivió al cambio de turno. | **enfermedad menor y RECURRENTE que la habilitó:** el cwd del shell persiste entre llamadas y me mordió **tres veces** esta sesión (`cp`, `sed`, `git rev-parse`). La entrada del 2026-08-09 ya dejó escrito el candidato *«anclar los comandos al repo con ruta absoluta o `cd` explícito en cada llamada en vez de confiar en que el cwd persista»*. Recurrió, así que deja de ser candidato: dos apariciones en dos días. | **candidatos para la retro, por orden de valor:** (1) cuando una mutación destructiva y su copia de seguridad van en llamadas distintas, **comprobar el backup por su cuenta antes de mutar** (`ls -la` de la copia, no el exit code del `cp` de hace dos turnos); (2) preferir la red que ya existe y no depende de mí —`git stash`, o simplemente commitear antes de mutar— sobre una copia a mano en el scratchpad; (3) rutas absolutas en todo comando de shell, ya escrito el 09-08 y ahora con segunda instancia. | contexto: mintstash, verificación del gate de orden de los workflows de datos.

2026-08-10 | entrega/segunda implementación · escribí la cuarta Y LA QUINTA del día, y llevaba el día entero arreglando esa misma clase | La sesión fue de punta a punta sobre un patrón: reglas escritas dos veces. Retiré ocho filtros de Scryfall distintos y los reduje a uno; colapsé seis copias de un predicado en una; encontré que el repo ya tenía `TOURNAMENT_LEGAL_PAPER_FILTER` en el dominio y nadie lo componía; y dejé el diagnóstico escrito en el vault con el título «nunca escribir la segunda». Después, para el script de purga, **escribí mi propio wrapper de `fetch`** en vez de componer `fetchScryfall`, que ya existía en `seed-card-list` con reintentos y `retry-after`. Murió en un 429 tras preguntar a Scryfall por 15.867 impresiones, sin haber escrito nada. | **enfermedad: la clase que estoy arreglando no es una clase de la que yo esté fuera.** Todo el día miré el patrón desde la posición del auditor —«¿quién escribió la segunda?»— y en ningún momento la pregunta se giró hacia lo que yo estaba escribiendo en ese mismo turno. Y el detalle que lo agrava: el fichero del que importé `chunkArray` es el mismo que exporta el `fetch` con reintentos. Lo tenía en la línea de import. No fue no saberlo, fue no preguntar. | **Y hubo una quinta, después de escribir esta entrada, que es la peor de las cinco.** `scryfallPrintingQuery` pegaba el filtro al final del alcance sin envolverlo, así que una búsqueda de usuario con un `or` dejaba media consulta sin filtrar (`bolt or shock game:paper` se lee como `bolt or (shock game:paper)`; las dos formas dan 58 y con paréntesis 57). El repo ya lo resolvía: `cheapestPrintingSearchQuery` envuelve sus ids en paréntesis **y su test lo explica con palabras** — *«wraps several cards in parentheses so the exclusions apply to all of them, not just the last»*. Había leído ese test esa misma sesión, actualizado sus assertions, y aun así escribí la versión sin envolver. **Leer la advertencia no impide ser el caso que describe**, que es exactamente lo que ya decía la entrada del medidor de líneas del mismo día. | **candidato para la retro, y es específico:** al escribir un helper de infraestructura (fetch, retry, backoff, paginación, chunking) dentro de un script nuevo, grepear el directorio por el verbo antes de escribirlo. No es escepticismo genérico: es que esos cinco helpers son los que más se duplican porque son los más aburridos de buscar. Nótese que el gate de ingesta que construí HOY habría cazado un filtro duplicado y no puede cazar esto: los gates que dejé cubren las reglas de dominio, no las de fontanería. | contexto: mintstash, purga de impresiones digitales.

2026-08-10 | verify-source/capacidad · reporté un suelo con la etiqueta de suelo puesta, y el número que se quedó fue el suelo | Andrés tenía que decidir si ensanchar un tipo del dominio y me pidió el tamaño. En vez de estimarlo, añadí el campo como obligatorio y le pregunté al compilador: **5 errores en 5 ficheros**. Lo reporté con el aviso explícito de que `tsc -b` se para en el primer proyecto que falla y que «el 5 es un suelo, no un total». Él decidió con ese número. El total real fueron unos 25 ficheros, de los que 72 errores caían en un solo fixture. | **enfermedad, y es la del 09-08 con el signo cambiado.** Allí afirmé una mejora sin medirla por el camino del usuario; aquí sí medí, sí nombré la limitación del instrumento, y aun así entregué la cifra en la posición donde se lee la respuesta. **Poner el aviso al lado de un número no neutraliza el número:** lo que Andrés se llevó fue «son 5», porque el 5 estaba en negrita y el aviso en subordinada. La decisión salió bien por suerte (25 ficheros seguía siendo asumible), no porque el reporte fuera honesto. | **candidato:** cuando el instrumento solo puede dar una cota, **reportar la cota como cota y no como número** — «al menos 5, y el instrumento no puede decirme el techo» — o gastar los treinta segundos en levantar el techo de verdad (aquí: `tsc -p` sobre los tres proyectos por separado, que sí lo habría dado). Un rango honesto vale más que un número con nota al pie. | contexto: mintstash, ensanchar la entidad `Printing` con `typeLine`. |

2026-08-10 | comunicación/el disparador no saltó dos veces en la misma sesión | Andrés me lo dijo dos veces con horas de diferencia: primero «dumb this down, está muy densa la lectura», y al final «me das un veredicto? me cuesta deducirlo de toda la respuesta». Las dos veces el contenido era correcto y estaba verificado contra la base; lo que falló fue el orden — enterré la conclusión debajo del recorrido para llegar a ella. | **enfermedad: es la familia del 09-08 (la regla estaba cargada y no disparó), no la de «falta una regla».** El contrato de reporte pide TL;DR en la primera línea y el `CLAUDE.md` del proyecto pide registro llano por defecto; los dos estaban cargados y los dos los había aplicado bien en la apertura de esta misma sesión. La hipótesis de por qué se cae: **el disparador está colgado del acto de reportar, y se debilita en proporción al trabajo acumulado detrás.** Cuanto más verificado tengo que contar, más se parece el reporte a un registro de lo que hice y menos a una respuesta a lo que se preguntó. | fix ya aplicado, en memoria y no en el log: veredicto en la primera línea, una frase, sin matices; los «necesito que decidas» numerados justo debajo; la evidencia y las autocorrecciones al final y no intercaladas. **Lo que queda para la retro es la forma general:** un contrato que se cumple al empezar la sesión y se afloja al final no está cargado, está recordado. | contexto: mintstash, sesión larga del filtro de ingesta.

2026-08-10 | gate/capacidad · construí tres gates y ninguno comprueba que la regla haga efecto | Cerré una sesión larga con tres gates nuevos, los tres del mismo patrón: la lista de sujetos derivada del directorio y no escrita a mano, y cada uno visto rojo contra el árbol real reintroduciendo el fallo exacto. Buen material. Pero los tres comprueban que la regla **esté**: que el script nombre el suelo `game:paper`, que la fila de cache nombre `type_line`, que el orden de los pasos sea el correcto. **Ninguno comprueba que la regla haga efecto.** El bug del `or` —el filtro pegado al final sin envolver el alcance, así que media consulta de usuario salía sin filtrar— pasó los tres en verde, porque el suelo ESTABA en la cadena. | **enfermedad: es el eje de capacidad del 06-08 aplicado a mis propios gates.** Lo documentado hasta hoy es «¿puede este check salir rojo?» (el trigger sin escrituras, 09-08) y «¿mide el sujeto correcto?» (el medidor de líneas). Esto es un tercer filo: el check mide el sujeto correcto, sabe ponerse rojo, y aun así solo verifica la PRESENCIA de una regla en el texto y no su EFECTO sobre el dato. Un gate de presencia es barato y por eso es el que sale; el de efecto exige un caso real con su resultado esperado, que es un test y no un grep. | **y el dato que hace esto material de retro y no una nota:** de los cinco defectos encontrados en esa sesión, **tres los cazó redactar un reporte** — el `or`, el jsonb vestigial de `card_lists` y la fila muda del radar de la trial. Ninguno lo cazó un gate, un test ni una comprobación deliberada. Los tres salieron de escribir en prosa lo que el código hacía y ver que la frase no se sostenía. Si la escritura está funcionando como el instrumento más productivo de la sesión, conviene preguntarse por qué no está modelada como un paso y sí lo están los gates que no encontraron nada. | contexto: mintstash, arco del filtro de ingesta, v0.45.1.

2026-08-11 | instrumento/exclusion-no-declarada · el `grep` de la sesión filtra por `.gitignore` y me dejó barrer secretos en vacío | Tras limpiar 46 entradas del historial de la terminal, barrí el home buscando la service role key y reporté «en ninguno». Era falso: `grep` en una sesión de Claude Code es una función de shell que envuelve `ugrep --ignore-files --hidden`, o sea que en recursivo **salta todo lo gitignoreado**, que es exactamente donde vive un secreto. Con ruta explícita funciona, y por eso el fallo es invisible: el mismo comando acierta y falla según cómo lo llames. | **lo que lo cazó**: un control positivo, no una relectura — pedirle que encontrara la clave en `.env.scripts`, donde yo sabía que estaba, y ver que no la encontraba. Un comando. | **el eje es nuevo**: no es capacidad («una pasada no es una medida») ni sujeto («medí mi propia reimplementación», 07-08). Es que **la herramienta trae una política de exclusión que no declara**, así que el barrido en negativo no significa lo que parece. Un «no hay ninguno» de un instrumento con filtro por defecto no es un cero, es un no-sé. | **candidato para la retro**: hacer del control positivo un paso obligatorio antes de reportar cualquier barrido cuyo resultado sea negativo. Es el único de los cuatro de hoy que propondría construir. | contexto: mintstash, rama código, cierre de los TODOs de tooling

2026-08-11 | regla-cargada-no-seguida · corrí `npx vitest run` con la prohibición leída veinte minutos antes | `CLAUDE.md` lo prohíbe inline y con su coste documentado; el commit `beedb32` de la noche anterior existe **precisamente** para arreglar los cinco sitios que mandaban ese comando; leí el fichero entero al abrir la sesión y cité ese commit en mi propio reporte de apertura. Y aun así lo escribí para correr un test suelto, porque `npm test` parecía significar «la suite entera». Esa corrida no quedó grabada. Andrés no tuvo que corregirme: lo dije yo al reportar, y luego descubrí que `npm test -- <fichero>` hace exactamente lo que yo quería y sí graba — está documentado en la cabecera de `scripts/run-tests.mjs`. | **por qué es el evento más incómodo del log**: es enforcement por prosa fallando con la prosa recién escrita, recién leída y recién citada. Ninguna de las tres vías de «una regla existe si se carga» estaba en juego: la regla ESTABA inline, que es una de las tres. | **lo que sugiere, sin construirlo hoy**: la regla se escribió como prohibición y no como sustituto. «Nunca X» sin «para el caso de correr un solo fichero, Y» deja al lector inventándose el camino. Segunda observación para la retro: puede que el hueco no sea de enforcement sino de que la regla no cubría el caso de uso que la rompe. | contexto: mintstash, rama código

2026-08-11 | TODO encolado que ya no era cierto al ejecutarse | Los tres TODOs de tooling se redactaron el 07-08 a partir de un informe `/insights` del 05-08 que analizaba sesiones hasta esa fecha. El repo cambió el 06-08 (nació `.githooks/pre-push`) y el 08-08 (nació `eslint.config.js`). Al abordarlos hoy: el TODO 1 estaba casi curado —el pre-push ya corría el gate de convenciones en cada push de cada rama, 0,79 s medidos—, el TODO 3 tenía una causa distinta y más concreta que la que la nota le atribuía, y las cifras del TODO 2 estaban cinco veces cortas (decía «los cinco seeders», eran 27 ficheros). | **no costó trabajo tirado**, porque el Phase 0 lo cazó antes de escribir código y de hecho mejoró el diseño de los tres. Costó que el plan aprobado por Andrés en la retro no era el plan que había que ejecutar. | **candidato para la retro**: un TODO que se encola en el vault lleva fecha de redacción y de evidencia; al abordarlo, el primer paso es revalidarlo contra el árbol, no ejecutarlo. Aplica a todo lo que la retro deja encolado para otra superficie, que es su patrón habitual (Cowork no escribe a repos, así que encola para Claude Code). | contexto: mintstash, rama código

2026-08-11 | copiar una heurística sin su premisa | Mi primer `.githooks/pre-commit` copió el recorte de salida del `pre-push` (grep de las líneas de fallo + `tail -6`). Ese recorte existe porque `npm test` escupe cientos de líneas y el nombre del test que falla se pierde por el final. `review.sh` imprime 26 líneas y ya se formatea. Resultado: el grep tiraba la línea `fichero:línea:contenido`, que es la única accionable porque `review.sh` la imprime sin marcador, y el `tail` arrancaba a media advertencia — **el hook acusaba a `AuthProvider.tsx`, que no tenía nada que ver**. Lo vi yo al probar el rojo, no Andrés. | **la forma**: copié la solución sin comprobar si el problema estaba presente. El recorte es correcto donde nació y mentiroso donde lo pegué. Segunda versión: enseñar la salida entera y decidir por el exit code, que `review.sh` documenta en su cabecera, en vez de por una cadena de texto. | contexto: mintstash, rama código

2026-08-11 | instrumento/pregunta-que-no-es-la-suya · di tres diagnósticos antes de verificarlos y dos eran falsos | (a) Medí que un arreglo bajaba el trabajo de 182.435 a 17.976 buffers y concluí «33 veces mejor» sobre un prototipo al que le faltaban los joins de propiedad; con ellos el planificador cambia de hash join a merge join y la cifra real era 10x. Se lo dije a Andrés como 33x. (b) Después usé esos mismos buffers para afirmar que el arreglo «no había movido el arranque en frío». Los buffers cuentan accesos totales; el frío depende de páginas DISTINTAS traídas de disco, y lo que quité eran 3.992 visitas a las mismas páginas ya cargadas. La conclusión correcta llegó al mirar `pg_stat_statements`, que dijo `shared_blks_read = 0`: no había arranque en frío que valga. | **el eje, y es distinto de los del 11-08 de arriba**: no es que el instrumento tuviera un filtro no declarado ni que una pasada no sea una medida. Es que **el instrumento era correcto y la pregunta no era la suya**. Buffers contestan «cuánto trabajo total»; yo les pedí «cuánto tarda la primera vez». Los dos números eran ciertos y la inferencia entre ellos, falsa. | **lo que lo cazó**: en (a), aplicar la migración y volver a medir la función entera. En (b), que Andrés corrigiera de memoria un número del vault. Ninguna de las dos fue una relectura mía. | **candidato para la retro**: antes de inferir de una métrica a un síntoma, escribir en una línea qué NO puede detectar esa métrica. La regla ya existe en el `CLAUDE.md` de mintstash («di qué NO habría podido detectar») y no la apliqué al salto entre métrica y síntoma, solo a la métrica. | contexto: mintstash, rama código, cierre de I7

2026-08-11 | vault/medición-que-cambió-y-no-se-anotó | El vault decía que el arranque en frío de la lista de 3.992 tardaba 3,34 s (medido el 06-08). Comparé mi medición de hoy (3,56 s) contra ese número y reporté que el arreglo no había servido de nada. Andrés lo corrigió de memoria: entre medias había subido a ~6 s, y por eso se había subido el tope del rol `anon` a 8 s. **Esa segunda medición no se anotó en ninguna parte.** | **el coste**: un turno entero de conclusión equivocada, más un párrafo de «mi recomendación estaba mal» que también estaba mal. El número viejo no mentía sobre lo que midió; mentía sobre ser el vigente. | **la forma**: el vault guarda mediciones con fecha, pero nada obliga a que una medición que cambia sustituya a la anterior. Una cifra sin «medida el X, revisada el Y» se lee como actual. Y el cambio que la habría delatado —subir un timeout de 3 a 8 s— sí dejó rastro en una migración, pero no en la nota que citaba el número. | **candidato para la retro**: cuando una acción cambia una condición que el vault tiene medida (subir un límite, cambiar una cadencia), el delta de cierre incluye actualizar esa cifra, no solo contar la acción. | contexto: mintstash, rama código, cierre de I7

2026-08-11 | registro/densidad · Andrés pidió dos veces bajar la densidad del mismo reporte | «dumb it down, la lectura esta densa» y «haces muchas referencias a namespaces infrastructurales que no recuerdo atop of my head». Los reportes llevaban nombres de funciones SQL, nombres de extensiones de Postgres y unidades internas (buffers) sin traducir, en una sesión de varias horas donde Andrés estaba decidiendo producto, no leyendo código. | **la regla ya existe y está cargada**: «lenguaje llano por defecto» está inline en el `CLAUDE.md` de mintstash y en la memoria de usuario. Fallé igual, y el patrón es que la densidad crece con la profundidad de la investigación: cuanto más metido estás en el detalle, más se te olvida que el lector no lo está. | **lo que sugiere**: la regla está escrita como preferencia de estilo permanente, y el fallo es situacional —aparece en el reporte largo tras la inmersión larga—. Un recordatorio ligado al momento (al cerrar una investigación de más de N pasos) pegaría mejor que una preferencia general que ya está escrita y ya se ignora. | contexto: mintstash, rama código, cierre de I7

2026-08-11 | registro/densidad · TERCERA vez en tres días, así que deja de ser incidente | «sintetiza, dumb it down, la lectura se hizo pesada». Es el tercer día consecutivo con una entrada de esta familia: el 10-08 fueron dos peticiones en la misma sesión, el 11-08 por la tarde otras dos, y hoy una más. Las tres veces el contenido estaba verificado; lo que falló fue el tamaño y el orden. | **la enfermedad ya no es «la regla no disparó», es que la regla no tiene forma operativa.** «Lenguaje llano» y «TL;DR primero» son preferencias de estilo, y las cumplo cuando el reporte es corto, que es justo cuando no hacen falta. La hipótesis, ya escrita el 10-08 y ahora con tercera instancia: **la densidad crece en proporción al trabajo verificado que llevo detrás**, porque el reporte se me convierte en registro de lo hecho en vez de respuesta a lo preguntado. Hoy el reporte largo llegó justo después de la sesión con más verificación de las tres. | **candidato para la retro, y esta vez con forma de mecanismo y no de recordatorio:** un tope duro. El reporte por defecto son el veredicto, lo que hay que decidir y los huecos, y nada más; el recorrido de cómo se llegó ahí solo si se pide. Escribir primero la versión de cinco líneas y expandir bajo demanda, en vez de escribir la larga y confiar en resumirla. La regla de estilo lleva tres días cargada y tres días fallando: no le falta énfasis, le falta un límite que no dependa de mi juicio en el momento. | contexto: mintstash, rama código, quitar el porcentaje de «mis listas»

2026-08-11 | verify-source/dos varas en la misma frase · reporté el tiempo sin muestrear mientras muestreaba los buffers | Le dije a Andrés «abre en medio segundo en vez de en siete y medio». Los dos números eran **pasadas sueltas** bajo `EXPLAIN ANALYZE`. Lo cacé al medir una tercera vez tras aplicar la migración: el mismo plan dio 3.093 ms. Con tres llamadas limpias la media real es **210 ms** (min 196, max 234, desviación 17), y los buffers, que sí había tratado con cuidado, salieron **59.386 y 59.377**: cero varianza. | **el eje es nuevo y es incómodo:** no es que no conociera la regla —«una pasada no es una medida» está inline en el `CLAUDE.md` del repo y la había aplicado bien a los buffers ese mismo turno—. Es que **apliqué el rigor a la métrica estable y no a la que varía, en la misma frase**. La regla se me activó por el instrumento (EXPLAIN, buffers, el terreno donde la aprendí) y no por la propiedad que la justifica, que es la varianza. Una regla aprendida con un ejemplo se queda pegada al ejemplo. | **agravante de contexto:** el propio `docs/sql-views.md` ya avisaba de que este cómputo tiene varianza enorme («el mismo plan medido en 281 ms y 3.774 ms con la base ociosa»), y lo había leído esa sesión para corregir otro párrafo del mismo fichero. | **fix ya aplicado**, inline en el `CLAUDE.md` de mintstash: una cifra de tiempo se reporta con tres llamadas y su min/media/máximo o no se reporta como cifra, y para tiempo no se usa `EXPLAIN ANALYZE`, que instrumenta y engorda el número. | contexto: mintstash, rama código, quitar el porcentaje de «mis listas»

2026-08-11 | regla-del-repo-con-agujero · «llama a la función en la sesión en que la escribes» es inaplicable si solo la puede ejecutar service_role | El `CLAUDE.md` de mintstash lo pide, y con un caso real detrás: la 121 se creó sin error y murió en la primera llamada porque `EXPLAIN` no vale en una función no volátil. La 123 define dos funciones, y una es `list_read_cost`, que la propia migración REVOKEa de todo el mundo y GRANTea solo a `service_role`. Mi conexión no lo es: `permission denied for function`. Subí el peldaño por otro camino —corriendo a mano la consulta exacta que la sonda ejecuta— pero el que la regla pide no se puede subir. | **la forma:** una regla escrita desde un caso donde el agente sí podía llamar a la función, aplicada a un repo donde parte de las funciones son deliberadamente inalcanzables para él. No es que la regla esté mal; es que **no dice qué hacer cuando no se puede cumplir**, y el silencio se resuelve saltándosela. | **segunda pieza, menor y de la misma tarde:** el MCP de Supabase no mantiene la sesión entre llamadas, así que un `PREPARE` y su `EXECUTE` no se pueden encadenar. El `PREPARE` sí valida (corre y falla si no resuelve), pero el peldaño de arriba hay que subirlo con una consulta normal, no con el prepared. | **candidato para la retro:** que la regla lleve su salida de emergencia escrita — si la función no es llamable por el agente, se corre a mano su carga útil y se declara en el reporte cuál de los cuatro peldaños queda sin subir y por qué. | contexto: mintstash, rama código, migración 123

2026-08-11 | retry barato · inventé un valor de enum que estaba a un grep | Escribí una entrada de changelog con `"type": "changed"`. Los tipos permitidos son tres —`new`, `improved`, `fixed`— y están declarados en `src/shared/changelog.ts` con un test que los vigila. Lo cazó la suite en el primer intento y costó un ciclo de tests. | **la forma, y es la de siempre en su versión más barata:** rellené un campo por lo que me pareció razonable en vez de por lo que el repo declara. Un grep de cinco segundos. Nota para la retro: no propongo gate, ya existe y funcionó — el test lo cazó y lo nombró. Vale como recordatorio de que **el coste de no mirar la fuente no siempre es un bug; a veces es solo un ciclo, y por eso no duele lo suficiente como para corregir el hábito.** | contexto: mintstash, rama código, changelog de la 123

2026-08-11 | FIX PASADO QUE FUNCIONÓ · el aviso del vault escrito con nombre concreto me paró antes de medir en vacío | El estado del 11-08 (noche) dejaba escrito que quedaban seis casos sin probar, «entre ellos **un usuario con colección, que es el que cambia el plan**». Medí la primera versión sobre el usuario de las 24 listas y 7.976 items, que es el peor caso de volumen y **tiene la colección vacía**: la rama de propiedad se estaba midiendo en el aire, con la subconsulta devolviendo cero filas. Lo vi en el plan y volví a medir sobre el usuario que sí la ejercita. | **por qué esto es material de auditoría y no autobombo:** es la contrapartida exacta de la entrada del 10-08 sobre el prototipo incompleto, y de la del 11-08 sobre el número viejo del vault. La diferencia entre un aviso que funciona y uno que no parece estar en la especificidad: «faltan casos por probar» no habría disparado nada; **«un usuario con colección, que es el que cambia el plan» nombra el caso y dice qué se pierde si no se prueba.** | **para la retro:** si esto se confirma, el patrón a exigir en los hilos abiertos del cierre es nombrar el caso y su consecuencia, no enumerar pendientes. | contexto: mintstash, rama código, medición de get_list_summaries

2026-08-12 | retry/trabajo perdido · reverti con `git checkout` un fichero que no estaba en el indice, DOS veces en la misma sesion | Para comprobar que un test sabe ponerse rojo, mutaba el codigo a proposito y revertia con `git checkout -- <fichero>`. Ese comando restaura desde el indice, y mis cambios no estaban puestos: la primera vez me llevo las nueve marcas de `ListDetailPage`, la segunda la cabecera entera de `PublicCollectionPage`. Las dos veces rehice el trabajo a mano. | **lo que duele no es el fallo, es la reincidencia.** Tras la primera dije en voz alta «esta vez las dejo en el indice antes de mutar», lo hice bien dos veces seguidas, y a la hora volvi a mutar sin poner nada. Una leccion aprendida y no convertida en habito no es una leccion: fue una frase. | **candidato para la retro, con forma de mecanismo:** la comprobacion de rojo es un patron recurrente y frecuente (cuatro mutantes en esta sesion, y es la disciplina que el `CLAUDE.md` de mintstash exige). Merece un procedimiento fijo — `git stash` del mutante en vez de `checkout`, o `git add` obligatorio antes de mutar — y no depender de que me acuerde. La pista de que hace falta: el fallo no fue de conocimiento, fue de secuencia bajo carga. | contexto: mintstash, rama codigo, cabeceras de I1

2026-08-12 | regla-cargada-no-aplicada · corri `npx tsc --noEmit`, dije OK, y el build lo desmintio | Quite dos `as` de las fixtures de un test para que no taparan drift de tipos. `npx tsc --noEmit` paso limpio y lo reporte asi; `npm run build` fallo acto seguido: la fixture tenia un campo inventado (`addedAt`, que `ListRow` no tiene) y le faltaban cuatro columnas. | **el agravante es que la regla estaba a la vista y con su porque escrito.** El `CLAUDE.md` de mintstash dice literalmente que el build es `tsc -b`, recorre los tres proyectos referenciados, y que el `tsc --noEmit` pelado era «el check debil sentado al lado del fuerte, aparentando cubrir». Lo habia leido esa misma sesion al abrir la tarea. | **la forma:** use el comando mas rapido para una comprobacion intermedia y trate su verde como resultado. El coste real fue bajo —lo caza la bateria— pero reporte «tsc OK» a Andres, o sea que la afirmacion salio antes de la verificacion buena. | **para la retro:** de la familia de «una comprobacion parcial no es una comprobacion», con el eje de que aqui el instrumento debil esta explicitamente documentado como debil y aun asi se usa por rapidez. | contexto: mintstash, rama codigo, cabeceras de I1

2026-08-12 | instrumento/alcance-de-la-consulta · una puerta de test que contestaba «alguien ha cargado» cuando la pregunta era «he cargado yo» | El test que prohibe acciones de dueño en superficies compartidas monta tres paginas seguidas sin limpiar entre medias. Mis ayudas esperaban con `screen.getByText`, que mira el documento ENTERO, asi que el tercer render daba la carga por buena en cuanto el segundo enseñaba su texto: el test juzgaba un contenedor todavia en pantalla de carga, 1.116 caracteres. **Con una accion de dueño inyectada a proposito en la lista publica, ese test pasaba en VERDE** mientras el de conjunto se ponia rojo. Atado a su propio contenedor con `within`, se pone rojo. | **el eje es nuevo y no lo tenia cubierto:** las entradas del 11-08 son «el instrumento trae un filtro que no declara» y «una pasada no es una medida». Este es el ALCANCE de la consulta — la puerta miraba mas ancho de lo que le correspondia, asi que la satisfacia el trabajo de otro. Un gate global usado como gate local. | **lo que lo cazo, y es lo que hay que retener:** no lo vi leyendo el test. Lo vi porque **corri los mutantes hasta el final en vez de parar en el primero que salio rojo**. El mutante 3 puso rojo el test de conjunto y verde el negativo, y esa discrepancia entre dos guardas que deberian caer juntas fue la señal. | contexto: mintstash, rama codigo, linea base de cabeceras

2026-08-12 | instrumento/el-mutante-tambien-se-verifica · concluí que un test tenia un agujero y el fallo era mi mutante | Para comprobar que la linea base cazaba una accion de dueño colada en la coleccion compartida, inyecte la marca en `<PublicSignupCta>`. La suite siguio verde y estuve a un paso de reportar que esa superficie no estaba cubierta. **El componente no propaga props al DOM**, asi que la marca nunca llego: el mutante no mutaba nada. Con un elemento crudo, rojo inmediato. | **la forma:** un control negativo tambien es un instrumento, y yo lo estaba usando sin verificar que hacia lo que creia. Es exactamente el mismo error que el control positivo previene en la direccion contraria — pedirle primero que encuentre algo que sabes que esta —, aplicado al mutante en vez de a la busqueda. | **para la retro:** al comprobar que un gate sabe ponerse rojo, la mutacion se confirma antes de interpretar el resultado (que la marca llego al DOM, que la linea cambio, que el fichero se escribio). Un mutante que no muta se lee como cobertura que falta, o peor, como cobertura que sobra. | contexto: mintstash, rama codigo, linea base de cabeceras

2026-08-12 | regla-cargada-no-seguida · `npx vitest run` OTRA VEZ, y el sustituto estaba escrito en este mismo log | Corrí `npx vitest run scripts/price-invariants.test.ts` para iterar sobre un fichero suelto. Lo dije yo al reportar y después pasé `npm test` completo, así que no se perdió ninguna corrida. | **el agravante no es la regla, es el log.** La entrada del 11-08 de esta misma familia ya cerraba con el hallazgo: `npm test -- <fichero>` hace exactamente lo que quiero y sí graba, y está documentado en la cabecera de `scripts/run-tests.mjs`. O sea que el 11-08 se identificó la causa (la regla se escribió como prohibición sin sustituto) Y se encontró el sustituto. Hoy, con las dos cosas escritas, volví a teclear el comando prohibido. | **lo que esto descarta:** ya no es «falta la regla» (está inline), ni «falta el sustituto» (está encontrado), ni «falta el porqué» (está documentado con su coste). Queda que **nada en el momento de teclear conecta la intención «correr un fichero» con el comando correcto**. Las dos veces el disparador fue el mismo: querer feedback rápido sobre un fichero durante la iteración, no al cerrar. | **candidato para la retro, con forma de mecanismo y no de recordatorio:** es lo que un hook de PreToolUse sobre Bash puede cazar con un grep de una línea — `npx vitest` en el comando, bloquear y devolver `npm test -- <fichero>`. Es de las tres vías de «una regla existe si se carga», y esta lleva dos instancias documentadas en la vía que no funciona. | contexto: mintstash, rama código, invariantes tras la 123

2026-08-12 | edición dentro de un flujo largo sin leer su estructura de control | Añadí una salida temprana a un invariante escribiendo `results.push(...)` + `return`, que es la forma natural de «y aquí se acaba». Ese bloque no era una función suya: vivía dentro de un flujo que asigna a `pairingResult` y hace el `push` al final, así que mi `return` se habría llevado por delante los invariantes que venían después. Lo vi al releer para verificar, antes de correr nada, y lo reescribí como `if/else` sobre la variable. | **la forma:** edité por el patrón que la intención sugiere («salir de aquí») en vez de por la estructura que el sitio tiene. El Edit me daba las cuatro líneas de contexto que pedí y en esas cuatro líneas la estructura no se ve; el `let pairingResult` estaba treinta líneas más arriba y el `push` diez más abajo. | **lo que lo cazó, y es lo mismo que el 11-08:** releer con la pregunta «¿esto hace lo que creo?» antes de dar nada por hecho, no un test — ningún test lo habría cazado, porque los invariantes siguientes simplemente no habrían aparecido en el informe y el veredicto habría salido verde con menos filas. | **para la retro:** al insertar una salida temprana en código ajeno, leer primero cómo termina el bloque contenedor. Barato de decir; el mecanismo real sería preferir leer el bloque entero antes de editar dentro de funciones largas. | contexto: mintstash, rama código, invariantes tras la 123

2026-08-12 | límite-del-instrumento · segunda vez en dos días que `service_role` deja al agente fuera y el rodeo se reinventa | Para verificar el diagnóstico necesitaba `get_basket_pairing_probe` y, después, correr el script de invariantes entero. Las dos cosas son `service_role` a propósito, y el MCP no lo es: `permission denied for function`. El rodeo que usé fue leer la definición con `pg_get_functiondef` y ejecutar su cuerpo a mano contra las tablas base, que da el mismo dato y me dejó afirmar «ninguna de las 55 listas discrimina hoy». | **es la segunda instancia**: el 11-08 hay una entrada por lo mismo, con la 123 y `list_read_cost`, y allí el rodeo fue idéntico —correr a mano la carga útil de la función—. Dos veces el mismo obstáculo, dos veces la misma solución, cero veces escrita. | **lo que sugiere:** no es fricción que se pueda eliminar, porque el `service_role` está bien puesto: el agente NO debe poder correr esas funciones. Lo que falta es que el rodeo sea procedimiento en vez de invención — leer la definición, reproducir el cuerpo, y **declarar en el reporte qué quedó sin ejecutar y por qué**, que es lo que separa esto de una comprobación parcial disfrazada de completa. Hoy lo declaré; el 11-08 también. Si va a pasar cada vez que un invariante o una sonda se toquen, merece estar escrito donde se lea al empezar, no descubrirse al chocar. | contexto: mintstash, rama código, invariantes tras la 123

2026-08-12 | verify-source/miré al emisor y no al consumidor · propuse un SKIP sin comprobar qué hace el sistema con un SKIP | Un invariante se quedó sin el dato que usaba de árbitro y propuse declararlo saltado en vez de dejarlo reportando OK. Verifiqué a fondo el invariante, su árbitro, y medí las 55 listas para saber cuánta cobertura se perdía. **No abrí la función que clasifica los veredictos**, que está en el mismo fichero cincuenta líneas más arriba y trata un SKIP igual que un rojo, tanto para el exit code como para decidir si manda correo. Resultado: cambié un verde falso por un correo rojo cada mañana. Lo descubrí al lanzar el job a mano, no al diseñar el arreglo, y se lo tuve que decir a Andrés después de haberle reportado el arreglo como cerrado. | **el eje, y es distinto de los de ayer:** los del 11-08 son «el instrumento trae un filtro que no declara» y «una pasada no es una medida». Este es que **cambié el VALOR que un componente emite sin mirar qué hace el sistema con ese valor**. Toda la verificación fue aguas arriba —de dónde sale el dato, por qué es NULL, a quién más afecta— y cero aguas abajo. La pregunta que no me hice cabe en una línea: ¿quién lee lo que este componente devuelve, y le da igual el cambio? | **lo que lo cazó:** que Andrés dijera «lanzalo a mano y estate pendiente». Sin ese run yo habría cerrado la sesión con el arreglo dado por bueno y él habría recibido el correo mañana a las 6:30, sin relación aparente con lo de hoy. | **candidato para la retro:** al cambiar el veredicto, el código de salida o el valor de retorno de algo, grep de quién lo consume ANTES de dar el cambio por diseñado. Es la regla del espejo del `CLAUDE.md` de mintstash —que hoy mismo extendimos de filas a columnas— aplicada a una tercera cosa: los veredictos. Tres instancias de la misma forma en un día sugiere que la regla no es sobre filas ni columnas ni veredictos, sino sobre **cualquier cosa que alguien más lea**. | contexto: mintstash, rama código, invariantes tras la 123

2026-08-12 | FIX PASADO QUE FUNCIONÓ · dos lecciones escritas esta misma mañana se aplicaron por la tarde | (a) Usé `npm test -- <fichero>` para iterar sobre un fichero suelto, que es el sustituto que la entrada de esta mañana identificó, en vez del `npx vitest run` que llevo dos días tecleando. (b) Hice `git add -A` antes de mutar código a propósito para comprobar que un test sabe ponerse rojo, que es la lección de la entrada de ayer sobre perder trabajo con `git checkout --`. Las dos veces salió bien: la corrida quedó grabada, y el mutante se revirtió sin llevarse nada por delante. | **por qué lo anoto y no es autobombo:** el log está lleno de diagnósticos y casi vacío de comprobaciones de si los arreglos pegan. La diferencia entre estas dos y las que fallan parece ser **que ambas sustituyen un comando concreto por otro comando concreto**, no que pidan recordar un principio. «Nunca X» falló tres veces; «para este caso, teclea Y» funcionó a la primera. | **matiz que le quita fuerza, y hay que decirlo:** las dos se escribieron hace horas y en la misma sesión, o sea que estaban calientes en contexto. Esto no dice nada sobre si aguantan dentro de una semana, que es la pregunta que importa. La comprobación de verdad es la siguiente sesión que abra en frío. | contexto: mintstash, rama código, invariantes tras la 123

2026-08-12 | retry/alcance-real-de-un-comando-de-git · `git stash pop` me saco un stash ajeno, y es la SEGUNDA instancia de la misma forma en dos dias | Queria mover un fichero nuevo sin trackear a la rama de Josune, asi que hice `git stash push -- <fichero>` y despues `git checkout -b` y `git stash pop`. El push no hizo nada: `git stash push` con una ruta **no coge ficheros sin trackear** si no le pasas `-u`, y no avisa mas que con un «Did you forget to git add?» que lei y no interprete. El `pop` siguiente saco un stash de Andres de `feat/referral-attribution`, con conflicto en `.claude/launch.json`. No se perdio nada porque el pop fallo y el stash se quedo intacto, y lo verifique antes de seguir. | **es exactamente la familia de la entrada de ayer sobre `git checkout --`**: comandos de git cuyo alcance real no es el que la intencion sugiere. Y el eje comun de las dos instancias es mas estrecho de lo que parece: **las dos veces el objeto era un fichero sin trackear**. `checkout --` restaura desde el indice y el fichero no estaba; `stash push <ruta>` ignora lo no trackeado. En los dos casos el comando dijo que si (o no dijo nada) y el efecto fue otro. | **para la retro, con forma de mecanismo:** dos instancias en dos dias sobre el mismo objeto piden una regla que no sea «acuerdate», sino un habito con disparador claro — antes de cualquier comando de git que mueva o restaure un fichero que acabo de crear, `git add` primero. Es el mismo patron que funciono con `npm test -- <fichero>`: sustituir un comando concreto por otro, no recordar un principio. | contexto: mintstash, rama codigo/docs, review de la rama de Josune

2026-08-12 | reestructurar sin verificar que el contenido sobrevive · el documento perdio un item entero y no lo detecto nada | Andres pidio el brief de review en un orden concreto de cuatro apartados. Al reescribirlo, la pregunta sobre el alcance de los filtros («la seccion Proximamente no se filtra, y hay un test que lo fija a proposito») **se cayo del documento**. Verifique que la estructura fuera la pedida, que las etiquetas cuadraran, que no hubiera guiones largos, y no verifique lo unico que importaba: que los items de la version anterior siguieran todos dentro. | **lo cazo Andres, no yo**, y ademas por casualidad: contesto las tres decisiones que le habia planteado en el chat, y una de ellas era precisamente la que ya no estaba en el documento. Si hubiera contestado solo dos, Josune habria recibido un documento sin esa pregunta y nadie lo habria sabido. | **la forma, y es nueva en este log:** las entradas anteriores de esta familia son sobre instrumentos que miden menos de lo que dicen. Esta es sobre **una transformacion de formato tratada como si fuera de solo presentacion**. Reordenar es reescribir, y una reescritura puede perder carga. Verifique la forma nueva contra el pedido y no contra la version vieja. | **para la retro:** al reestructurar un documento que ya existe, la comprobacion no es leer el resultado, es **diff de items entre las dos versiones**. Barato: una lista de los N puntos antes, la misma lista despues, y que cuadre el conteo. | contexto: mintstash, rama docs, brief de review para Josune

2026-08-12 | mi propio instrumento contestaba una pregunta mas estrecha, y presente su verde como respuesta a otra | Reporte que Josune «no ensancho la cuenta de valores crudos» con los numeros del chequeo de tokens, identicos en su rama y en el merge-base. Eso es cierto y se lee como «respeto los design tokens», que es falso: el chequeo cuenta valores **distintos**, asi que reutilizar un crudo que ya esta en el fichero no mueve la cifra. Sus `mb={48}` son pixeles crudos con `--space-12` existiendo, y su gris es de la paleta por defecto de Mantine. | **el agravante es de quien era el instrumento.** Las entradas anteriores de esta familia son sobre gates y selectores ajenos que miraban mas estrecho de lo que declaraban. Aqui **elegi yo la metrica, la corri yo, y presente su resultado como respuesta a una pregunta que la metrica no contesta**. La pregunta era «uso bien los tokens» y la metrica contesta «cuantos valores distintos hay». | **lo que lo cazo:** que Andres preguntara explicitamente «ha usado bien los design tokens? y las reglas de arquitectura». Sin esa pregunta el brief se habria ido con un elogio que no se sostenia, y encima habria dejado sin decir dos cosas que si eran reales (TD-152 y TD-153 salieron de mirar al nivel correcto). | **para la retro:** antes de usar el numero de un chequeo como respuesta, escribir en una linea que pregunta contesta EXACTAMENTE ese numero, y compararla con la pregunta que se hizo. Si no son la misma frase, el numero es contexto y no respuesta. | contexto: mintstash, rama codigo, review de la rama de Josune

2026-08-12 | FIX PASADO QUE FUNCIONO · el control positivo corrido en los DOS sentidos evito dos reportes falsos seguidos | Sospeche que un test de Josune era flojo porque comparaba totales (`badges >= headings`) en vez de afirmar por version. En vez de reportarlo, lo mute. **Mutacion gruesa** (quitar del todo el descarte de versiones vacias): el test se pone ROJO, o sea que mi sospecha era falsa tal como la habia formulado. **Mutacion fina** (dejar que UNA sola cabecera quede vacia): los 12 tests pasan en VERDE con una cabecera vacia pintada en pantalla, que es el caso exacto que el contrato declara. | **por que lo anoto:** con solo la primera mutacion habria reportado «el test esta bien» y me habria callado un agujero real. Con solo la segunda habria reportado «el test no cubre nada», que es injusto y falso. **El hallazgo preciso solo existe porque corri las dos.** Es la leccion de la entrada de esta manana sobre verificar el mutante, aplicada en las dos direcciones en vez de una. | **lo que esto sugiere sobre la forma del arreglo:** las lecciones de este log que pegan son las que sustituyen un comando por otro. Esta es distinta: sustituye **una mutacion por dos**, gruesa y fina, y la señal esta en la discrepancia entre las dos. Es mas caro y no automatizable con un hook, asi que su unica via es estar escrito donde se lea al empezar una review. | contexto: mintstash, rama codigo, review de la rama de Josune

2026-08-13 | encuadre · traje como hallazgo lo que el vault ya predecía, y costó un turno | Al auditar Node 24 vi que el cron de invariantes de las 08:01 había salido rojo. Diagnostiqué bien y lo dije bien: no era regresión ni tenía que ver con Node, había corrido sobre `5f30ef1d` y el arreglo entró a las 11:42. Pero lo presenté como «un hallazgo lateral», y el PARA RETOMAR del vault que yo mismo había leído al abrir la sesión decía literalmente que mirar los dos runs de mañana «es la única prueba que falta». Andrés tuvo que preguntar «¿no hay nada en la vault que lo corrobore?». | **la forma es nueva en este log y no es de verificación: es de composición.** El retrieve se hizo, el estado se leyó, el dato se comprobó contra la fuente, y aun así la evidencia nueva se reportó como si el contexto no existiera. Todas las entradas anteriores son sobre instrumentos que miden mal; esta es sobre información correcta mal encuadrada, que consume la atención de Andrés en descartar una alarma que el vault ya había desactivado. | **para la retro:** antes de etiquetar algo como hallazgo, comprobar si el vault ya lo predice. Si lo predice, la frase no es «encontré X» sino «X ocurrió como el vault decía que ocurriría». Es barato y cambia lo que Andrés tiene que hacer con la frase: de investigar a asentir. | contexto: mintstash, rama código, auditoría de Node 24

2026-08-13 | el fixture probaba la mitad de su regla, y el fixture era mío | Escribí un check de versión de Node que imprime las tres versiones, con un test que afirma que la línea se ve «para poder leerla en el log del build». El test estaba verde. Pero `verify.sh` se traga la salida de una comprobación cuando pasa, así que en el build de Vercel —la única superficie donde importa saber qué Node eligió Vercel— el log solo enseñaba `✅`. El test decía la verdad sobre el script y una falsedad sobre la batería, que es quien lo ejecuta. | **el agravante es la distancia temporal.** La regla del fixture a medias está inline en el `CLAUDE.md` de mintstash desde el 13-08 por la mañana, o sea escrita esta misma semana, y la leí al abrir la sesión. Aun así probé el sujeto aislado y no el sujeto dentro de su envoltorio, que es exactamente la mitad que importaba. | **lo que lo cazó, y no fue el test:** ir a leer el log de Vercel para ver el valor con mis ojos. O sea la misma cosa que salvó lo de `.reveal` el 12-08 — querer verificarlo en la superficie real en vez de fiarse del verde. | **para la retro:** cuando un control se ejecuta DENTRO de otro (un check dentro de una batería, un script dentro de un workflow), el test del control no dice nada sobre lo que el envoltorio hace con su salida. Son dos sujetos y piden dos comprobaciones. | contexto: mintstash, rama código, auditoría de Node 24

2026-08-13 | FIX QUE FUNCIONÓ · el control se estrenó en rojo solo, en su primera hora y sobre una condición real | Terminé de escribir el gate que exige que el Node que corre coincida con `.nvmrc`, y el primer `git push` de la rama lo canceló el pre-push: «esto corre sobre Node 20.20.2 y el repo pide la 24.19.0». No era un fixture ni una mutación deliberada. Andrés había corrido `nvm alias default 24` un rato antes y su terminal nueva estaba en la 24; el shell de la sesión del agente conservaba su foto vieja del PATH y seguía en la 20, con todos mis `npm test` y `git push` dentro. | **por qué lo anoto:** el log está lleno de gates que se verificaron plantando la violación a mano. Éste no hizo falta verificarlo así, porque la violación ya estaba ahí y nadie lo sabía. Un control que en su primera hora caza algo que el autor no sabía que estaba pasando es la señal más limpia de que vigila algo real y no una idea. | **la lección de fondo, que se fue al vault de mintstash y no es sobre Node:** un cambio de entorno hecho a mitad de sesión vale para el humano y no para el agente. Cualquier instalación, variable o credencial nueva se queda fuera del shell ya abierto, y suponer que se comparte entorno produce el mismo verde falso que un instrumento descalibrado. | contexto: mintstash, rama código, auditoría de Node 24

2026-08-14 | el shell del agente arrancó en Node 20 otra vez, con el gotcha ya escrito de ayer | Abrí sesión, creé la rama y antes de correr nada pregunté `node -v`: 20.20.2, con el repo pidiendo la 24.19.0 en `.nvmrc` y en `engines`. Cargué nvm y seguí. No costó ningún verde falso porque lo miré antes de medir, pero es exactamente la misma situación que ayer, en sesión nueva y a las pocas horas. | **la forma, y es de cobertura de una regla, no de instrumento:** lo que la sesión de ayer dejó montado es **detección** —el gate de versión rechaza el push— y no **prevención**. El gotcha está escrito en `working-preferences#trampas-de-instrumento`, o sea en prosa que se lee al abrir si el retrieve entra a esa profundidad. Ayer lo cazó el gate en el push, después de haber corrido la batería entera sobre el runtime equivocado; hoy lo cacé yo por acordarme, que es justo el mecanismo que la regla «una regla existe si se carga» descarta. | **para la retro:** esto pide una de las tres vías, no una cuarta lectura. La más barata es que el propio arranque de cualquier tarea de rama código haga `node -v` contra `.nvmrc` y lo diga en una línea, antes del primer comando. Detectarlo en el push es tarde: entre medias hay una batería entera medida en otro runtime. | contexto: mintstash, rama código, cabecera compartida de I1

2026-08-14 | corrí una transformación mecánica sobre 37 ficheros sin haberla probado en uno | Añadir un puerto puso en rojo 37 tests que construían su árbol de dependencias a mano. Escribí un script que les insertaba un import y esparcía unos valores por defecto, y lo lancé sobre los 37 de golpe. Insertaba el import «después de la última línea que empieza por `import`», que es falso cuando un import ocupa varias líneas: concatenó dos imports en una línea y dejó ficheros sin parsear. Revertí los 37 con `git checkout --` y lo rehice poniendo el import como primera línea del fichero, que siempre es válido. | **la forma:** no es que el script tuviera un bug, es que **lo estrené sobre la población entera**. El repo tiene escrita la regla inversa para controles —«un control no está hecho hasta correrlo contra la población entera»— y aquí el error es el simétrico: una transformación se prueba en UNO y se mira el resultado antes de soltar los N-1. Verificarlo costaba un fichero y una lectura. | **lo que lo salvó de ser caro:** que todo estaba commiteado, así que revertir fue una línea y no perdí nada. Si hubiera lanzado el barrido con trabajo sin commitear encima, la vuelta atrás habría sido a mano. | **para la retro:** antes de un barrido sobre N ficheros, correrlo sobre 1, leer el diff de ese 1, y solo entonces los demás. Y no lanzarlo nunca con el árbol sucio. | contexto: mintstash, rama código, I4 fase 1

2026-08-14 | una regla escrita del `CLAUDE.md` promete una garantía que el repo no puede dar, y la perseguí | La regla del `withLatency` dice que un hook que refresca tras mutar debe tener un test que afirme que **cuando la mutación resuelve, quien lee ya tiene el valor nuevo**. Escribí ese test literalmente y salió intermitente: tres corridas alternando rojo y verde. Cambié el hook de `invalidateQueries` a `refetchQueries` pensando que la carrera era del dato; siguió intermitente. Lo que falta no es el dato —la caché lo tiene cuando la mutación resuelve— es el **render de React**, y el idioma del repo para eso (`await act(async () => {})`) tampoco lo cierra de forma determinista. Acabé afirmando lo que sí se puede afirmar, que el lector acaba enseñando lo guardado sin recargar. | **por qué esta entrada es nueva en el log:** todas las anteriores de esta familia son sobre **instrumentos** que miden menos de lo que declaran. Ésta es sobre una **regla escrita** que declara más de lo que su propio repo puede cumplir, y que además viene con una tabla de tres filas que da la impresión de estar medida. La fila «refetch disparado y no esperado → FALLA con withLatency» es cierta del mecanismo y no alcanzable como aserción estable en un test. | **el coste:** varias corridas de la suite entera y dos cambios de hook, uno de ellos revertido, persiguiendo una promesa en vez de dudar de ella. La regla «si lo medido y lo observado se contradicen, duda del instrumento primero» tiene un hermano que no está escrito: **duda también de la regla**. | **para la retro:** una regla del `CLAUDE.md` que afirma una garantía debería decir cómo se comprobó que es alcanzable. Si nadie la ha visto cumplirse de forma estable, es una intención y hay que escribirla como tal. | contexto: mintstash, rama código, I4 fase 1

2026-08-14 | `git add` con una ruta inexistente no encola NADA, y mi silenciador lo tapó | Al cerrar el vault pasé a `git add` seis rutas, dos de las cuales ya no existían porque yo mismo había movido esos ficheros a `archive/`. Git aborta el comando entero ante una ruta que no existe: **no encoló ninguna de las seis**. Como lo había escrito con `2>/dev/null` para tragarme un ruido distinto, no vi el error, y el `git commit` siguiente informó de que no había nada que commitear mientras yo leía otra cosa. Costó un turno. | **la forma es la familia que este log ya tiene dos veces** —`git checkout --` y `git stash push <ruta>`—: comandos de git cuyo alcance real no es el que la intención sugiere. Ésta añade un agravante que es mío y no de git: **el silenciador**. `2>/dev/null` convirtió un error explícito y perfectamente claro en un silencio. | **para la retro:** no redirigir stderr en comandos que escriben (`add`, `commit`, `mv`, `rm`); el silenciador es para lecturas ruidosas. Y tras un `git add` con rutas explícitas, mirar qué quedó encolado antes de commitear — `git diff --cached --name-only` cuesta nada y contesta la pregunta exacta. | contexto: mintstash-knowledge, cierre de sesión

2026-08-14 | el vigilante de CI corrió fuera del repo, y su fallo se leyó como un fallo de CI | Andrés pidió que le avisara al terminar la corrida de la v0.47.0, así que lancé `gh run watch <id>` en segundo plano. **El trabajo en segundo plano no hereda el directorio de las llamadas anteriores**, así que arrancó fuera del repo y `gh` murió con «failed to determine base repo». La notificación que llegó decía «background command failed with exit code 1». | **la forma, y es la que hace daño:** un vigilante roto se presenta con la misma cara que un sujeto roto. Leído deprisa, «el comando que vigila CI ha fallado» se lee como «CI ha fallado», y la conclusión correcta —CI seguía corriendo tan tranquilo y pasó en verde— solo apareció al abrir el fichero de salida. Es la familia de *un rojo no es información hasta saber sobre qué se pone*, con el eje nuevo de que **el instrumento y el sujeto comparten canal de error**. | **lo que lo salvó:** leer la salida antes de contársela a Andrés, en vez de reenviar el estado del comando. | **para la retro:** todo comando lanzado en segundo plano lleva su `cd` explícito, porque no hereda directorio. Y al reportar el resultado de un vigilante, distinguir siempre «el vigilante falló» de «el sujeto falló»: son dos frases distintas y el canal es el mismo. | contexto: mintstash, rama código, release v0.47.0

2026-08-14 | tres correcciones a documentos durables en una sesión, las tres por afirmar desde inferencia | Escribí en el vault que el brief de la fase 1 de I4 «también quedó en false» (estaba en `true` y archivado desde hacía días); que la desviación E5 de un smoke «cayó de rebote en el barrido del 05-08» (tenía commit propio del 04-08, `ee2537d`, con el síntoma literal en el título); y que el eje de artista sería redundante en las listas de Mei (falso: `axisState` ya esconde cualquier eje con un solo valor). Las tres se corrigieron el mismo día, dos porque Andrés preguntó. | **la forma común, y no es «me equivoqué»:** las tres veces razoné por parecido —esto se parece a aquella familia, luego cayó con ella— y **lo escribí en un sitio durable sin ir a mirar**. Un doc del vault se lee mañana como un hecho, no como una hipótesis, y no lleva marca de cuál de las dos era. La regla «verificar contra la fuente» estaba cargada y la apliqué al código que tocaba, no a lo que escribía sobre él. | **el coste real fue bajo y engaña:** las tres se cazaron en horas. Pero la que nadie hubiera preguntado se habría quedado. | **para la retro:** una afirmación sobre el pasado (cuándo se arregló algo, qué estado tiene otro fichero, si un arco cerró) es tan verificable como un número de la base — `git log`, `grep`, abrir el fichero— y cuesta segundos. Si va a un doc durable y no se ha mirado, se escribe como duda o no se escribe. | contexto: mintstash-knowledge, cierre de arcos e I4

2026-08-14 | escribí una entrada de deuda desde una lectura parcial y la publiqué; la tumbó una pregunta de Andrés | TD-163 decía que desde I4 cualquiera que encienda avisos mete su lista privada en la muestra que el chequeo diario lee cada mañana, y lo planteaba como problema de privacidad con plazo («antes de que I4 llegue a más gente; con la fase 2 es tarde»). Andrés preguntó: «la lista es privada, pero la notificación va a ellos mismos, ¿es un problema que sea privada?». Al ir a comprobarlo: el workflow **ya muestrea tres listas privadas al azar por granularidad**, por parámetro propio y a conciencia. Las suscripciones no abren ninguna puerta. Media entrada era falsa, plazo incluido. | **la forma:** escribí la entrada mirando `fetchPinnedListIds` y no el fichero donde vive, que declara el muestreo de privadas unas funciones más abajo. La regla del repo lo dice con estas palabras —*un fichero no es el repo, una tabla no es el esquema, un grep no es una lectura*— y le falta el escalón de abajo: **una función no es el fichero**. | **lo que más pesa:** una entrada de deuda con plazo dirige trabajo futuro. Ésta habría metido una tarea en el camino crítico de la fase 2 por un riesgo inexistente. | **para la retro:** antes de publicar una deuda que afirma un mecanismo, leer el fichero entero donde vive ese mecanismo, no la función. Y desconfiar especialmente cuando la entrada se escribe *de rebote* revisando otra cosa, que es como nació ésta. | contexto: mintstash, revisión del smoke de I4 fase 1

2026-08-14 | HALLAZGO · nada conecta la respuesta de un brief con la ficha de deuda que la esperaba | TD-55 —la unificación del detalle de carta, que bloquea el último entregable de la iniciativa— llevaba **seis días contestada por Claude Design** sin que su ficha tuviera una línea. Al buscar la segunda instancia apareció TD-102: dice «bloqueado en crit de Claude Design» mientras su brief está archivado y en `validated: true`, o sea veredicto consumido, **sin escribir qué se decidió**. | **por qué es mecanismo y no descuido:** el mismo día montamos un gate en el pre-push del vault que exige que cada brief declare su estado, y nació justo de esta enfermedad (de 18 briefs a CD, uno tenía sección de respuesta). Pero el gate vigila **el brief**. La deuda que estaba esperándolo es otro fichero, y **entre los dos no hay hilo**: un brief puede pasar a `validated: true` mientras N fichas siguen diciendo «bloqueado». | **la asimetría que lo hace silencioso:** el brief sabe que fue contestado; la ficha no sabe que su bloqueo se levantó. La información existe y no viaja. | **para la retro, y es una de las tres vías:** que un brief con respuesta **nombre qué desbloquea** (una línea `Desbloquea: TD-55, TD-102`) y que el gate exija esa línea cuando `validated` pasa a `true`. Con eso el hilo existe y se puede comprobar. Anotar estas dos a mano no impide la tercera. | contexto: mintstash-knowledge, preparación de I1

2026-08-14 | tercera vez de `nvm` en dos días, y la cura propuesta sigue sin existir | La batería completa salió roja en «versión de node»: el shell de la sesión en la 20.20.2 con el repo pidiendo la 24.19.0. Cargué nvm y volvió a verde. Es la tercera anotación de lo mismo en este log en dos días, y hoy pasó **dos veces en la misma sesión**, porque cada comando arranca su propio shell. | **lo que la repetición dice, que es lo único nuevo:** las dos entradas anteriores ya diagnosticaron la enfermedad y propusieron la cura —que el arranque de cualquier tarea de rama código haga `node -v` contra `.nvmrc` y lo diga en una línea—. La cura no se construyó, así que el gate sigue cazándolo tarde, después de haber corrido cosas sobre el runtime equivocado. | **para la retro:** esto ya no necesita más diagnóstico, necesita que alguien lo implemente o que se decida explícitamente vivir con ello. Una tercera entrada descriptiva no aporta nada más; la siguiente vez que aparezca, la anotación debería ser sobre por qué no se hizo. | contexto: mintstash, rama código, release v0.47.0

2026-08-13 | verify-source/capacidad (entrada traída por el brief del harness, ingerido 14-08) | afirmé que TEMPEST-STORE no tenía Taiga, Tundra ni Volcanic Island — falso: estaban, en PO/LP. La causa: el cruce contra wantslist aplicaba EN SILENCIO el suelo de condición de la lista, y reporté su resultado como inventario. Corregido por Andrés con dos capturas. | eje conocido: el instrumento contesta una pregunta más estrecha que la que se le hace, y el filtro no se declara. | contexto: Cowork 13-ago, MintStash; fuente: meta/brief-harness-2026-08-13.md.

2026-08-13 | verify-source/capacidad | conté 139 criaturas legendarias en la colección de Andrés; son 39 — la consulta no filtraba por `portfolio_id` y sumaba los holdings de TODOS los usuarios de MintStash. Lo cacé yo al verificar antes de escribir, pero el primer número ya había entrado en mi razonamiento. | forma: medir el universo equivocado y razonar sobre la cifra antes de verificarla. | contexto: Cowork 13-ago, MintStash; fuente: meta/brief-harness-2026-08-13.md.

2026-08-14 | dije «la batería entera está verde» habiendo corrido la de rama, que no ejecuta los tests | Al revisar `redesign/changelog-novedades` corrí `npm run verify:branch`, salió verde y lo reporté a Andrés como «corrí la batería entera con Node 24 y sale verde», concluyendo «la rama está bien». El nivel `rama` corre node, convenciones, tokens, lint y migraciones; **no corre la suite**. Dos tests llevaban en rojo desde el merge y solo aparecieron cuando fui a añadir los míos. | **la forma, y es de encuadre además de instrumento:** el repo tiene TRES niveles de batería y los tres imprimen el mismo `✅ todo verde`. La palabra «verde» no lleva dentro sobre qué se puso verde, y yo la trasladé a una afirmación de alcance mucho mayor («la rama está bien») sin decir qué nivel había corrido. Es hermana de la entrada del vigilante de CI: el sujeto y el instrumento comparten vocabulario. | **el coste:** Andrés leyó un reporte que decía que la rama estaba lista cuando tenía la suite roja, y lo corregí yo dos turnos más tarde, no él. | **para la retro:** al afirmar que algo está verde, nombrar el nivel en la misma frase. Y si la afirmación es sobre una rama entera, el nivel es `completo`; `verify:branch` no autoriza esa frase. Es barato: la salida ya imprime el nivel en su primera línea, solo hay que leerla y citarla. | contexto: mintstash, rama código, review de la rama de Josune

2026-08-14 | lancé `prettier` sin comprobar que el repo lo configurara, y me reescribió un fichero entero | Necesitaba reindentar el cuerpo de un `return` después de meter un componente contenedor, y tiré de `npx prettier --write` sobre el fichero. El repo **no tiene `.prettierrc`**, así que corrió con sus valores por defecto y convirtió todas las comillas simples del fichero en dobles, además de reflowear imports. El diff pasó de tres cambios a un fichero entero. Revertí con `git checkout --` y lo rehice con un script que solo añade dos espacios a las líneas del bloque. | **la forma:** una herramienta de formateo sin configuración del proyecto no formatea «como el proyecto», formatea **como ella**. Es la familia de comandos cuyo alcance real es mayor que la intención —`git add` con ruta inexistente, `git checkout --`— con el agravante de que aquí el alcance excesivo produce un diff plausible que compila y pasa los tests, así que un gate no lo caza. Solo lo caza mirar el diff. | **lo que lo salvó:** que estaba todo commiteado, así que revertir fue una línea. Segunda vez en dos días que eso me salva. | **para la retro:** antes de lanzar un formateador, comprobar si el repo lo declara (`ls .prettierrc* .editorconfig`, `grep prettier package.json`). Si no lo declara, no es la herramienta del repo y no se usa para un reindentado local. | contexto: mintstash, rama código, nits visuales de Novedades

2026-08-14 | arreglé el mismo acoplamiento tres veces, y las dos primeras curas parecían suficientes al escribirlas | Los tests de la sección «Próximamente» de la página de novedades dependían de qué hubiera hoy en `changelog.json`. Cura 1: saqué el contrato a una función pura con fixtures propios — correcta y se queda, pero dejé tres tests de página apoyados en los datos reales. Cura 2: hice esos tres **derivar** el tipo de entrada de los datos en vez de escribirlo a mano, que se siente robusto. Cura 3, forzada por el gate de la release: la fase 1 vacía esa sección al publicar, así que derivar de ella no vale de nada; la página tiene que **recibir** el changelog como entrada. | **la forma, y no es «me equivoqué dos veces»:** las dos primeras curas atacaban *el acoplamiento al VALOR* de los datos. La enfermedad era el acoplamiento a *que los datos existan*. Y lo que la destapó no fue pensar mejor, fue que **un paso del proceso borra a propósito lo que el test lee** — algo que ninguna de las dos curas podía ver porque las dos se validaron entre releases. | **la pregunta que las habría ahorrado:** ¿algún paso del flujo (release, migración, cron, barrido) ESCRIBE en lo que este test LEE? Si sí, el test necesita datos propios, y eso suele significar que el sujeto debe aceptarlos como entrada en vez de importarlos. | **para la retro:** al arreglar un test acoplado a datos reales, distinguir explícitamente las dos capas —valor y existencia— antes de elegir la cura. Y sospechar de cualquier cura validada solo en el estado tranquilo del sistema. | contexto: mintstash, rama código, release v0.48.0

2026-08-14 | mi reporte fue tan denso que Andrés tuvo que pedirme que lo simplificara, con la regla de lenguaje llano cargada | Entregué la review de la rama en el formato del contrato, con tablas de mediciones, secciones anidadas y el detalle de cada hallazgo. Andrés respondió: «esta muy denso y dificil de leer/entender tu reporte, sintetiza, dumb it down, que necesitas de mi? que pasos tenemos por delante para cerrar esto?». Reescrito en un tercio del tamaño, con tres bloques: qué pasó, qué necesito de ti, qué pasos quedan. | **la forma:** confundí **completitud** con **calidad de reporte**. Todo lo que escribí era cierto y verificado, y precisamente por eso lo metí todo: el trabajo de medir se convirtió en obligación de contarlo. El contrato de reporte pide «cuerpo conceptual», no inventario de evidencia, y la regla de registro llano ya está en el vault y la leí al abrir. | **la señal que ignoré:** las tres preguntas de Andrés eran «qué necesitas de mí» y «qué pasos quedan». Ninguna de las dos estaba en primera línea de mi reporte; estaban deducibles del cuerpo. Un reporte donde la acción hay que deducirla ya falló, independientemente de lo bien medido que esté. | **para la retro:** la evidencia medida va al mensaje de commit y al vault; al chat va el veredicto, la decisión que necesito y el siguiente paso. Si un reporte tiene más de una tabla de números, probablemente está escrito para mí y no para Andrés. | contexto: mintstash, rama código, review de la rama de Josune

2026-08-14 | HALLAZGO · una regla con dos condiciones y un gate que solo vigila una | La regla 05 de `docs/ui-rules.md` dice que `--text-2xs` (11px) es **solo para mayúsculas y siempre con `--tracking-wide`**. Las insignias de la página de novedades son 11px, en minúscula y sin tracking, y llevaban así desde la rama. El síntoma que las delató fue visual y llegó por Andrés: las píldoras se comían las colas de la «j» y la «g», porque el CSS del Badge de Mantine recorta la caja del texto de altura-de-mayúscula a línea-base, que es correcto **si el texto es mayúscula**. | **por qué es mecanismo:** el gate de tokens sí implementa la regla 05, pero solo su mitad de **suelo** (nada por debajo de 9px, nada fraccionario). La condición de mayúsculas no la mira nadie. O sea que la regla tiene dos afirmaciones y el control cubre una, y la descubierta llegó por un ojo humano dos releases después. Es la misma forma que `fw={700}` escondiendo 16 infracciones de 18 por no leer la prop de Mantine. | **el agravante, que es mío:** arreglé el síntoma con `overflow: visible` **antes** de leer la regla 05. Eso hace desaparecer el recorte y deja la infracción viva, o sea que tapé la única señal que había. Lo cacé al ir a escribir la trampa en `ui-rules.md` y encontrarme la regla ya escrita tres líneas más arriba de donde iba a poner mi nota. | **para la retro:** antes de arreglar un defecto visual, leer el normativo del área. Y al escribir un gate para una regla con varias condiciones, enumerar las condiciones y decir en el propio gate cuáles vigila y cuáles no. | contexto: mintstash, rama código, nits visuales de Novedades

2026-08-15 | Andrés tuvo que pedirme «dumb it down» DOS veces en la misma sesión, y ya hay una entrada igual del 14-08 | La primera fue un reporte denso de estado; la segunda, peor, fue explicándole un plan: le describí el mecanismo interno (`wired`, una lista por componente que declara qué capacidades pinta de verdad) cuando él solo necesitaba saber que dos funciones estaban construidas en el sitio equivocado. Respondió: «me has confundido con tu descripción, dumb it down, es mas importante que yo entienda». | **por qué es entrada y no repetición:** la del 14-08 diagnosticó «confundí completitud con calidad de reporte» y su cura era sobre REPORTES. Ésta ocurrió explicando un PLAN, que es un género distinto: en un reporte el riesgo es contarlo todo, y en un plan es contarlo desde dentro. Le expliqué la implementación cuando la decisión que le pedía era de producto. La cura de ayer no cubría este caso porque no lo contemplaba. | **la señal que ignoré:** ya me había pedido «dumb it down» hacía pocos turnos. Después de una petición así, el listón baja para el resto de la sesión y yo lo devolví a su sitio en cuanto el tema se puso técnico. | **para la retro:** al explicar un plan, la unidad no es el mecanismo sino la consecuencia para el usuario. Y un `dumb it down` no expira al turno siguiente. | contexto: mintstash, rama código, arco I1

2026-08-15 | propuse un plan que contradecía el objetivo de la iniciativa que había leído esa misma sesión | Al abrir el cableado de datos propuse «llevar el selector de columnas y la selección múltiple a las cuatro superficies». Andrés: «porqué estás llevando funcionalidades a 4 superficies si lo que queremos es que sean una que se fragmente en varios sitios?». Tenía razón: la iniciativa se llama literalmente *una sola vista*, la había leído en el retrieve de apertura, y aun así propuse portar función por función. | **la forma:** leí el objetivo y planifiqué desde el estado actual en vez de desde el objetivo. El código presentaba cuatro superficies, así que razoné en cuatro. El nombre de la iniciativa era la corrección y estaba a la vista. | **lo que lo agrava:** el propio repo tenía la respuesta escrita en un sitio —`surface.ts` ya construye una colección como una lista con propósito de guardar— y yo lo leí DESPUÉS de que Andrés me corrigiera, no antes. La contradicción entre el modelo y los componentes era medible en cualquier momento. | **el valor:** de esa corrección salió la decisión de fondo de la sesión (`decision-una-coleccion-es-una-lista`), que desaparcó TD-58 y cambió el plan de las próximas semanas. O sea que la fricción produjo lo mejor de la sesión. | **para la retro:** al planificar dentro de una iniciativa, releer su criterio de terminado ANTES de proponer los pasos, no solo su estado. Y si el plan consiste en repetir algo N veces, sospechar. | contexto: mintstash, rama código, arco I1

2026-08-15 | tres vigilantes de fondo que no podían leer lo que vigilaban, en una sola sesión | `until [ -f /dev/null ]` (siempre falso: `-f` es fichero regular y `/dev/null` es un dispositivo) dando vueltas 36 minutos bajo la etiqueta «short wait», y lo vio Andrés; `gh run view` lanzado desde un directorio sin repo, que devuelve vacío para siempre; y `curl | grep` de un número de versión sobre una página que lo pinta en cliente. | **la forma, y es de proceso además de instrumento:** un vigilante roto **no falla, se calla**, y el silencio es indistinguible de «la cosa aún no ha ocurrido». No hay señal que lo delate. Los otros instrumentos rotos de este log al menos daban un número equivocado. | **la cura, que es de una línea y la apliqué a la tercera:** correr la comprobación UNA vez y mirar la salida antes de armar el bucle. | **para la retro:** el arnés podría avisar de un `until` de fondo que lleva N minutos sin cambiar de estado; hoy nada lo hace y depende de que a alguien le llame la atención el contador. | contexto: mintstash, rama código, release v0.48.1

2026-08-15 | escribí un fixture decorativo y solo lo destapó envenenar el código | Al curar un test que la release rompía, monté un fixture de changelog con diez versiones de un mes y una de otro. El test pasaba. Fui a envenenarlo —cambié la clave de agrupación a una constante— y **siguió verde**: con ese reparto cada página tenía un solo mes, así que agrupar bien o mal daba lo mismo. Rehecho con seis y seis, los dos venenos lo tumban. | **la forma:** construí el fixture para que el test pasara, no para que pudiera fallar. Es la regla «un fixture que no se ha visto en rojo no prueba nada» aplicada a mí mismo — y la regla ya estaba en el `CLAUDE.md` del repo, cargada. Saberla no impidió escribirlo; lo impidió ejecutar el veneno. | **para la retro:** el veneno no es la verificación final de un test nuevo, es parte de escribirlo. Y el orden importa: envenenar ANTES de dar el fixture por bueno, no después de reportarlo. | contexto: mintstash, rama código, release v0.48.1

2026-08-15 | HUECO DE HERRAMIENTA (no error mío) · no puedo pulsar en el navegador integrado, así que no pude correr el smoke | Con la sesión de Andrés abierta en el navegador integrado, mis clics informan coordenadas y **no llegan a la página**: comprobado dos veces con elementos distintos (una pestaña que no cambia y un enlace de navegación que no navega), y por `ref` además de por coordenadas. Sí funcionan navegar, leer el DOM, medir geometría, capturar y disparar eventos. | **la consecuencia:** un smoke que no puede pulsar no prueba lo que ese smoke tenía que probar —interacción—, así que hubo que delegarlo a Cowork con un prompt escrito. Correcto, pero es un ida y vuelta que no habría hecho falta. | **lo que sí pude hacer:** verificar geometría, capacidades y estados por DOM, y disparar el evento de clic para comprobar cableado. Sirve para «esto está bien conectado», no para «esto se usa bien». | **para la retro:** merece medirse si es estable o intermitente; si es estable, el modelo de smoke delegado deja de ser una opción y pasa a ser el único. | contexto: mintstash, rama código, smoke de la rama de superficies
