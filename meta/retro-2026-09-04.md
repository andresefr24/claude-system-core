# Retro 2026-09-04 — ventana 08-30 → 09-04

Corrida automática (scheduled task). Write autorizado: este fichero + filas en `pending-decisions.md`.
Nada más tocado: ni `rules/`, ni `skills/`, ni versiones, ni `CHANGELOG.md`, ni vaults de proyecto.

---

## TL;DR

El sistema **escribió 25 observaciones y aterrizó 1 cambio de proceso** desde el 14 de agosto.
Ese es el número de la semana y gobierna todo lo demás: 0 de los 5 builds aprobados el 08-14
han aterrizado en 21 días, 6 propuestas siguen sin decisión (3 de ellas 14 días), y Cowork
sigue cargando **v0.5.0 contra v0.15.0 del repo** — tercera medición semanal, mismo valor.

La propuesta que predijo esto —**contrapresión entre la retro y la cola de builds**— lleva
7 días en el buzón sin decidir, y su condición de disparo («filas aprobadas sin construir de
hace >2 semanas») **se cumple hoy**. Así que esta retro se porta como si estuviera aprobada:
**una sola propuesta nueva**, y el resto es la cola ordenada.

Diagnóstico nuevo, en una línea: **el arranque de sesión da por sabidas dos cosas que nadie
le dice** — a qué proyecto se aplica, y si hay otro agente escribiendo en el mismo árbol.
5 instancias en 3 días, cobertura cero en el buzón.

---

## Necesito que decidas

1. **La cola, primero.** ¿Se vacía o se da de baja? Hay 5 builds aprobados el 08-14 con 21 días
   y 3 filas del 08-01 que aún dicen «falta commit». Con la fila de contrapresión (08-28)
   decidida, esto deja de repetirse; sin ella, la retro del 11-09 volverá a escribir este párrafo.
2. **P2 (nueva): precondición de arranque.** ¿Se aprueba el sustituto mecánico de dos preguntas
   en `session-open` (proyecto declarado + árbol exclusivo)?
3. **P3: enmienda a «Calibración antes de la lectura»** (fila 08-28, pendiente). ¿Se aprueba con
   los tres ejes nuevos que las 10 instancias de esta semana exponen, o se decide tal como está?

---

## Capa 1 — Síntomas (se cuentan)

26 entradas de friction-log en la ventana: 08-30 ×5 · 09-02 ×4 · 09-03 ×7 · 09-04 ×10.

| Familia | Instancias | Entradas |
|---|---|---|
| El instrumento no puede contestar la pregunta que se le hace | **10** | 08-30 (techos de coste inventados) · 09-02 (`updated_at` ciego al escritor) · 09-03 (`find` sin `-L`; grep por asunto de commit; hash circular) · 09-04 (RLS invisible a los 3 instrumentos; linter ≠ causa; registro propio ≠ mundo; `.limit` vs corte de PostgREST; sonda que confirma) |
| Aislamiento y durabilidad razonados sobre el sujeto que la tarea NOMBRA | **5** | 08-30 (`git init` en pre-push rompe el `.git/config` compartido, ×2 entradas) · 09-03 (entorno en temporal de sesión, consumidor posterior) · 09-04 (dos agentes en un solo vault; apertura sin proyecto; sesión paralela descubierta en el `push`, ×2 en media hora) |
| Patrón antes que reparación | **3** | 08-30 (cerrar «las N encontradas» ×4 seguidas, changelog falso publicado en v0.52.5; protección escrita solo para TypeScript) · 09-02 (fillfactor: cura en la capa del síntoma, migración 190 deshecha en un día) |
| Registro / forma del reporte | **2** | 09-02 (doble «for dummies» en el mismo hilo) · 09-03 (reporte ordenado por MI incertidumbre, no por el estado de las cosas) |
| Herramienta usada fuera de su contrato de invocación | 2 | 09-04 (`bash .githooks/pre-push` a mano; `npx` dentro del timeout del trabajo) |

Las dos últimas entradas las marca el propio log como «no merece regla». Se respeta ese juicio:
no generan propuesta. Quedan como línea de observación.

## Capa 2 — Enfermedades (se nombran)

**E1 — El productor de proceso está automatizado y el consumidor no.**
Ya nombrada el 08-28. La medida de hoy la vuelve irrefutable: 26 commits desde el 14-08,
**25 son entradas de friction-log y 1 toca `plugins/core`** (`056cd29`, y no venía del buzón).
El tope de 3 propuestas limita la producción, no la cola. La retro corre sola; construir se lo
tiene que acordar alguien.

**E2 — La sonda se elige por disponibilidad y por nombre, no por si puede contestar la pregunta.**
`verify-source` (punto 5, v0.14.0) audita la **lectura** cuando lo medido contradice lo observado.
La fila pendiente del 08-28 audita la lectura **contra un caso conocido**. Ninguna de las dos
audita la **elección**, que es donde fallan las 10 de esta semana. Y la entrada del 09-04 sobre
la sonda que confirma expone la asimetría exacta: se duda del instrumento cuando el resultado
incomoda, nunca cuando confirma. Tres ejes que la fila pendiente no lleva:
- **la elección se justifica contra la pregunta** («¿qué otra cosa podría producir este mismo
  resultado?») antes de leer el número, no después;
- **quién toma la medida es parte del instrumento** — RLS, `service_role`, `SECURITY DEFINER`:
  el número sale perfecto y describe un camino que nadie recorre. Cinco días midiendo el camino
  que no fallaba, y una spec de 800 líneas sobre una pantalla que ningún dueño ve;
- **la simetría** — una sonda que confirma recibe la misma segunda pasada que una que contradice.

**E3 — El arranque da por sabido su propio sujeto.** *(nueva, sin cobertura en el buzón)*
`route-task` enruta por tipo de output; la apertura lee el vault **del proyecto**. Entre las dos
falta elegir proyecto, que es un dato que ninguna pide y las dos necesitan. Con un proyecto por
directorio no se ve; con un directorio paraguas —`~/Documents`, tres vaults— se ve siempre.
La misma forma, un eje más allá: el aislamiento se razona sobre el repo que la tarea nombra y no
sobre **todos los repos que la tarea toca**. Un worktree separa los checkouts de código; el vault
es un repo aparte y uno solo, y lo escriben las dos ramas **por construcción**, porque el ritual
de cierre escribe ahí. Coste real esta semana: cero, por suerte. El modo de fallo que el log
nombra es un `git add -A` que se lleve trabajo a medias de otra sesión.

**E4 — Sin señal nueva atribuible.** Las familias de patrón-antes-que-reparación y de registro
fallaron con la regla cargada **inline en el `CLAUDE.md` del proyecto**, no vía plugin. Sus curas
de plugin (`patterns.md`, tope estructural de `report-contract`) son v0.15.0 y **no están
cargadas en Cowork**, así que no se les puede imputar el fallo ni acreditar la mejora. Ver capa 3.

## Capa 3 — Auto-auditoría

**Lo que se puede medir:**

| Fix | Estado | Veredicto |
|---|---|---|
| Sincronía `marketplace.json` ↔ `plugin.json` (disciplina de entrega, v0.14.2) | ambos en `0.15.0` | **Aguantó** |
| «La retro commitea lo que escribe» (corolario barato, 08-28) | `retro-2026-08-21.md`, `-08-28.md` y `pending-decisions.md` todos *tracked*; árbol limpio | **Aterrizó.** Único cambio de proceso de la ventana que se cerró |
| Core silencioso por defecto (aprobado 08-14) | `vault-freshness.sh` sigue emitiendo texto cuando no encuentra vault | **No construido**, 21 días |
| Criterio enforzable/solo-criterio en `rules-have-loaders.sh` (aprobado 08-14) | sin clasificación en el script | **No construido**, 21 días |
| Gate `node -v` vs `.nvmrc` (aprobado 08-14) | sin rastro en el plugin | **No construido**, 21 días |
| 3 filas del 08-01 con «falta commit (Mac/CC)» | siguen diciéndolo | **34 días** |

**Lo que NO se puede medir, y es el hueco de siempre:** el plugin cargado en Cowork es
**v0.5.0**; el repo va por **v0.15.0**. Cargadas 6 reglas (`ingest`, `report-contract`, `retro`,
`routing`, `session-close`, `session-open`); ausentes `comms`, `inception`, `patterns`,
`registro`, `verify-source` y el directorio `hooks/` **entero**. Tercera semana consecutiva con
la capa 3 comida por esto. Superficies consultadas: `.remote-plugins/plugin_0118KkTYNnaWLPhdd8viUtcG/.claude-plugin/plugin.json`
→ `"version": "0.5.0"`; `ls` de su `rules/` → 6 ficheros; `ls` de su `hooks/` → no existe.

Un detalle que conviene ver: esta corrida siguió el `retro.md` **del repo** porque lo leí por
ruta explícita. Una corrida que se dejara llevar por la skill cargada habría seguido el de
v0.5.0 — sin loop de decisión, sin auditor externo, sin «la entrega es parte del cambio».

## Barrido del vault (a grosso modo)

- `friction-log.md`: 257 KB en 385 líneas, ~26 entradas nuevas por semana de ~1,5 KB cada una.
  Sigue siendo el mejor instrumento del sistema y el único que se alimenta solo, pero es
  **append-only** y la retro lo lee entero cada semana. Las entradas cuya enfermedad ya está
  curada o ya tiene fila decidida han dejado de ser load-bearing para retros futuras. Hubo un
  precedente de compactación (07-25). **Señalado, no propuesto** — el tope de 3 manda, y este
  no compite con los tres de arriba.
- `pending-decisions.md`: 33 filas, 6 pendientes. Sano como archivo; el problema no es el fichero.
- `skill-candidates.md`: **ningún candidato cruzó su umbral esta semana.**
  `capture-audit` sin evidencia de volumen. *Enforcement general* sigue sin señal en sesión
  fresca: la única candidata (09-03, tratar el resumen comprimido como si fuera el vault) es
  sesión heredada, el mismo caveat que lo tiene aparcado desde el 07-25. *Ops de volumen* sigue
  en 1 ocurrencia.
- `meta/insights/`: el informe más reciente es del **2026-08-05, 30 días**. Ya lo cruzó la retro
  del 08-07. Toca correr `/insights` en Claude Code y dejar el HTML en la carpeta: sin auditor
  externo fresco, el diagnóstico de arriba no tiene contraste independiente.

---

## Las tres propuestas

### P1 — Decidir la contrapresión (re-lista, fila 08-28)

No es propuesta nueva: es la fila del 08-28 con la segunda semana de datos. Evidencia añadida
hoy: **25 commits de observación por 1 de proceso** desde el 14-08; 0 de 5 builds del 08-14
aterrizados en 21 días, los tres verificables comprobados uno a uno contra el árbol; 0 decisiones
en la ventana. La condición que la propia fila define —«filas aprobadas sin construir de hace
>2 semanas»— **se cumple**. Ataca E1. *Recomendación: decidir esta primera; gobierna a las otras dos.*

### P2 — Precondición de arranque: proyecto declarado y árbol exclusivo *(NUEVA)*

Dos preguntas de respuesta mecánica al abrir, antes del primer paso del ritual. No prosa nueva:
sustituto concreto, en la línea de la escalera de `patterns.md`.

- **(a) ¿Qué proyecto?** Si el directorio de arranque contiene más de un `active-context.md` en
  su árbol, el ritual **no adivina**: lista los candidatos, dice cuál ha rozado y por qué, y deja
  que Andrés lo fije. Es literalmente lo que funcionó el 09-04, elevado a paso.
- **(b) ¿Estoy solo en este árbol?** En todo repo al que la sesión vaya a **escribir** —y el vault
  cuenta, porque el cierre escribe ahí—: `git status` y `git log origin/main..main --name-only`
  al **abrir**, y declarar en el primer mensaje si hay trabajo ajeno. Descubrirlo en el `push` es
  el peor momento posible: es cuando la acción ya va hacia fuera y arrastra trabajo de otro.
- **(c) Corolario de una línea:** la decisión de aislamiento enumera **todos** los repos que la
  tarea toca, no solo el que la tarea nombra.

**Evidencia:** 5 instancias en 3 días (08-30 ×2, 09-03 ×1, 09-04 ×3, una de ellas repetida media
hora después en la misma sesión). Cobertura en el buzón: **ninguna** — la fila del 08-07 sobre
proyecto→esquema de vault resuelve *qué fichero hace de `decisions.md`*, no *qué proyecto es*.
Ataca E3. *Recomendación: aprobar.*

### P3 — Enmienda a «Calibración antes de la lectura» (fila 08-28, pendiente)

La fila sigue siendo correcta y ahora va corta. Sus tres formas canónicas (ausencia · número que
acusa a tu propio cambio · test nuevo) cubren la calibración, y **10 instancias nuevas** exponen
tres ejes que no cubren: la **elección** del instrumento, **quién** toma la medida, y la
**simetría** entre confirmar y contradecir. Texto propuesto en la fila del buzón. Evidencia
acumulada: 8 instancias en la ventana anterior + 10 en esta = **18 en 14 días**, con
`verify-source` cargada inline en el repo donde ocurrieron. Ataca E2. *Recomendación: aprobar
enmendada; si se prefiere no ampliar el alcance, aprobar la fila tal cual y abrir los tres ejes
como fila aparte — pero no dejarla otra semana sin decidir.*

---

## Confianza y huecos

- **Alta** en la capa 3 y en E1: todo son medidas directas sobre el árbol y el `git log`, y las
  cito con la superficie consultada. Los tres builds del 08-14 los comprobé uno a uno; los dos
  lado-CC (bloqueo de `npx vitest`, disciplina de git-escritura) **no los puedo ver desde aquí**
  y los cuento como no construidos por asociación con la misma sesión encolada — es inferencia,
  no medida.
- **Media en la capa 1, y el hueco es del instrumento, no del conteo.** No leí transcripciones
  crudas: conté sobre el friction-log, así que mi cuenta de síntomas **hereda su cobertura**.
  El log lo alimenta el cierre de sesión, luego una sesión que no cerró no dejó rastro y yo no
  puedo saber cuántas fueron. Es exactamente la clase de ceguera que E2 describe, aplicada a
  esta retro, y la declaro en vez de dejarla implícita.
- **E4 es una abstención, no un hallazgo.** Con Cowork en v0.5.0 no puedo atribuir ni acreditar
  nada a los fixes v0.11.0–v0.15.0 en ese runtime. Tercera semana igual.
- **Sin contraste externo.** El informe de `/insights` tiene 30 días. Convergencia y divergencia
  con un auditor independiente es la mitad del valor de la capa 2, y esta semana no la tengo.
